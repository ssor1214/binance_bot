# 핸드오프 2026-08-28 — e3 CM 스캘퍼 / 구현 감사

전날 핸드오프: [HANDOFF_2026-08-27.md](HANDOFF_2026-08-27.md)

오늘 한 일은 **튜닝이 아니라 감사**다. 진입 축에 엣지가 있는지 재고,
CM_Ultimate_MA_MTF_V2 구현이 원본과 맞는지 대조했다. 결론은 아래 두 줄이다.

- **진입 축에 비용을 덮을 엣지가 없다.** 22개 구성이 전부 같은 띠 안이었다.
- **구현 버그는 실재하지만 손실의 원인이 아니다.** 다 고쳐도 격차가 8배 남는다.

라이브 설정은 **아무것도 바꾸지 않았다.** 코드 변경은 아래 5-1(잠복 lookahead) 하나뿐이고
그 경로는 현재 꺼져 있어 매매 동작에 영향이 없다(대조 검증 완료).

---

# 1. 오늘의 핵심 측정 — 수수료 **전** 엣지가 이미 음수다

원장 `logs/scalp_bot_e3_cm_ledger.jsonl` (dry_run 제외 840건):

```
구간        수수료전(명목대비)   수수료     순      t
전체 840      -0.0788%        0.0423%  -151.42  -3.40
최근 400      -0.0493%        0.0381%   -58.82  -1.66
최근 200      -0.0324%        0.0370%   -24.94  -0.97
최근 100      -0.0129%        0.0340%    -9.70  -0.43
```

**"엣지는 있는데 수수료가 먹는다"가 아니다. 수수료를 빼기 전에 이미 마이너스다.**
t값이 단조 개선 중이지만, 이건 전략이 좋아진 게 아니라 표본 구간이 상승장이라
롱 베타를 먹은 것으로 설명된다(아래 3장).

부수 사실(최근 200건): 승률 61.0%인데 손익비 0.53 → 본전 필요승률 65.2%.
0.53 은 설정값 `--cm-tp-max-roe 4.3 / --stop-fixed-roe 8.0` = 0.54 와 일치한다.
**사이징은 승/패 균일(0.97배)이라 원인이 아니다.**

---

# 2. 백테스트 — B-1(진입 축) / B-2(익절 상한) 둘 다 기각

새로 만든 하네스(2026-08-27 하네스는 커밋 누락으로 유실됐다):

- [scripts/cm_fetch_cache.py](scripts/cm_fetch_cache.py) — 공개 klines 전용 수집기.
  **인증 엔드포인트 0회, API 키 미사용**, 호출 간 0.25초, 총 336 호출.
  84심볼 3분봉 4,500개(9.4일) + 4시간봉 300개.
- [scripts/cm_backtest.py](scripts/cm_backtest.py) — CM 하네스. `scalp_bot_e3.py` 를 import 해
  `_series_hma` / `cm_tp_price` / `cap_tp_roe` / `pullback_depth_pct` 를 **원본 그대로** 호출.
- [scripts/cm_signal_edge.py](scripts/cm_signal_edge.py) — 청산규칙 제거한 순수 방향성 엣지 + 드리프트 중립화.

## 검증 게이트는 **통과**했다 (지난 하네스와 달리)

기준을 08-27 13:00 이후 137건으로 잡았다 — `--stop-fixed-roe 8.0` 이 그때부터
효력이 생겨서, 그 이전이 섞인 "최근 200/400건"은 baseline 이 아니다.

```
항목              라이브(n=137)   하네스(n=1,248)   오차
TP_LIMIT 비중       67.2%          68.5%          +1.3%p
STOP 비중           32.8%          31.5%          -1.3%p
익절 ROE 중앙       +4.19~4.25     +4.30          +0.05
손절 ROE 중앙       -7.97          -8.00          -0.03
거래빈도            9.3건/h        5.9건/h        -37% (미달)
```

지난 하네스의 실패 지점(TP 체결률 90% vs 라이브 54%)은 **재발하지 않았다.**
미달은 거래빈도 하나이고 원인이 구조적으로 명확하다: 라이브는 10초 폴링으로
형성 중인 봉의 EMA5 터치를 잡는데 하네스는 3분 마감 단위다. 진입 **가격**은 같고
진입 **기회 수**만 적게 잡히므로 **상대비교로만 쓸 것. 절대 거래수를 라이브 예측으로 쓰지 말 것.**

## B-1 진입 축 — t>2 양수 구성 0개

```
구성                     건/h   수수료전엣지    t
(a) 눌림대기 [현행]      5.9    -0.0358      -1.16
(b) 즉시진입             5.2    -0.0814      -2.35   <- 유일하게 유의하게 나쁨
(c) 눌림만료 2봉/1봉     5.5/5.2 -0.0550/-0.0457
(d) 눌림깊이 0.5%/0.3%   4.1/3.7 -0.0405/-0.0549
(e) flip 1/2/3/5/-1      4.9~5.9 -0.040/-0.055/-0.030/-0.036/-0.031
(f) HTF필터 OFF          5.1    +0.0339      +1.02
(g) require_cm_tp ON    17.8    -0.0856      -6.52   <- 12개 중 최악
```

12개가 전부 -0.086 ~ +0.034 의 좁은 띠 안이고, 그 폭이 표준오차와 같다.
**진입 방식을 어떻게 바꿔도 측정 가능한 차이가 없다.**

- **`--require-cm-tp` 는 절대 켜지 말 것.** 거래수를 3배로 늘려 원칙 1에 가장 매력적으로
  보이지만 엣지가 최악이다. 전형적인 "빈도만 늘고 질이 무너지는" 함정.
- **e3(flip 5) vs e4(flip -1) A/B 는 이 표본으로 결론이 안 난다** (-0.0358 vs -0.0310,
  차이가 표준오차의 1/5). 표본을 더 쌓아도 무의미하므로 **조기 종료 권고.**
  CLAUDE.md 의 "그림자 태그를 e4 로 재기동" 미완료 항목도 이로써 의미가 없어졌다.

## B-2 익절 상한 — 해제해도 이득 없음

```
구성                건/h   엣지      t      TP체결률  승률   보유m
TP4.3/SL8.0 [현행]  5.9   -0.0358  -1.16   68.5%   68.5%   12
TP6 /SL8.0          4.4   -0.0509  -1.24   62.1%   62.1%   18
TP8 /SL8.0          3.5   -0.0610  -1.15   55.7%   55.7%   24
TP10/SL8.0          2.8   -0.0090  -0.14   53.9%   53.9%   24
TP무제한/SL8.0      1.9   +0.0165  +0.17   47.0%   47.0%   30
```

손익비가 커지는 만큼 승률이 정확히 떨어져 **상쇄된다.** 10개 구성 전부 같은 띠.
그리고 상한을 풀면 보유가 12→30분으로 늘어 슬롯 회전이 죽고 **거래수가 1/3**이 된다
(원칙 1 정면 위반). SL 6.0 은 거의 모든 TP 대역에서 SL 8.0 보다 나쁘다.

**-> TP 4.3 / SL 8.0 유지. 이 축에서 더 튜닝하지 말 것.**

---

# 3. 결정타 — 드리프트를 빼면 신호의 정보량이 0이다

청산규칙·슬롯·수수료를 다 걷어내고 방향성만 쟀다(표본 23만 신호).
원시값만 보면 라이브 조합이 15분 +0.0666%(t=+14.1)로 훌륭해 보인다.
**하지만 LONG +0.35 / SHORT -0.30 의 완벽한 대칭은 엣지가 아니라 상승 드리프트의 서명이다.**
그리고 라이브 필터를 통과한 신호의 **85%가 LONG** 이다.

같은 시각 84심볼 횡단면 평균을 차감하면:

```
필터                          15분              1시간
CM 신호 전체                 +0.0106 (t+4.0)   +0.0175
CM + 4h EMA200 정합          +0.0108 (t+2.8)   +0.0138
CM + HTF + flip<=5 [라이브]  +0.0053 (t+1.2)   -0.0022
CM + HTF역행 [차단 중]       +0.0104 (t+2.9)   +0.0213
```

1. **라이브 조합이 가장 나쁘다.** 0과 구분 불가. 3분 지평에서는 음수.
2. **4h EMA200 필터가 알파를 하나도 안 더한다** — 정합 +0.0108 vs 전체 +0.0106.
   08-27에 "순익 부호를 가르는 유일한 요소"로 채택한 근거(3일 실측)는
   **엣지가 아니라 롱 베타를 측정한 것**일 가능성이 높다. 차단 중인 역행 그룹이
   1시간 지평에서 오히려 낫다.
3. **전환필터가 값을 파괴한다** — 무필터 +0.0106 → flip<=5 +0.0053 → flip<=2 +0.0042.

**규모 판정: 잔여 엣지 +0.011~0.014%, 왕복 비용 0.11%(수수료 0.07 + 슬리피지 0.04).
필요한 것의 1/8~1/10 이다.** 청산을 어떻게 만져도 못 메운다.

**주의**: `--cm-htf-filter` 를 끄는 판단은 "베타를 먹을 것인가"의 문제이지 엣지 문제가 아니다.
상승장에서 실제로 돈을 벌어주고 하락장에서 정확히 그만큼 잃는다.
표본 9.4일이 전부 상승장 한 국면이라, **다른 국면에서 재측정 전에는 끄지 말 것.**

---

# 4. 다시 재지 말 것 (청산 축은 닫혔다)

되돌림 청산은 08-27에 **네 번** 기각됐다(HANDOFF_2026-08-27.md:473).
오늘 시간 청산으로 **다섯 번째**를 확인했고 같은 결론이다:

```
60분 만료: 손절 2건(-3.79) 막고 익절 8건(+13.73) 죽임
30분 만료: 손절 -28.47 막고 익절 +33.43 죽임
```

**실패 양식이 동일하다.** 승자와 패자가 초반에 구분되지 않으므로
(보유 중앙값 익절 5.8분 / 손절 5.2분), 진행 중 포지션에 손대는 규칙은
반드시 승자를 더 많이 자른다.

원칙 2 보강(`AMBIGUOUS_QUICK_PROFIT_EXIT`)이 CLAUDE.md에 명문화돼 있는데 실측과 충돌한다.
**원칙이 틀린 게 아니라 적용 대상이 사라진 것**으로 본다 — 전환필터가 "애매한 거래"를
진입 단계에서 이미 걸러내므로, 남은 모집단에서 되돌림이 잡는 건 손절 갈 거래가 아니라
아직 갈 여지가 있는 익절 갈 거래다.

**함정**: 손절건에만 되돌림을 적용해 재계산하면 "+105 개선"이라는 그럴듯한 숫자가 나온다.
그 계산은 되돌림이 잘라먹었을 **익절건을 빼지 않은 것**이다. 낙관 상한이 아니라
방향이 틀린 값이다. 실측은 SL -9%p 줄 때 TP -31%p 로 반토막이다. **이 실수를 반복하지 말 것.**

---

# 5. 구현 감사 — CM_Ultimate_MA_MTF_V2 원본 대조

## 5-1. [수정 완료] alt-resolution 경로의 lookahead

[scripts/scalp_bot_e3.py](scripts/scalp_bot_e3.py) `cm_ultimate_ma_mtf_v2()`.

종전에는 상위봉의 **open_time** 으로 backward 병합했다:

```python
aligned = _pd.merge_asof(df[["open_time"]], source_df[...].assign(out1=avg),
                         on="open_time", direction="backward")
```

open_time=T 인 상위봉의 out1 은 **T+span 에 확정되는 종가** 기반 값인데,
이 병합은 그 값을 `[T, T+span)` 구간의 차트봉에 붙인다.
**백테스트에서는 lookahead 이고 라이브에서는 리페인트다**(봉 안에서 판정이 뒤집힌다).

TradingView 의 `security(..., lookahead_off)` 는 상위봉이 마감된 뒤에만 값을 준다.
**수정: 상위봉 시각을 span 만큼 밀어 '마감 시각' 으로 병합한다.**
span 은 `source_df["open_time"].diff().median()` 으로 추론하고, 못 재면 `None`(판정 불가)을 낸다.

- **라이브 영향 0**: 이 경로는 `--cm-use-alt-resolution` 을 켤 때만 탄다.
  현재 라이브는 꺼져 있다. 수정 전/후 40심볼 대조에서
  `out1/out2/ma_up/ma_down/cr_*/crossed/close` 및 `flip_age` **불일치 0건**.
- 테스트 4개: `..._uses_only_closed_htf_bars` (기존 테스트를 정정 — 종전 이름은
  `..._aligns_latest_htf_value` 였고 **버그 동작을 고정하고 있었다**),
  `..._has_no_lookahead` (미마감 상위봉 종가 누출 감지),
  `..._accepts_epoch_ms_open_time`, `..._needs_two_htf_bars`.
- 전체 스위트는 변경 전후 동일하게 1 failure + 68 errors 다(전부 선재하는 실패, 이 변경과 무관).

### 원칙 판정 (CLAUDE.md 요구 형식)

- **원칙 0 — 지킴.** TradingView `security(..., lookahead_off)` 의 원본 의미로 되돌린 것이다.
- **원칙 1 — 무관.** 이 경로가 실행되지 않으므로 거래수 변화 0.
- **원칙 2 — 무관.** 라이브 40심볼 대조 불일치 0건. 진입/청산 판단이 한 건도 안 바뀐다.
- **원칙 2 보강 — 무관.** 청산 규칙을 건드리지 않았다.

**단, alt-resolution 을 켜는 순간 원칙 1 판정이 바뀐다**: 상위봉이 마감돼야 값이 나오므로
기동 직후와 상위봉 경계 직후에 신호가 잠깐 비고, 상위봉이 2개 미만이면 판정 불가(None)다.
켤 때는 원칙 1 영향을 따로 재야 한다.

### 이 수정이 한 번 회귀를 냈다 (기록용)

첫 구현은 `_span` 을 그대로 더했다. `open_time` 이 **int64(ms)** 면 `median()` 이 float 라
컬럼이 float64 로 승격되고, `merge_asof` 가 `incompatible merge keys` 로 **죽는다.**
수정 전에는 산술이 없어 int64 도 통하던 경로라 **새 수정이 만든 회귀**였다.
`is_integer_dtype` 이면 `round().astype(원래 dtype)` 으로 되돌려 고쳤고,
datetime64 / int64 ms / float64 ms 세 dtype 모두 같은 값(101.5)을 내는 것을 확인했다.

**교훈**: 병합 키에 산술을 넣으면 dtype 이 조용히 승격된다. 라이브(datetime64)만 보고
넘어갔으면 외부 하네스에서만 터졌을 것이다.

## 5-2. [미수정 · 문서 불일치] 원칙 0 의 "15분 HullMA20" 이 실제로는 3분봉이다

`--cm-use-alt-resolution` 이 꺼져 있어 `use_current_resolution=True` →
`source_df = df` = 신호봉 = `--signal-tf-min 3` = **3분봉**.
HullMA20 이 3분봉에서 60분 룩백, 15분봉에서 300분 룩백 — **완전히 다른 신호**다.

`htf_uptrend()` 독스트링(scalp_bot_e3.py:1119)이 이미 이 사실을 알고 있었지만,
그때 고친 건 4시간 EMA200 쪽뿐이고 15분 HullMA20 쪽은 남았다.

**고쳐도 나아지지 않는다** ([scripts/cm_resolution_audit.py](scripts/cm_resolution_audit.py)):

```
HullMA20 계산 시간대       n        15분 중립          60분 중립
3분봉 [현행]            81,641   +0.0142 (t+3.3)   +0.0299 (t+4.0)
15분봉 [원칙 0]         16,238   +0.0068 (t+0.6)   +0.0316 (t+1.7)
```

**문서가 코드를 잘못 기술한 것**이지 코드를 문서에 맞추면 좋아지는 게 아니다.
-> **CLAUDE.md 원칙 0 을 "3분봉 HullMA20 + 4시간 EMA200"으로 정정해야 한다(미처리).**

## 5-3. [미수정 · 함정] alt-resolution 을 켜면 15분이 아니라 **일봉**이 된다

```python
p.add_argument("--cm-res-custom", type=str, default="D")   # D = 일봉
```

5-2를 "고치려고" `--cm-use-alt-resolution` 만 켜면 HullMA20 이 **일봉**으로 계산된다.
15분을 원하면 **`--cm-res-custom 15` 를 반드시 함께 줘야 한다.**

## 5-4. [미수정 · 의도적] 리샘플 중복 — 신호가 항상 한 봉(3분) 낡다

`.env:53 INTERVAL=3m` 이라 WS 워커는 **3분봉 스트림**을 열고([bot/ws_worker.py:211](bot/ws_worker.py#L211)),
`get_klines` 가 3분봉을 돌려준다. 그런데 `signal_bars()` 가 거기에 `resample_bars(df, 3)` 을 **또** 건다.

`resample_bars` 는 마지막 묶음이 `minutes` 개를 못 채우면 미완성으로 보고 버리는데,
이미 3분봉이면 각 묶음 크기가 1이라 `1 < 3` — **항상 최근 마감봉을 버린다.**

```
입력 3분봉 10개 -> 결과 9개.  입력 마지막 00:27 -> 출력 마지막 00:24
```

`klines_limit_for_tf()` 독스트링이 "**1분봉이** 몇 개 필요한가"인 걸 보면
원래 1분봉 입력 전제로 짠 코드인데 `INTERVAL` 이 3m 으로 바뀌며 전제가 깨졌다.

**이걸 잡아야 할 `--max-signal-age` 가 봉 나이를 재지 않는다** — `time.time() % 60`,
즉 "매 분의 앞 10초"라는 벽시계 창이다(scalp_bot_e3.py:1916 주석). 이름과 동작이 달라
이 지연이 오래 안 보였다.

**고치지 않았다. 고치면 오히려 미세하게 나빠지기 때문이다**
([scripts/cm_stale_bar_audit.py](scripts/cm_stale_bar_audit.py)):

```
                        15분 중립         30분 중립        60분 중립
정상(신호봉 다음 봉)   +0.0142 (t+3.3)  +0.0294         +0.0299
현행(한 봉 더 늦게)    +0.0179 (t+4.2)  +0.0336         +0.0335
```

한 봉 지연이 HullMA 노이즈 필터처럼 작동했다. **우연이다.**
고치려면 원칙 2에 따라 별도 검증이 필요하다.

## 5-5. [미수정 · 영향 미미] 원본과의 잔여 불일치

- `ma_up = out1 >= prev` / `ma_down = out1 < prev` 로 **상보적**. CM 원본은
  `out > out[smoothe]` / `out < out[smoothe]` 라 동률이면 둘 다 false(직전 색 유지).
- `_series_sma` / `_series_wma` 워밍업이 **부분창 평균**. Pine 은 `na`.
- `_series_ema` 가 첫 값으로 시딩. Pine 은 `sma` 시드.
- `_series_tilson_t3_second` 가 first 버전과 **완전히 동일한 코드**다.
  독스트링은 원본의 `sgd(gd(...))` 특이형을 보존한다고 주장하지만 실제로는 안 한다.
  `atype=8` 일 때만 문제이고 현재 `atype=4` 라 미사용.
- `htf_uptrend()` 가 **형성 중인 4시간봉**을 포함해 EMA200 을 계산한다. lookahead 는
  아니지만 4시간 안에서 판정이 뒤집힐 수 있고, 하네스(마감봉 사용)와 어긋나는 지점이다.
- **파리티 테스트가 없었다.** 기존 CM 테스트는 `assertIn("out2", out)` 같은 형태 검사뿐이라
  5-1~5-4 가 전부 통과했다.

---

# 6. 남은 것 / 다음 사람에게

## 커밋 — 아직 안 했다. 반드시 커밋할 것

08-27 하네스가 **커밋 누락으로 통째로 유실**돼 오늘 처음부터 다시 만들었다.
같은 일을 반복하지 않으려면 아래를 커밋해야 한다(사용자 지시 없이 진행하지 않았다).

```
수정   scripts/scalp_bot_e3.py        5-1 lookahead 수정 (+dtype 회귀 수정)
수정   tests/test_scalp_bot_e3.py     CM 테스트 정정 1 + 신규 3
수정   archive/handoffs/README.md     핸드오프 체인에 08-28 연결
신규   HANDOFF_2026-08-28.md          이 문서
신규   scripts/cm_fetch_cache.py      공개 klines 수집기
신규   scripts/cm_backtest.py         CM 하네스 (유실 재발 방지 — 이번엔 반드시 커밋)
신규   scripts/cm_signal_edge.py      드리프트 중립 엣지 측정
신규   scripts/cm_resolution_audit.py 3분봉 vs 15분봉 대조 (5-2 근거)
신규   scripts/cm_stale_bar_audit.py  한 봉 지연 비용 측정 (5-4 근거)
```

`scratch_cm_klines.json`(24MB)은 커밋하지 말 것 — 재생성 가능한 캐시다.

## 재시작은 공짜가 아니다 — 실측 -5.08 USDT (06:00, 사용자 지시로 수행)

**`--adopt-max-stop-roe 5` 때문에 ROE -5% ~ -8% 구간의 보유 포지션은
재시작하는 순간 확정 손실이 된다.** 신규 진입은 `--stop-fixed-roe 8.0` 으로 버티는데
**채택 포지션만 손절이 -5% 로 조여지기** 때문이다.

06:00 재시작 실측(보유 4건 중 2건이 그 구간에 있었다):

```
06:00:21  FILUSDT  SL 등록실패(-2021 Order would immediately trigger)
06:00:36  재시작 복구 요약: 채택 4건 / SL성공 2건 / SL실패 2건
06:00:45  FILUSDT  LONG  ROE-5.50%  순 -2.6401  (보유 209분)
06:00:50  龙虾USDT LONG  ROE-5.18%  순 -2.4399  (보유  27분)
                                    합계 -5.08 USDT
```

**시장이 움직여서가 아니다.** 재시작을 안 했으면 두 건은 -8% 손절을 달고 살아 있었다.
`-2021` 은 "새 손절가가 이미 현재가를 지났다"는 뜻이고, 봇은 재등록 대신 즉시 시장가로 턴다.

**-> 재시작 전에 반드시 보유 포지션의 현재 ROE 를 확인할 것.**
-5% 아래 포지션이 있으면 그 건들은 재시작 비용으로 확정된다. 급하지 않으면
그 포지션들이 정리된 뒤에 재시작하는 것이 싸다.

## 재시작 절차 (06:00 에 실제로 쓴 것)

1. **전체 테스트를 HEAD 와 비교**한다. 절대 수치가 아니라 **차이**를 본다
   (이 저장소는 선재 실패가 많다: HEAD 1008개 중 8 failures + 70 errors).
2. 보유 포지션 / 원장 건수 / 로그 줄수를 스냅샷한다.
3. **PowerShell 로** 죽인다. Git Bash 의 `ps`/`kill` 은 python.exe 를 못 죽이는데
   `grep -c` 가 0 을 반환해 "죽었다"고 오판한다(08-21 실사고: 구버전 4 + 신버전 2 동시 실주문).
4. 죽일 대상은 **4개**다: e3 부모/자식 + 그 ws_worker 부모/자식.
   e3 는 같은 명령줄로 부모-자식 쌍이 뜨고(정상), 자식이 ws_worker 쌍을 낳는다.
   그림자(`--instance-tag`)는 건드리지 않는다.
5. `Stop-Process -Force` 후 **개수를 반드시 재확인**한다.
6. `.\start_e3.ps1` 로 띄운다(인자가 한 곳에만 있다).
7. 채택 로그와 `WS 준비 완료 10/10` 을 확인한다.

**자동 재기동 감시기는 없다.** 테스트 중 `supervisor` 프로세스가 잠깐 보이는데
`tests/test_supervisor_parent_guard.py` 가 띄우는 일시적 프로세스이고 곧 종료된다.
`run_forever.py` 는 e3 를 관리하지 않는다. 워치독 자기재기동
(scalp_bot_e3.py:1546)은 **메인 루프 행(hang) 감지 시에만** 동작하므로
강제종료에는 되살아나지 않는다.

## 현재 가동 상태 (06:00 재시작 이후)

```
PID 13804  e3-라이브 (부모 6820)   기동 08-28 06:00   <- 5-1 수정 포함
PID 3620/3880  ws_worker            기동 08-28 06:00
PID 21760/2324 그림자(shadow)       기동 08-27 20:00   <- 재시작 안 함, 구버전 코드
인자는 start_e3.ps1 그대로 (08-27 20:00 과 동일)
```

**5-1 수정은 매매 동작을 바꾸지 않는다**(alt-res 경로 미실행, 40심볼 대조 불일치 0건).
즉 이번 재시작은 코드 반영이 목적이었을 뿐 동작 변화는 없다 —
**그런데도 위의 -5.08 비용이 들었다.** 다음에 같은 판단을 할 때 이 점을 볼 것.

그림자는 구버전 코드로 계속 돌고 있지만 같은 이유로 동작이 동일하다.
어차피 2장 결론대로 **A/B 자체가 조기 종료 대상**이다.

## 미처리

- **CLAUDE.md 원칙 0 정정** (5-2). 지금 문서는 실행되지 않는 전략을 기술하고 있고,
  그걸 근거로 판단하면 계속 틀린다. 사용자 승인 필요.
- **e3/e4 A/B 조기 종료** (2장). 결론이 안 나는 A/B 다.
- `scratch_cm_klines.json` (24MB) — 세션 캐시. `archive/scratch_scripts/` 로 옮기거나 삭제.

## 다음 단계를 잡는다면

파라미터 축은 닫혔다(22개 구성 전부 같은 띠). 남은 방향은 **방향 신호 자체의 교체**다.
CLAUDE.md 원칙 0에 기록된 메인 봇 뼈대(볼밴+EMA+RSI 방향일치, 15분 +0.446%)가
이번 CM 측정치의 40배인데, **그 수치도 드리프트 중립화를 안 거쳤으므로 재측정 전에는
믿으면 안 된다.** `cm_signal_edge.py` 의 `measure_n` 을 그대로 재사용할 수 있다.

## 하네스를 인용할 때 반드시 함께 볼 한계

- 표본 9.4일(08-19경~08-28)이고 **전부 상승장 한 국면**이다. 드리프트 중립화로
  국면 의존은 걷어냈지만 "국면이 바뀌면 다른 결론인가"는 이 데이터로 답할 수 없다.
- **거래빈도가 라이브의 63%** 다(5.9 vs 9.3건/h). 상대비교로만 쓸 것.
- **한 봉 안에서 손절선·익절선 동시 접촉의 순서를 알 수 없다.** 기본은 손절 우선(비관),
  낙관 가정이면 +0.022%p 좋아진다. 이 폭이 구성 간 격차보다 크다 —
  결론("전부 0")은 안 흔들리지만 **"어느 게 1등"은 의미가 없다.**
- 호가 깊이/실제 슬리피지, `UNDERFILL_CLEANUP`(라이브 11%), 재시작 채택 포지션,
  사이징 배율이 모델에 없다. 슬리피지 왕복 0.04% 는 **가정이지 실측이 아니다.**

## 8. 관찰 실험 시작 및 원복 계획 (2026-08-28 10:15 KST)

사용자 승인에 따라 라이브 설정과 포지션은 변경하지 않고, 현재 설정을 기준선으로
30~50건의 실거래 관찰을 시작한다. 목표는 자산 회복을 낙관적으로 가정하는 것이 아니라
`CM TP 유효/무효`, 평균 승리/손실, 손절률, 부분체결, 롱/숏 및 심볼별 결과를 분리해
회복 정체의 원인을 확인하는 것이다.

### 관찰 기준선

- 3분봉 CM 방향 + 4시간 EMA200 필터
- `--cm-atype 4`, `--cm-flip-max-bars 5`
- v2 지정가 진입, 1슬롯 1회 진입
- `--cm-tp-max-roe 4.3`, `--stop-fixed-roe 8.0`
- 조기청산 OFF: `--early-adverse-sec 0`, `--mae-cut-roe 0`, `--giveback-arm-roe 0`
- 거래량 필터, `--max-concurrency 10`, `--max-same-side 3` 유지
- 라이브 재시작·필터 완화·손절 확대는 관찰 중 시행하지 않는다.

### 판정 지표

30~50건 누적 후 승률 단독이 아니라 수수료 차감 순손익, 평균 승리/평균 손실,
손익분기 승률, CM TP 유효/무효별 손절률과 건당 손익을 판정한다. 원장과 거래소
실현손익이 불일치하는 구간은 별도 표시하고 확정 성과로 사용하지 않는다.

### 개선 및 원복 규칙

개선안은 한 번에 하나만 shadow에서 비교하고, 라이브 반영 전 사용자 승인을 받는다.
후보는 우선 `CM TP 무효/폴백` 원인 분리이며, 거래량 필터 완화·손절 확대·조기청산 재활성화는
기본 개선안으로 채택하지 않는다.

- 개선안 배포 후 순손익, 평균손실, 손절률 또는 거래소-원장 정합성이 기준선보다 악화되면 즉시 원복한다.
- 원복은 `start_e3.ps1`의 직전 기준값으로 되돌리고, 새 설정으로 임의 재시작하지 않는다.
- 현재 기준 원복값은 `signal-tf-min=3`, `cm-atype=4`, `cm-flip-max-bars=5`,
  `cm-tp-max-roe=4.3`, `stop-fixed-roe=8.0`, 조기청산 관련 세 값 `0`이다.
- 재시작이 필요한 변경은 보유 포지션 ROE와 거래소 주문·상태 파일을 먼저 확인하고,
  재시작 비용과 채택 손절 상한(`adopt-max-stop-roe=5`)을 함께 기록한다.

### 현재 작업 판정

이번 단계는 **기준선 관찰 단계**다. 아직 라이브 설정 변경이나 재시작은 하지 않았다.
관찰 표본이 충분해질 때까지 “회복 중” 또는 “전략 개선”으로 확정하지 않는다.

## 9. HTF 0.3 shadow 비교군 시작 (2026-08-28 11:45 KST)

슬롯 부족 원인을 라이브에서 직접 완화하지 않고, 4시간 EMA200 역행 신호만 분리해
dry-run 비교군을 시작한다. 라이브 e3와 기존 e4는 변경하지 않는다.

### 비교 설계

- 기준군: 라이브 e3 설정 (`cm-htf-counter-mult=0`, `cm-flip-max-bars=5`)
- 기존 e4: 전환필터 비교군 (`cm-flip-max-bars=-1`, HTF 차단 유지)
- 신규 htf03: 전환필터는 라이브와 동일하게 `5`, HTF 역행만 `0.3` 배 증거금으로 허용
- 모든 군은 3분봉, HMA20, 1슬롯, 85심볼, 거래량 필터를 공유한다.
- 신규 파일: `start_e3_shadow_htf03.ps1`
- 신규 shadow 태그: `htf03`; 라이브 원장·상태·주문과 분리한다.

### 측정 항목

shadow에서는 순손익·승률을 라이브와 비교하지 않는다. dry-run은 거래소 TP/SL 체결을
재현하지 못하기 때문이다. 비교하는 값은 시간당 후보수, 진입 시도수, 예상 슬롯 점유,
롱/숏 비율, 스킵 사유, 신호 방향이다. 실제 손익 판정은 라이브 기준군 원장의
수수료 차감 순손익과 거래소 정합성을 기준으로 한다.

### 원복 절차

- htf03 비교군이 후보만 늘리고 품질이 악화되거나, 라이브에 영향을 주는 정황이 있으면
  해당 shadow 프로세스만 종료한다.
- 라이브 `start_e3.ps1`의 `cm-htf-counter-mult=0`은 유지한다.
- 라이브에 반영할 때는 별도 사용자 승인 후 포지션·주문·상태·WS를 점검하고 재시작한다.
- 라이브 반영 후 악화되면 즉시 기준값 `cm-htf-counter-mult=0`으로 되돌리고 재시작한다.
- 기준 원복값: `signal-tf-min=3`, `cm-atype=4`, `cm-flip-max-bars=5`,
  `cm-tp-max-roe=4.3`, `stop-fixed-roe=8.0`, 조기청산 관련 값 `0`.

이번 조치는 **shadow 비교군 시작**일 뿐이며, 라이브 매매법 변경이나 라이브 재시작은
수행하지 않았다.

## 13. CM05 라이브 적용 대기 (2026-08-28 13:xx KST)

사용자가 cm05 완화를 라이브에도 적용하도록 요청했다. `start_e3.ps1`에는
`cm-invalid-tp-size-mult=0.5`, `cm-invalid-tp-min-margin=15`를 반영했지만,
적용 시점에 거래소 보유 포지션 3건(FILUSDT, AKEUSDT, PROMUSDT)과 각 TP/SL 주문이
존재해 라이브 재시작은 보류했다.

재시작 시 기존 포지션 채택 과정에서 손절 재등록 실패 또는 현재가를 지난 손절선의
시장가 정리가 발생할 수 있으므로, 포지션과 보호 주문이 정리된 뒤에만 재시작한다.
재시작 전 거래소 포지션·미체결 주문·알고리즘 주문이 모두 0인지 다시 확인한다.

재시작 전까지 현재 실행 중인 라이브 프로세스에는 새 옵션이 적용되지 않는다.
따라서 이 상태는 **런처 설정 반영 완료, 라이브 프로세스 적용 대기**다.

### 사용자 승인 후 실험 개시 확인 (2026-08-28 13:03 KST)

1차 완화 실험을 실제로 개시했다. `htf03`는 현재 `--dry-run --attach-ws`로 실행 중이며,
라이브 e3의 `cm-htf-counter-mult=0`과 라이브 포지션·주문·원장에는 영향을 주지 않는다.
현재 htf03은 진입 7건을 기록했고 스킵 누적은 2,999건이다. htf03의 dry-run 청산 손익은
실제 거래소 TP/SL 체결을 재현하지 않으므로 품질 판정에 사용하지 않는다.

실험 종료 또는 라이브 반영 전까지는 후보수·진입수·슬롯·롱/숏 비율·스킵 사유만 비교한다.
htf03 결과가 나쁘면 htf03 프로세스만 종료하고 라이브 기준값을 그대로 유지한다.

## 12. CM TP 무효 거래 크기축소 shadow 시작 (2026-08-28 13:10 KST)

CM 방향·3분봉·거래량·4시간 필터·전환필터는 라이브와 동일하게 두고, CM TP가 무효여서
RR 폴백으로 가는 거래만 증거금 `0.5배`로 줄이는 `cm05` dry-run shadow를 시작한다.
일반 최소 증거금이 30 USDT라 단순 배율만 적용하면 다시 30으로 복원되므로, cm05에만
전용 하한 `15 USDT`를 적용한다. 새 옵션의 기본값은 `1.0/0`이라 라이브 동작은 변하지 않는다.

- 런처: `start_e3_shadow_cm05.ps1`
- 태그 및 분리 파일: `cm05`
- dry-run 손익은 라이브 TP/SL 체결과 달라 손익 판정에 사용하지 않는다.
- 비교 항목: 시간당 후보·진입수, 슬롯 점유, 롱/숏 비율, CM TP 무효 비중, 스킵 사유.

### cm05 원복

cm05가 후보만 늘리고 품질이 악화되거나 관측을 오염시키면 cm05 프로세스만 종료한다.
라이브는 계속 `cm-invalid-tp-size-mult=1.0`, `cm-invalid-tp-min-margin=0` 상태를 유지한다.
라이브 반영은 별도 승인 후 재시작하며, 악화 시 위 두 값을 기준값으로 되돌리고 재시작한다.

## 14. CM05 라이브 반영 및 재시작 결과 (2026-08-28 13:17 KST)

사용자 승인으로 `start_e3.ps1`의 CM TP 무효 거래 크기축소 옵션을 라이브에 반영했다.
새 라이브 프로세스 인자에서 `cm-invalid-tp-size-mult=0.5`,
`cm-invalid-tp-min-margin=15`를 확인했다.

재시작 전 거래소에는 FILUSDT, AKEUSDT, PROMUSDT 3개 포지션과 보호 주문이 있었다.
재시작 중 FILUSDT는 재채택 및 SL 등록에 성공했지만, AKEUSDT는 손절선이 현재가를
이미 지나 API `-2021`이 발생했고 즉시 시장가 정리됐다.

- 재시작 시 실현된 AKEUSDT 순손실: `-2.7540 USDT`
- 재시작 복구 요약: `채택 2건 / SL성공 1건 / SL실패 1건`
- 재시작 후 남은 라이브 상태: `FILUSDT` 1건
- 재시작 후 `WS 준비 완료 10/10` 확인
- e4, htf03, cm05 shadow는 유지

이번 사건은 원복 가능성 기록이 아니라 **재시작 자체가 보유 포지션에 비용을 만들 수
있다는 실측 사례**다. 다음 원복 또는 재배포 전에도 포지션 ROE와 손절선 통과 여부를
먼저 확인한다. CM05 라이브 반영 후 성과 악화 시 `cm-invalid-tp-size-mult=1.0`,
`cm-invalid-tp-min-margin=0`으로 되돌리고, 포지션·주문 점검 후 재시작한다.

## 10. 라이브 무대기주문 재시작 (2026-08-28 11:54 KST)

사용자 요청으로 재시작 전 거래소 상태를 확인했다. 라이브 e3 기준 보유 포지션 0건,
미체결 주문 0건, 알고리즘 주문 0건, 상태 파일의 심볼 0건이었다. 따라서 기존 포지션
채택이나 재시작 손실 없이 라이브 본체와 전용 WS만 재기동했다.

- 새 라이브 e3 PID: `22452`(부모 `23768`)
- 새 라이브 WS PID: `24424`(자식 `14804`)
- 기동 인자: `start_e3.ps1` 기준선 그대로
- 복구 요약: `채택 0건 / SL성공 0건 / SL실패 0건`
- `WS 준비 완료 10/10` 확인 후 실시간 신규 진입 재개
- e4 및 htf03 shadow는 종료하지 않고 유지
- 재시작 후 라이브 상태 파일: `symbols=[]`, `entry_order_symbols=[]`

이번 재시작은 매매법 변경 적용이 아니라 빈 상태에서의 운영 재기동이다. 이후 개선안을
라이브에 적용할 때도 같은 사전 점검과 원복 기록 절차를 따른다.

### 관찰 시작 실측 (2026-08-28 10:xx KST)

`logs/scalp_bot_e3_cm_ledger.jsonl`에서 `dry_run=false`이고 청산시각이 있는 행만
읽어 계산했다. 원장과 거래소 손익 불일치 경고가 있었으므로 자산 잔고 자체의 원인 분석이
아니라 e3 거래 흐름의 기준선으로만 사용한다.

```
구간        청산수  승률   순손익       건당      평균승리    평균손실
전체          875   55.3%  -149.4285    -0.1708    +0.8979     -1.4936
최근 50        50   66.0%    +2.5613    +0.0512    +1.5530     -2.8641
최근 30        30   66.7%    +3.9795    +0.1326    +1.4856     -2.5733
재시작 이후     7   71.4%    -1.3184    -0.1883    +1.2698     -3.8336
```

최근 30~50건은 플러스로 회복했지만 재시작 이후 표본은 7건뿐이고 순손익은 음수다.
따라서 현재 결론은 **개선 확정이 아니라 관찰 지속**이다. 승률이 70% 안팎이어도 평균
손실이 평균 승리보다 약 2~3배 커지는 구조가 반복되면 자산이 480~490 구간에서 정체할
수 있다. 다음 관찰에서는 이 비대칭이 CM TP 유효/무효 및 청산사유별로 어디에서 생기는지
분리한다.

### cm05 원복 및 라이브 재시작 (2026-08-28 13:56 KST)

자산 하락 확인 후 `--cm-invalid-tp-size-mult 0.5` 및
`--cm-invalid-tp-min-margin 15`를 라이브 런처에서 제거하여 기본값(`1.0`, `0.0`)으로
원복했다. 라이브 e3 본체와 전용 WS만 종료하고 e4/htf03/cm05 shadow는 유지했다.

- 새 라이브 e3 PID: `17096`(자식 `24272`)
- 새 라이브 WS PID: `9652`
- 기동 잔고: `458.5821 USDT`
- `WS 준비 완료 10/10` 확인
- FILUSDT 기존 포지션 채택 및 TP/SL 관리 재개 확인
- 새 명령행에서 cm05 축소 인자 미포함 확인

관찰된 하락 원인 가설은 cm05 단일 원인으로 확정하지 않는다. 재시작 직후 AKE 기존
포지션의 SL 즉시발동 실패에 따른 강제청산 손실, 이후 3분봉에서 발생한 龙虾/TUT 등
연속 손실, 그리고 평균손실이 평균승리보다 큰 비대칭이 함께 작용했을 가능성이 높다.
원복 후 동일 기준으로 손익과 청산사유를 다시 비교하며, 추가 변경은 별도 승인 없이는
하지 않는다.
### C안 구현: 체결 직전 CM 재확인 (2026-08-28)

A/B/C 비교 결과 CM 방향 자체에는 양의 선행 엣지가 있으나, 현재 B안은 CM 신호 후
EMA 눌림을 기다리는 동안 조건이 바뀐 뒤에도 지정가를 발주할 수 있다. 이를 줄이기 위해
C안을 구현했다. 신규 지정가 발주 직전에 최신 3분봉 CM 신호, 4시간 EMA200 정합,
`cm-flip-max-bars` 유효성을 다시 확인한다.

- 신규 옵션: `--cm-recheck-on-entry`
- 기존 포지션 보호·손절·TP·거래량·슬롯 제한은 변경하지 않음
- 재확인 실패 시 주문을 발주하지 않고 pending만 취소함
- 이미 체결된 포지션을 시장가로 되돌리는 방식은 사용하지 않음
- 원복: `start_e3.ps1`에서 `--cm-recheck-on-entry` 한 줄을 제거 후 재시작
- 판정: 시간당 진입수, 재확인 실패수, 롱/숏별 순손익, 평균손실, 슬롯 점유시간을
  원복 기준과 50~100건 비교

이 변경은 CM 방향을 완화하는 것이 아니라 체결 시점에도 CM 원칙을 유지하는 보강이다.

### C안 보류 및 주문 지연 계측 우선 (2026-08-28)

C안은 라이브 런처에서 보류한다. 지정가 발주 직전 재확인만으로는 발주 후 장시간
대기하다가 CM 조건이 변한 뒤 체결되는 문제를 막지 못하므로, C안의 우위는 아직
검증·확정하지 않는다.

우선 주문 TTL과 시각 계측을 보강했다.

- 라이브 런처에 `--entry-order-ttl-sec 45`를 명시
- pending의 CM 신호 시각을 `signal_at`으로 진입주문 상태에 저장
- 주문 발주 시 신호 후 경과시간과 TTL을 로그에 기록
- 체결 시 신호->발주, 발주->체결, 전체 지연시간을 로그에 기록
- C안 옵션 `--cm-recheck-on-entry`는 런처에서 제거하여 라이브 미적용 상태 유지

기존 로그에서 296~297초 미체결 대기가 관측됐으므로, 다음 확인에서 실제 주문 상태와
TTL 초과 취소 여부를 반드시 대조한다. 지연시간별 순손익을 충분히 모은 뒤 C안 재개 또는
원복을 별도 승인한다.

### 동일방향 연속손절 차단 배포 (2026-08-28)

12시 이후 STOP_EXCHANGE 9건 중 정상 진입 롱 손절이 8건이며, 12:33~13:48 롱 손절이
집중됐다. 같은 방향 STOP_EXCHANGE 2건/60분 가상 적용 시 이후 기존 손실 거래 10건
(`-23.66 USDT`)이 차단 대상이었고, 단순 제거 기준 순손익은 `-12.22 -> +11.44 USDT`로
개선되는 후보였다. 대체 거래가 들어오는 효과는 측정하지 못했으므로 확정 수익으로 보지 않는다.

- 라이브 옵션: `--same-side-stop-cooldown-sec 3600`

## 2026-08-28 3배 레버리지 통일

- e3 런처에 `--leverage 3`을 명시해 신규 포지션 레버리지를 3배로 고정했다.
- 3배는 수수료율 자체를 낮추지 않는다. 동일 증거금이면 명목 포지션과 거래당 수수료 절대액이 기존 5배 대비 약 40% 감소한다.
- 반대로 거래당 절대 수익·손실도 감소하므로 순익 개선은 보장되지 않는다. 손실 변동성과 수수료 부담을 줄이는 방어 조정으로 평가한다.
- 원복은 `start_e3.ps1`에서 `--leverage 3`을 제거하고 기본값 또는 원하는 배율을 명시한 뒤 재시작한다.

## 2026-08-29 잔액 부족 임시 운용

- 실측 USDT 잔액 `12.8871`, 포지션·미체결 주문 `0건` 상태에서 신규 진입이 막힌 원인은 기존 `--min-leg-margin 30`이 잔액보다 큰 것이었다.
- 거래 재개를 위해 e3 런처를 임시로 `--leverage 10`, `--min-leg-margin 1.4`, `--max-leg-margin 12`로 변경한다.
- 10배는 수수료율을 낮추지 않으며, 동일 증거금 기준 명목가와 거래당 수익·손실·수수료 절대액을 키운다. 저잔액에서 손실 변동성과 청산 위험이 커진다.
- 원복 시 `--leverage 3`, `--min-leg-margin 30`, `--max-leg-margin 50`으로 되돌리고 재시작한다.
- 후속 조정: 최소 증거금을 `5 USDT`로 상향하고 최대 증거금 `12 USDT`는 유지했다. 잔액 12.8871 USDT 기준 최대 2개 안팎의 슬롯만 가능하며, 잔액 변동·거래소 최소명목·보호주문 여유에 따라 더 적을 수 있다.

## 2026-08-29 초단타 임시 모드

- 이전 거래와 분리된 일시 운용으로 e3 신규 진입 레버리지를 `10배`로 변경한다. 앞서 적용했던 20배는 폐기했다.
- 원칙 0·1·2, CM 방향·눌림 실행, 3분봉 신호, 1회 진입, 수동 필터·손절·TP 구조는 변경하지 않는다. 초단타라는 이유로 신호 필터나 손절을 완화하지 않는다.
- `--min-leg-margin 5`, `--max-leg-margin 12`, 주문 TTL `45초`는 유지한다. 10배도 수수료율을 낮추지 않고 3배보다 명목가·수익·손실·수수료 절대액과 청산 민감도가 크다.
- 사용자 원복 요청 시 이 임시 설정을 폐기하고 이전 설정인 `--leverage 3`, `--min-leg-margin 30`, `--max-leg-margin 50`으로 되돌린 뒤 재시작한다.

## 2026-08-29 초단타 다중봉 임시 모드

- e3 런처에만 `5분 CM 방향 + 5분 방향 확인 + 1분 눌림 실행`을 적용했다. 기존 15분 방향 확인은 거래 빈도를 과도하게 줄여 제거했다.
- 4시간 추세 필터는 이 임시 모드에서 제거했지만, 방향·눌림·손절·보호주문 원칙은 유지한다.
- 유효 인자: `--signal-tf-min 5`, `--confirm-tf-min 5`, `--entry-tf-min 1`.
- 원복은 신호봉을 3분으로 되돌리고 추가 15분·1분 인자를 제거하며 `--cm-htf-filter`를 다시 추가한 뒤, 레버리지·증거금도 기존 지정값으로 복구한다.

## 2026-08-29 CM 초단타 기준 적용

### 운영 명칭 구분

- 현재 라이브: `e3 변형-초스캘프 단타` — 5분 CM 방향, 1분 실행, 1분 WS, 10배, 저잔액 임시 설정.
- 기존 전략: 일반 `e3` — 원복 대상의 기존 거래 로직. 현재 변형 모드의 성과와 혼합해 비교하지 않는다.

- 5분 CM 방향은 필수로 유지한다.
- 5분 방향 재확인용 중복 조건은 제거했다. `--confirm-tf-min`을 런처에서 제거했다.
- 5분 CM 전환 후 5봉 이내 강제 조건은 제거했다(`--cm-flip-max-bars -1`). 방향 유지 여부는 진입 강제조건이 아니라 관찰용 가점으로 취급한다.
- 1분봉 눌림 발생, 손절폭 하한, 예상 수수료를 반영한 익절 가능성은 필수로 유지한다.
- 5분 CM 반대 방향·중립 신호는 계속 차단 또는 대기한다. 4시간 필터는 적용하지 않는다.
- 원복 요청 시 `--signal-tf-min 3`, `--entry-tf-min` 제거, `--cm-flip-max-bars 5`, `--cm-htf-filter` 복구, 레버리지·증거금 `3배 / 30 / 50 USDT`로 되돌린다.
- 원복 완료: 일반 e3 런처를 `3분봉 + 4시간 EMA200 필터 + 전환 5봉 제한 + 3배 + 최소/최대 증거금 30/50 USDT`로 복구했다. 변형 전용 1분 실행·1분 WS 강제·추격진입 차단은 일반 e3 런처에서 비활성화했다.
- 2026-08-29 저잔액 재가동: 일반 e3 로직은 유지하고 임시 운용값을 `10배 / 최소 5 / 최대 12 USDT`로 설정했다. `--start-paused`는 제거해 신규 진입을 허용했다. 원복 시 `3배 / 30 / 50 USDT`로 되돌린다.
- 2026-08-29 신호 지연 버그 수정: pending CM 신호 유효시간을 기존 3600초에서 주문 TTL `45초`로 제한했다. 만료 신호는 `신호노후`로 기록·폐기되어 늦은 추격 진입으로 전환되지 않는다.
- TP 기대값 개선: 일반 e3에 `--min-net-tp-rate 0.001`(수수료 차감 후 최소 0.10%)을 적용했다. 근거 없는 소형 TP를 차단하고 STOP_EXCHANGE 계측은 유지한다. 원복 시 이 인자를 제거하거나 기존 기본값 `0.0002`로 되돌린다.
- 2026-08-29 레버리지 임시 변경: 일반 e3 신규 진입 레버리지를 `10배 -> 15배`로 변경했다. 기존 포지션은 거래소 기존 레버리지를 유지한다. 원복 시 런처의 `--leverage 10`으로 되돌리고 재시작한다.
- 초단타 실행 병목 수정: 런처 폴링을 기본 10초에서 `--poll 1`로 낮췄다. 이는 스캔 시작 간격을 줄이는 설정이지, 85심볼 순차 REST 조회를 초단위로 보장하는 것은 아니다.
- 추가 수정: e3가 WS 워커에 `INTERVAL=1m`을 명시하도록 변경했다. 1분 캐시를 기반으로 1분 실행봉과 5분 CM 봉을 공급한다. 원복 시 이 환경변수 주입을 제거하고 기존 WS 기본 `3m`으로 되돌린다.
- 최소 증거금 임시 변경: `5 -> 4 USDT`. 현재 변형-초스캘프 모드에서 저잔액 거래 재개 여지를 늘리기 위한 변경이며, 원복 시 `5 USDT`로 되돌린다.
- 원칙 3 봇 적용: 신호봉 종가 대비 진입 우위가 `0.05%` 미만이면 `추격진입차단`으로 주문을 만들지 않는다. 기존의 우위 부족 증거금 축소가 아니라 진입 차단으로 변경했다. 일반 e3 원복 시 `--min-entry-edge-pct 0`으로 되돌린다.
- 같은 방향 STOP_EXCHANGE가 최근 60분 내 2건 이상이면 해당 방향 신규 진입만 차단
- 기존 포지션 TP/SL 관리와 반대 방향 신규 진입은 유지
- CM 방향, 거래량 필터, 3분봉, 슬롯 상한, 손절폭은 변경하지 않음
- 원복: 런처에서 위 옵션을 제거하거나 `0`으로 변경 후 포지션·주문 점검 뒤 재시작
- 판정: 50~100건 동안 순손익, STOP_EXCHANGE, 롱/숏, 시간당 거래수, 슬롯 점유시간 비교
- 2026-08-29 저잔액 증거금·TP 계측 개선 승인/적용:
  - `--min-leg-margin 4`, `--max-leg-margin 8`로 신규 진입 증거금 범위를 낮춰 가용 증거금 부족과 슬롯 독점을 완화한다.
  - 한 스캔 사이클 신규 진입 발주는 `--max-new-orders-per-cycle 2`로 제한한다. 신호를 완화하는 것이 아니라, 잔여 증거금 경쟁을 줄이기 위한 발주 선별이다. 동일 심볼 중복·동일 방향 상한·물타기 금지·장기보유 관찰 원칙은 유지한다.
  - 기존 증거금 산식의 수수료·가격변동 여유분 `min_notional / leverage * 1.12`를 유지해 주문 전 버퍼를 선반영한다.
  - TP 지정가 주문의 `tp_order_placed_at` 및 청산 시점까지의 `tp_wait_sec`를 원장에 기록한다. TP 체결률은 기존 `exit_reason=TP_LIMIT`로, 대기시간은 신규 필드로 산출한다.
  - 먼 TP 차단은 기존 `--min-net-tp-rate 0.001`을 우선 기준으로 유지한다. 실측 TP 체결률·대기시간을 확보하기 전에는 TP 거리를 추가 변경하지 않는다.
  - 이 변경의 부분 원복값은 `--min-leg-margin 5`, `--max-leg-margin 12`, `--max-new-orders-per-cycle` 제거(무제한)이며, 레버리지 15배와 나머지 일반 e3 설정은 유지한다. 전체 일반 e3 원복은 별도 승인 후 수행한다.
- 2026-08-29 재기동 중 운영 버그 수정:
  - 비ASCII 거래소 심볼 표시명이 Windows `cp949` 콘솔 출력과 충돌해 e3가 종료되는 문제가 확인되었다. `say()` 출력에 ASCII escape 폴백을 추가했다. 전략/진입 조건은 변경하지 않았다.
- 2026-08-29 TP 상한 임시 조정:
  - 일반 e3 라이브의 `--cm-tp-max-roe`를 `4.3`에서 `2.3`으로 변경했다. 15배에서 약 0.153%의 가격 이동에 해당하며, 지정가 TP가 너무 먼 목표를 기다리는 시간을 줄이기 위한 단일 축 변경이다.
  - `--cm-tp-pullback-pct 0.5`, `--min-net-tp-rate 0.001`, CM 방향·눌림 진입·4시간 EMA200 필터·손절 규칙은 유지한다. 보유 포지션은 종료하지 않고 재시작 시 TP 지정가만 새 상한으로 동기화한다.
  - 원복 시 `start_e3.ps1`의 `--cm-tp-max-roe`를 `4.3`으로 되돌리고 재시작한다. 효과 판정은 TP_LIMIT 체결률, `tp_wait_sec`, 수수료 차감 순손익을 분리해 비교한다.
- 2026-08-30 수동매매 보호:
  - 상태파일에 소유권이 없는 계좌 포지션은 기본적으로 e3가 채택·TP 등록·손절 등록·청산하지 않도록 변경했다.
  - `--adopt-unowned-positions`를 명시한 경우에만 미소유 포지션 자동 채택이 허용된다. 기본 런처에는 이 옵션을 넣지 않는다.
  - e3가 상태파일로 이미 추적 중인 포지션은 계속 관리한다. 원복 시 해당 옵션의 기본 동작을 되살리는 별도 승인과 함께 수행한다.
- 2026-08-30 레버리지 재조정:
  - 라이브 e3 레버리지를 `15배`에서 `10배`로 변경하고 재시작했다. TP 상한 `ROE 2.3%`, 증거금 `4~8 USDT`, 사이클당 신규 발주 최대 2건은 유지한다.
  - 원복 시 런처의 `--leverage 10`을 `15`로 되돌리고 재시작한다.
- 2026-08-30 저잔액 주문 여유 조정:
  - 최소 증거금을 `4`에서 `3.5 USDT`로 낮추고 최대 노출률을 `0.95`에서 `0.85`로 낮췄다. 레버리지는 10배를 유지한다.
  - 목적은 잔액 전부를 주문에 사용하지 않으면서 거래소의 수수료·가격변동 여유분을 확보하는 것이다. 수익 보장이나 거래 빈도 보장은 아니다.
  - 원복 시 `--min-leg-margin 4`, `--max-exposure 0.95`로 되돌리고 재시작한다.
