# HANDOFF E6 — 볼밴 역추세 지정가 전략

작성일: 2026-09-02 KST
상태: 구현 완료 / 스모크 테스트 통과 / 실거래 기동 중

## 1. 전환 배경

기존 e5는 CM 방향 신호에 F1 밴드폭 선별을 결합했다. 그러나 CM 방향 자체의
수수료 전 기대값이 음수였고, e5 원장도 방향·체결·원장 분리 문제 때문에
수익화 증거가 되지 못했다. e5는 보존하고, 검증된 별도 가설인 볼밴 역추세를
e6로 분리한다.

핵심 가설은 1분봉 볼밴 하단 접촉 후 롱, 상단 접촉 후 숏이며, 목표는 볼밴
중심선이다. 이 가설은 백테스트의 양수 holdout 결과를 실제 지정가 체결률과
역선택까지 검증해야 한다.

## 2. e6 확정 설계

* 방향: CM·HullMA 추격 방향을 사용하지 않는 볼밴 역추세
* 롱: 하단 밴드 접촉/이탈 후 하단 지정가 매수
* 숏: 상단 밴드 접촉/이탈 후 상단 지정가 매도
* 청산: 볼밴 중심선 reduceOnly 지정가
* 손절: 거래소 조건부 STOP 필수, 초기에는 기존 보호 배선 유지
* 시간봉: 1분봉
* F1: 상위 20% 범위 우선 검증; 좁은 92~94%를 사용하지 않음
* e5와 별도 프로세스·PID·상태·원장·로그·텔레그램

## 3. 슬롯·증거금 정책

| 잔고 | 최소 진입 증거금 | 최대 슬롯 |
|---:|---:|---:|
| 100~200 USDT | 25 USDT | 4 |
| 200~300 USDT | 35 USDT | 6 |
| 300~500 USDT | 50 USDT | 8 |
| 500 USDT 초과 | 50 USDT | 구간별 +2 |

슬롯은 동시 진입 보장이 아니라 상한이다. 가용 증거금과 최대 노출 한도를
우선하며, 최소 증거금을 지키기 위해 무리하게 노출·레버리지를 확대하지 않는다.
초기 실거래는 4슬롯 상한과 고정 사이즈로 시작하고 복리 사이징은 검증 후에만
허용한다.

## 4. 체결·회전율 보호

* 심볼별 동시 진입 주문 1개
* 지정가 진입 TTL 30~45초, 미체결 자동 취소
* 부분체결 최소비율 미달 시 잔여 주문 취소 및 잔량 정리
* 심볼별 청산 후 10~15분 재진입 쿨다운
* 심볼별 시간당 진입 상한
* 주문시각·호가·중간가·스프레드·체결 대기시간 기록
* 체결 후 5초·15초 역선택 기록

## 5. 복리 전환 게이트

다음 조건을 모두 통과하기 전에는 복리 사이징과 슬롯 확대를 하지 않는다.

1. 실제 체결 100건 이상
2. 수수료·슬리피지 포함 순손익 플러스
3. 체결률과 미체결률 확인
4. 체결 직후 역선택이 백테스트 가정과 크게 다르지 않음
5. 단일 심볼 제거 후에도 결과 유지
6. 3개 이상 시간대/시장 국면에서 결과 유지
7. e6 원장과 거래소 userTrades/income 대조 일치
8. 손절 누락 0건 및 부분체결 잔량 0건

## 6. 현재 전환 상태

2026-09-02 확인 시점에 기존 e5의 마지막 포지션은 거래소 조회 결과 이미
종료되어 있었다. 포지션 0건, 일반 미체결 0건, 조건부 주문 0건을 확인했다.
e5 프로세스도 종료했다. 따라서 현재 e6 실거래 프로세스는 실행 중이 아니다.

e6는 아직 기존 e3/e5 실행부를 겉으로만 바꾸는 방식으로 기동하지 않는다.
볼밴 접촉 지정가 진입, 중심선 TP, 계층 사이징, e6 전용 원장과 체결·역선택
계측을 구현하고 테스트한 뒤에만 실거래를 시작한다.

## 8. 구현·기동 기록 (2026-09-02 23:33 KST)

e5에서 보호주문, 진입 TTL·미체결 취소, 부분체결 정리, WS 부착, 원장 배선을
재사용하고 e6 전용으로 다음을 교체했다.

* CM 방향 신호 제거, 1분봉 볼밴 하단 접촉 LONG / 상단 접촉 SHORT
* 진입 목표를 EMA 눌림선이 아닌 해당 볼밴으로 변경
* TP 계획값을 볼밴 중심선으로 변경
* F1 기본 범위 상위 20%(`80~100%`)
* 잔고 계층 최소 증거금: 100~200=25, 200~300=35, 300+=50 USDT
* 초기 최대 슬롯 4개, 1차 고정 사이즈
* e6 전용 Telegram/상태/브리핑 플래그

드라이런 스모크 테스트는 정상 종료·주문 0건으로 통과했다. 이후 거래소에서
포지션 0건·일반 미체결 0건·조건부 주문 0건을 확인하고 e6 실주문을 기동했다.

* 인스턴스 태그: `e6_live_20260902`
* 시작 잔고: 110.5484 USDT / 5배
* WS: 기존 워커 부착 모드
* 시작 시 복구 포지션: 0건
* 로그: `logs/scalp_bot_e3_cm_run_e6_live_20260902.log`

초기 로그의 전략명은 공통 e3 문구로 한 차례 기록됐으며, 코드에는 다음 재기동부터
`볼밴 역추세+밴드 지정가`로 표시되도록 수정했다. 첫 실거래 후 체결률·역선택·수수료
포함 순손익을 별도 e6 원장으로 확인해야 하며, 복리 사이징은 아직 금지한다.

## 7. 계좌 손익 해석 주의

계좌의 일일 플러스는 이체·수동매매·미실현손익·수수료와 분리해 확인해야 한다.
e6 성과 판정은 거래소 실현손익 및 수수료와 e6 clientOrderId 원장을 함께
대조한다. 봇 원장만으로 계좌 전체 성과나 복리 가능성을 판정하지 않는다.

## 9. CM 완전 제거 및 재시작 (2026-09-02 23:36 KST)

e6에서 CM은 방향 신호뿐 아니라 전환 연령 게이트와 확인 경로도 사용하지 않도록
완전히 제거했다. 실행 로그의 전략 라벨도 `볼밴 역추세+밴드 지정가`로 변경했다.

재시작 전 거래소 포지션·미체결 주문·조건부 주문이 모두 0건인 것을 확인했다.
기존 e6 프로세스를 종료하고 다음 설정으로 실주문 재기동했다.

* 인스턴스: `e6_live_nocm_20260902`
* 방향: 볼밴 하단 접촉 LONG / 상단 접촉 SHORT
* 체결: 밴드 지정가
* TP: 볼밴 중심선
* F1: `80~100%`
* 최대 슬롯: 4개
* 시작 잔고: 110.5484 USDT / 5배
* WS: `10/10` 준비 완료
* 로그: `logs/scalp_bot_e3_cm_run_e6_live_nocm_20260902.log`

현재 e6는 실주문 상태이나 아직 복리 사이징은 금지한다. 최소 100건의 실제 체결과
수수료·슬리피지·역선택 검증 전에는 전략 수익성을 확정하지 않는다.

## 10. 스캔 heartbeat·Telegram 응답 보강 (2026-09-02)

WS 준비 이후 메인 스캔 진행이 로그에 없고 텔레그램 버튼 처리가 긴 85심볼
스캔에 막히던 문제를 수정했다. 심볼 단위로 Telegram 입력을 중간 처리하고,
30초마다 `스캔 heartbeat 심볼n/85 보유n 대기n`을 기록한다. e6 명령은 `/e6`로
분리했다.

재시작 전 거래소 포지션·미체결·조건부 주문 0건을 확인했다. 최종 인스턴스
`e6_live_final_20260902`를 실주문으로 재기동했고, WS `10/10` 준비 및 첫
heartbeat `심볼10/85 보유0 대기0`을 확인했다.


## 11. 경로 시뮬레이션 + 실거래 현황 (2026-09-03 07:30, **정정본**)

**분석·제안 전용 기록이다.** 사용자가 *"거래를 건드릴 권한이 없어. 분석 및 제안만"*
이라고 명시했다. 아래 어떤 항목도 실행 지시가 아니다.

> **이 장은 앞선 초안을 정정한 것이다.** 초안은 수정 전 `e6_live_nocm_20260902`
> 로그를 현재 상태로 기록해 **"볼밴 접촉 0건 / 워밍업 부족 의심"** 이라고 썼는데
> **틀렸다.** 코덱스 지적이 맞다. 볼밴 신호 생성과 F1 선별은 정상이다.

### 11.1 실거래 현황 — e6 는 이미 거래했다

e6 원장 4종을 합산했다(2026-09-03 07:30 기준).

    conservative   1건   -1.1208
    stopfix2      11건   -4.9600
    tpcenter       3건   -0.9056
    tp42           6건   -2.8154
    ------------------------------
    합계          21건   -9.8018

    건당 -0.4668 / 승률 19.0% / 보유 중앙 2.9분
    gross(수수료 전) -8.3745 / 수수료 1.4273
    증거금 합계 563.3 -> **증거금 대비 -1.74%**
    청산: STOP_EXCHANGE 13 / TP_LIMIT 4 / UNDERFILL_CLEANUP 2 / EARLY_ADVERSE 2

**수수료 전에도 `-8.37`** 이므로 수수료 문제가 아니다.
순손익 정확히 0인 건이 1건 있다(수동개입/조회실패 추정기록 — 11.5 참조).

### 11.2 파이프라인은 정상 작동한다

`tp42` 로그 실측:

    진입주문 시도    7
    거래소등록 성공  7      <- 실패 0
    진입(체결확인)   9
    지정가 TP 등록   7
    TTL 만료         0
    미체결 취소      0

**"대기 후보 -> 발주" 구간은 막혀 있지 않다.** 시도 7건이 전부 등록·체결됐고
TTL 만료·미체결 취소가 0이다. 발주 파이프라인 계측을 추가할 필요가 현재로선 없다.

### 11.3 진짜 병목은 `사이클발주상한` 이다

    스킵누적 진입7 스킵392
      [근접손절순간1 쿨다운10 신호노후1 손절선통과1 방향편중11 **사이클발주상한368**]

**스킵 392건 중 368건(94%)이 사이클발주상한**이다. 신호노후는 1건뿐이다.

초안에서 내가 "신호노후 34건이 병목"이라고 쓴 것은 **WS 가 죽어 있던 시점
(00:23, `bbfix`)의 로그**였다. 그때는 `msg60=0 / consec=6,263,099 / 심볼 최신성
129.6분` 이었다. **현재 WS 는 복구됐다**(`msg60=4377 / err60=0 / 최신성 0.8분`),
그리고 `--help` 인자로 떠 있던 비정상 ws_worker 4개도 정리됐다.

즉 **신호노후는 WS 장애의 증상이었고 지금은 해소됐다.** 남은 병목은 발주상한이다.

**제안**: 발주상한이 의도된 보호장치라면 그대로 두는 것이 맞다(신호 대비 실제
진입이 1.8%). 다만 **표본이 안 쌓여 판정이 늦어진다**는 대가가 있다. 복리 게이트
1번(체결 100건)에 도달하는 속도를 좌우하므로, 상한을 조정한다면 그 목적을
명시하고 조정 전후를 분리 집계할 것을 제안한다.

### 11.4 경로 시뮬레이션 — TP 를 어떻게 잡아도 상한이 음수다

[scripts/bb_race_lab.py](scripts/bb_race_lab.py)(커밋 `bd1b667` 사전등록,
`2cc8fb2` TP 변형 추가). **파일은 저장소에 있다** — 코덱스가 확인하지 못한 것은
커밋 이전 시점을 본 것으로 보인다.

1분봉 60일 81심볼, F1 `80~100%`, 최대보유 60봉, stride 15.
비용 TP청산 `0.04%` / SL·시간초과 `0.07%`.

    TP 비율    손절 0.3%    손절 0.5%
      25%      -0.1720     -0.1893
      40%      -0.1682     -0.1851
      60%      -0.1650     -0.1838
      80%      -0.1672     -0.1855
     100%      -0.1674     -0.1870   <- e6 현행(중심선)

**10개 조합 전부 `-0.16 ~ -0.19%` 에 몰린다. 롱·숏도 모두 음수**라 드리프트가 아니다.

**왜 평평한가**가 핵심이다.

    TP 25%:  승률 41.9% x (+0.140) = +0.059
    TP 100%: 승률 21.9% x (+0.499) = +0.109

TP 를 가깝게 하면 승률이 그만큼 오르고 이익이 그만큼 준다 — **곱이 거의 보존된다.**
가격이 마팅게일에 가깝다는 뜻이고, 그 위에 손절이 고정 비용을 얹는다
(손절 0.3% 는 57~73% 발동, 매번 -0.300).

**가장 중요한 함의**: HANDOFF_2026-08-31 의 볼밴 역추세 `+0.0127%`(t `+3.3`/`+3.5`)는
**고정 15분 종가 청산** 기준이라 *"떨어졌다 15분 안에 회복한 거래"* 를 포함한다.
**TP/SL 구조는 그것들을 손절로 먼저 내보낸다.** 평균회귀는 실재하지만 손절선에
닿은 뒤에 일어난다. **두 측정은 다른 것을 재므로 `+0.0127%` 를 이 구조의 수익
근거로 쓰면 안 된다.**

### 11.5 이 시뮬레이션의 한계 (코덱스 지적 4번 — 전적으로 동의)

시뮬과 e6 실구현은 **체결 표본이 다르다.**

    시뮬:  밴드 접촉 -> 그 가격 즉시 체결(100%), TTL 없음, 부분체결 없음
    e6  :  밴드 지정가 -> TTL 45초 -> 미체결 취소, 부분체결 정리

**따라서 이 결과를 e6 실거래 성적의 예측으로 단정하면 안 된다.**
다만 10조합이 전부 같은 자리에 모여 있어, **체결 방식이 바꿀 수 있는 폭보다
손절 비용이 크다**는 점은 지적해 둔다.

실측이 시뮬과 같은 방향인 것도 사실이다(실거래 건당 `-0.4668` vs 시뮬 상한 `-0.167`).
다만 21건은 표본이 작아 그 자체로는 판정 근거가 아니다.

### 11.6 원장 != 계좌 (HANDOFF_2026-09-01 11장과 동일 주의)

* `drop_manual_trades` 가 수동 개입 체결을 봇 성과에서 분리한다
  (tp42 로그에 `수동 개입 체결 1건 제외 1000BONKUSDT` 기록).
* 청산 직후 체결조회 실패 건은 `real_net` 0 으로 기록된다(e6 21건 중 1건).

**계좌 기준 집계 도구가 여전히 없다.** userTrades/income 대조 없이는
복리 게이트 7번(`e6 원장과 거래소 대조 일치`)을 만족시킬 수 없다.
**이것이 지금 가장 시급한 계측 공백이다.**

### 11.7 현재 상태 (2026-09-03 07:30)

* **e6 프로세스는 실행 중이 아니다.** 마지막 로그 `tp42` 02:28,
  직전에 `일시정지 - 신규 진입만 멈춥니다`(02:25:16) 기록.
* 대신 **`e5_live_return2_20260903` 이 실행 중**이다(원장 07:11 갱신).
* WS 정상: `msg60=4377 / err60=0 / consec=0 / 심볼 최신성 0.8분`.
* 호가 기록기 정상 가동(작업 스케줄러 `ObookRecorderEnsure`).

### 11.8 제안 순서

1. **계좌 기준 집계 도구** — 원장만으로는 판정 불가. 복리 게이트 7번의 전제다.
2. **발주상한의 목적 명시** — 보호장치면 유지, 표본 확보가 목적이면 조정 전후 분리 집계.
3. **손절 구조 재검토** — 시뮬 10조합에서 손절 비용이 지배적이었다.
   다만 **청산 개선은 기대값 부호를 못 바꾼다**(HANDOFF_2026-08-31 20·24장).
4. 수익성 판정은 위 1번이 갖춰진 뒤에.

## 12. e5·e6 종합 진단 — e6 손익비는 산술적으로 불가능하다 (2026-09-03 10:07)

**분석·제안 전용 기록이다.** 사용자가 *"거래를 건드릴 권한이 없어. 분석 및 제안만"*
이라고 명시했다. 아래 어떤 항목도 실행 지시가 아니다.

### 12.1 전체 실적

                          e5              e6
    건수                  64건            21건
    순손익                -15.5451        -9.8018
    건당                  -0.2429         -0.4668
    승률                  23.4%           19.0%
    gross(수수료 전)      -11.1469        -8.3745
    수수료                4.3982          1.4273
    증거금 대비           -0.91%          -1.74%
    보유 중앙             4.1분           2.9분

**둘 다 수수료 전에도 적자다.** 수수료를 0으로 만들어도 진입 자체가 지고 있다.

일자별(KST, e5+e6 합산):

    09-02   24건   -4.1872   승률 21%
    09-03   61건  -21.1597   승률 23%
    합계    85건  -25.3469   건당 -0.2982

### 12.2 ⚠ e6 의 손익비는 이길 수 없는 구조다

              익절 중앙(가격)   손절 중앙(가격)   손익비    필요 승률   실제 승률
    e5          +0.817%         -0.441%       1:0.54     35.1%      23.4%
    e6          +0.276%         -0.533%       1:1.93     **65.9%**   19.0%

**e6 는 익절이 손절의 절반이다.** 손익비 `1:1.93` 이면 **승률 65.9%가 필요한데
실제는 19%** 다. 그리고 11장 경로 시뮬레이션이 낸 이 구조의 **이론 승률은
21.9%**(TP=중심선, 손절 0.3%)였다. **필요치의 3분의 1이다.**

즉 e6 는 표본 부족 문제가 아니라 **설계상 도달 불가능한 승률을 요구한다.**
원인은 명확하다 — TP 를 중심선으로 두면 목표가 가깝고(`+0.276%`), 손절은
상대적으로 멀다(`-0.533%`). e5 는 그 반대(TP 멀고 SL 가까움)라 필요 승률이 35%다.

**이것이 11장 시뮬레이션(10조합 전부 `-0.16~-0.19%`)의 실거래 확인이다.**

### 12.3 e6 에서 역선택 증거가 보인다

            손절건 MFE 중앙   1% 미달 비율
    e5        +0.80%          53%
    e6        +0.42%          **75%**

**e6 손절건의 75%가 유리 방향으로 1%도 못 갔다** — 진입하자마자 틀렸다는 뜻이다.

11장 11.5 에서 "시뮬이 못 잰 것"으로 명시한 **역선택**이 이것으로 보인다.
밴드에 지정가를 걸어 체결됐다는 것은 대개 **가격이 그 방향으로 계속 가는 중**
이라는 뜻이다. 즉 체결되는 표본이 "바로 반등하는 건"이 아니라 "계속 가는 건"에
쏠린다. 시뮬은 밴드 접촉 시 100% 체결을 가정해 이 효과가 빠져 있었고,
**그래서 시뮬(-0.167)보다 실거래(-0.467)가 나쁘다.**

### 12.4 방향·인스턴스 분해

    e5   LONG 41건 -0.2139 / SHORT 23건 -0.2945
    e6   LONG  6건 -0.3612 / SHORT 15건 -0.5090

**양방향 모두 음수다.** 한쪽이 다른 쪽을 보상하는 구조가 아니다.

    e5_live_return2      40건  -11.3579   승률 25%
    e5                   11건   -1.7413   승률 27%
    e5_age20              9건   -3.1777   승률  0%
    e5_f1_84_95           4건   +0.7318   승률 50%   <- 유일한 플러스
    e6_stopfix2          11건   -4.9600   승률 18%
    e6_tp42               6건   -2.8154   승률 33%
    e6_tpcenter           3건   -0.9056   승률  0%
    e6_conservative       1건   -1.1208   승률  0%

`e5_f1_84_95` 가 플러스이나 **4건이다.** 건당 표준편차가 `0.72%`(가격 기준)이므로
4건의 오차범위는 `±0.36%` 다. **설정의 우열을 말할 수 있는 표본이 아니다.**

### 12.5 표본 크기 문제 — 라이브는 검증 도구가 될 수 없다

실측 건당 가격수익률 표준편차 `0.72%`. t=2 로 엣지를 검출하려면:

    볼밴 역추세 +0.0127%   ->  12,858건   하루 20건이면    643일
    횡단면 역추세 +0.0081%  ->  31,609건   하루 20건이면  1,580일
    가상의 큰 엣지 +0.05%   ->     830건   하루 20건이면     41일

**측정된 최선의 엣지를 라이브로 확인하려면 643일이 걸린다.**

2026-09-02~03 사이 인스턴스가 10개 넘게 바뀌었다(e5 4종 + e6 6종+).
각각 1~40건이므로 **전부 통계적으로 무의미한 표본**이고, 하룻밤 손익이 전략 탓인지
운 탓인지 구분되지 않는다. **설정을 바꿔 하룻밤 돌리고 결과로 판단하는 루프는
정보를 거의 못 얻으면서 자금만 소모한다.**

### 12.6 결론

1. **e5·e6 모두 진입 엣지가 음수다**(gross 기준 `-11.1` / `-8.4`).
   청산 파라미터로는 부호를 바꿀 수 없다.
2. **e6 는 추가로 손익비가 불가능하다** — 필요 승률 65.9% vs 이론 승률 21.9%.
3. **e6 는 역선택 증거가 있다** — 손절건 75%가 MFE 1% 미달.
4. **라이브로는 검증할 수 없다** — 643일치 표본이 필요하다.

### 12.7 제안 (실행 아님)

* **검증 순서를 뒤집을 것.** 지금은 배포 후에 검증한다. 복리 게이트 8조건 중
  최소 둘은 **배포 전**으로 옮길 수 있다 —
  경로 시뮬 기대값 양수([bb_race_lab.py](scripts/bb_race_lab.py)),
  아웃오브샘플+순열검정([edge_signal_holdout.py](scripts/edge_signal_holdout.py)).
* **계좌 기준 집계 도구가 먼저다.** 원장은 수동매매를 빼고(`drop_manual_trades`)
  조회실패를 0 으로 적는다(e5 2건 / e6 1건). 손익을 정확히 알 수 없으면
  어떤 판정도 불가능하다. 복리 게이트 7번의 전제이기도 하다.
* **남은 실측 후보는 둘뿐이다** — 펀딩 캐리(네 관문 통과, 연 2.2%, 저유동성 조건)와
  호가·주문흐름(미탐색, 데이터 축적 중).

### 12.8 현재 상태 (2026-09-03 10:07)

* **봇 프로세스 없음.** e5 원장 마지막 갱신 08:33, e6 는 02:27.
* WS 정상, 호가 기록기 정상(작업 스케줄러 `ObookRecorderEnsure`).

## 13. 그림자 표본 및 방향 재검증 (2026-09-03)

e6 실거래는 재개하지 않고, 기존 그림자 표본을 이용해 순수 볼밴 역추세의
방향별 실행 가능성을 재확인한다. 밴드 지정가의 이론 진입가와 신호 시점의
실제 `mark_entry`를 분리한다. 전자는 즉시 체결 가정의 전략 기대값이고,
후자는 신호가 늦게 관측되었을 때의 역선택을 나타낸다.

### 13.1 현재 그림자 표본 37건

    기준                 전체       LONG       SHORT
    5초 mark 기준       +0.0002%   -0.0309%   +0.0296%
    15초 mark 기준      -0.0350%   -0.0459%   -0.0246%
    15초 양수 비율       29.7%       27.8%       31.6%

표본상 SHORT가 LONG보다 덜 나쁘지만, 15초 기준으로는 SHORT도 음수이며
수익 엣지로 판정할 수 없다. 또한 `mark_entry`는 밴드 이론 진입가보다
LONG·SHORT 모두 약 0.7% 불리한 방향으로 관측되어, 신호 감지 지연과
지정가 체결 표본의 차이를 별도 계측해야 한다.

### 13.2 다음 검증 규칙

* e6 실거래 및 복리 사이징은 중지 상태를 유지한다.
* SHORT 단독 그림자 검증을 우선한다. LONG은 현재 표본에서 후보에서 제외한다.
* F1은 상위 20%(80~100%)로 고정하며, 검증 중 문턱을 변경하지 않는다.
* 1분봉 볼밴 상단 접촉 후 SHORT만 기록한다.
* 각 표본에 이론 밴드가, 실제 관측 mark, 5/15/30/60초 mark,
  실제 지정가 체결 여부와 체결 직후 역선택을 기록한다.
* 최소 100건의 유효 표본과 수수료·슬리피지 차감 후 양수, 방향·시간대별
  재현성을 모두 확인하기 전에는 실거래 전환하지 않는다.

현재 결론은 “SHORT가 최선으로 확정”이 아니라 **SHORT가 다음 검증의
우선 후보**라는 수준이다. 기존 37건은 초기·F1 적용 전 기록이 섞여 있어
최종 판정 표본으로 사용하지 않는다.

### 12.9 추가 — 라이브 4건의 플러스는 60일 표본에서 음수였다 (2026-09-03)

12.4 에서 `e5_f1_84_95` 가 4건에 `+0.7318`(승률 50%)로 유일한 플러스였다.
같은 게이트를 1분봉 60일로 재서 실체인지 확인했다. **재현되지 않는다.**

    1분봉 60일, 보유 15분          건당 | 심볼중앙    t(시각/심볼)  건/심볼일
    [F1 84~95] + CM추세         -0.0135 | -0.0096   -1.0/-2.6     19.3   n=94,247
    [F1 80~100] + CM추세        -0.0093 | -0.0065   -1.8/-2.1     35.4
    [F1 92~94] + CM추세         -0.0129 | -0.0097   -0.6/-1.2      3.5
    CM 전체(선별 없음)           -0.0076 | -0.0045   -6.6/-5.7    187.8
    [순수볼밴] 단순터치 역추세     +0.0127 | +0.0106   +3.3/+3.5     33.4

**94,247건은 라이브 4건의 2만 배 표본이다.** 국면 3등분도 전부 음수
(`-0.0190` / `-0.0133` / `-0.0085`)라 특정 시기 문제가 아니다.

**그리고 F1 은 CM 을 개선하지 않는다. 좁힐수록 악화된다:**

    선별 없음  -0.0076  ->  80~100  -0.0093  ->  84~95  -0.0135

밴드폭 상위(고변동성) 구간에서 CM 추세추종이 특히 안 통한다는 뜻이다.

**이것이 라이브를 검증 도구로 쓰면 안 되는 구체적 실례다.** 사전에 계산한
4건의 95% 신뢰구간은 `-0.5297% ~ +0.8816%`(t `+0.49`)로 **어떤 값이어도 모순되지
않았고**, 실제 값은 음수였다. 신뢰구간 폭 `1.41%` 가 재려는 엣지 `0.0127%` 의
**111배**였다. 덧붙여 그 4건은 2건(DASH·TUT)이 플러스 전액을 만든 구조였다.
