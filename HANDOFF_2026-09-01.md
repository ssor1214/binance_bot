# HANDOFF 2026-09-01 — 탐색 축을 바꾼다: 방향 예측에서 구조적 프리미엄으로

이 문서는 **새 방향**의 시작점이다. 같은 날의 방향 예측 측정 열다섯 번은
[HANDOFF_2026-08-31.md](HANDOFF_2026-08-31.md) 16~24장에 있다. 그쪽을 먼저 읽을
필요는 없다. 결론만 알면 된다:

> **공개 봉 데이터로 방향을 맞히는 시도는 열다섯 번 전부 기각됐다.**
> 최선이 `+0.0127%/건`(비용선 `0.04%` 의 32%)이고, 현행 전략은
> 92만 건에서 `-0.0038%`(t `-5.3`/`-5.8`)로 유의하게 음수다.

---

# 1. 이 문서가 생긴 계기 — 사용자의 지적이 옳았다

열다섯 번째 기각 뒤 내가 "봉 데이터로는 안 된다, 남은 건 호가 축뿐"이라고 정리했다.
사용자가 반박했다: **"남들은 어떻게든 봇으로 수익을 만들고 있어. 우리가 못 찾았을
뿐이야."**

**맞는 말이었고, 내 프레임이 좁았다.** 내가 증명한 것은

> 공개 봉 데이터 + **방향 예측** + 5~60분 보유 + 개인 수수료 구조

이 조합이 비용선을 못 넘는다는 것이지, "자동매매가 안 된다"가 아니다.
그리고 실제로 수익을 내는 봇들은 대부분 이 조합 **밖**에 있다.

더 중요한 건 — **호가 축도 결국 방향 예측이다.** "다음에 오를까 내릴까"를 더 미세한
데이터로 맞히려는 것이다. 진짜로 다른 축은 **예측을 하지 않는 것들**이었고,
나는 그쪽을 한 번도 뒤지지 않았다.

---

# 2. 펀딩 캐리 — 오늘 처음으로 네 관문을 다 통과했다

## 왜 이게 측정된 적이 없었나

`edge_lab.py` 는 **방향 신호**를 잰다. 그래서 펀딩도 "펀딩이 높으면 숏" 같은
**방향 예측**으로만 테스트됐다(CLAUDE.md 원칙 0 보강의 "가격 외 데이터" 항목).

**델타 중립 캐리**(현물 롱 + 선물 숏으로 방향 위험을 없애고 펀딩만 수취)는
방향을 예측하지 않으므로 **edge_lab 이 구조적으로 측정할 수 없다.**
도구가 없어서 안 잰 축이지, 재보고 기각한 축이 아니었다.

도구를 새로 만들었다: [scripts/funding_carry_lab.py](scripts/funding_carry_lab.py)
(판정 기준을 **결과 보기 전에** 커밋 — `8b33811`).

## 결과

136심볼(생존 97 + 상장폐지 39) x 3,458회 정산(8시간), train 60% / holdout 40%:

    관문2  train->holdout 심볼별 평균펀딩 상관   +0.211                  통과
    관문3  holdout 3등분  +0.0023 / +0.0014 / +0.0023  전부 양수         통과
    관문4  심볼셔플 순열 1,000회   백분위 100.0% (p=0.000)               통과
    관문1  비용 차감 후 holdout 총수익   +2.567%                         통과

K 별 상세(하나를 고르지 않으려고 전부 보고한다):

| K | holdout 연율 | 비용차감 후 | 폐지수 |
|---|---|---|---|
| 5 | +1.72% | +1.940% | 3 |
| 10 | +2.22% | +2.560% | 4 |
| 15 | +2.22% | +2.567% | 5 |
| 20 | +2.24% | +2.590% | 7 |
| 30 | **+0.05%** | -0.176% | 10 |
| 전체 | **-1.54%** | -2.184% | — |

## 왜 이건 다른가 — 예측이 아니라 구조다

펀딩이 양수인 이유는 예측이 아니라 **시장 구조**다. 레버리지 롱 수요가 만성적으로
많아서 **76.2%의 시간 동안 롱이 숏에게 낸다.** 구조는 지속되므로 train->holdout
상관이 남는다.

오늘 죽은 후보들과 대조하면 차이가 선명하다:

| 후보 | 홀드아웃 검정 |
|---|---|
| 위상(시간대) 계절성 | 상관 **-0.043**, 순열 백분위 65.7% |
| 볼밴눌림 | 심볼셔플 순열 백분위 **69.4%** (p=0.31) |
| **펀딩 캐리** | 상관 **+0.211**, 순열 백분위 **100.0%** (p=0.000) |

관문 4 가 특히 강하다 — 무작위 15심볼의 holdout 평균이 `-0.00137` 인데 실제 선택은
`+0.00203` 이고, **1,000회 중 단 한 번도 실제를 넘지 못했다.**

---

# 3. 그러나 실행 권고가 아니다 — 경제성이 미달이다

통계 관문을 통과한 것과 돈이 되는 것은 다르다. 다섯 가지가 걸린다.

**① 연 +2.2% 다.** 상장폐지를 뺐던 예비 측정에서는 `+4.82%` 였는데 **넣자 절반이
됐다.** 오늘 열다섯 번 반복해서 본 그 패턴이 여기서도 그대로 나왔다.

**② 자본이 두 배 묶인다.** 현물 매수 + 선물 증거금. 위 수치는 **명목 대비**이므로
**총자본 대비로는 연 1.1~1.5%** 다. 무위험 이자율 수준이거나 그 아래다.

**③ 선택한 15개 중 5개(33%)가 holdout 중 상장폐지됐다.** 코드는 폐지 후 구간을
"기여 0"으로 처리하는데(`carry(..., dead_as_zero=True)`), 실제로는 **강제 청산**이다.
현물을 어떤 가격에 파는지, 선물 숏이 어떻게 정리되는지가 손익에 들어가야 하고
**그 손실은 이 숫자에 없다.**

**④ K=30 에서 무너진다**(`+0.05%`). 136심볼 중 상위 15~20개, 즉 15%에서만 작동한다.
그리고 그 심볼들은 정확히 **저유동성 소형주**다(KOMAUSDT, CLOUSDT, AKEUSDT 등).

**⑤ 안 잰 것**
* **베이시스 변화** — 델타중립 손익 = 펀딩 + (베이시스 변화). 현물 가격 데이터가
  없어 재지 못했고 **0 으로 가정**했다. 장기 보유에서 기대값은 0 에 가깝지만
  (펀딩이 perp 를 spot 에 붙들어 두는 기제이므로) 가정은 가정이다.
* **청산 위험** — 선물 숏 다리는 가격 급등 시 증거금이 필요하다.
* **실제 진입 슬리피지** — 소형주라 비용 모델(현물 왕복 0.20% + 선물 왕복 0.04%)
  보다 클 수 있다.

**마진이 얇아서, 안 잰 위험 하나만 현실화돼도 음수가 된다.**

---

# 4. 그래서 가져갈 결론

**"펀딩 캐리를 실행하자"가 아니라 "탐색 축이 틀렸다"** 이다.

> **구조적 프리미엄(캐리·베이시스·유동성 제공)은 방향 예측보다 지속성이 있다.**
> 앞으로 후보를 찾을 때 "가격이 어디로 갈까" 축을 먼저 뒤지지 말 것.

이 한 줄이 오늘의 실질적 성과다. 열다섯 번의 기각은 이 문장을 얻기 위한 비용이었다.

---

# 5. 다음에 볼 축 (전부 예측 없는 것들)

| 축 | 성격 | 데이터 상태 |
|---|---|---|
| 펀딩 캐리 | 통과했으나 연 2.2%, 마진 얇음 | 있음 |
| **베이시스 캐리** | 분기물 선물 vs 현물 가격차 | 없음, 공개 API 로 수집 가능 |
| **거래소 간 차익** | 같은 코인의 거래소별 가격차 | 없음, 공개 API 로 수집 가능 |
| **유동성 제공(마켓메이킹)** | 스프레드를 **번다** | 호가 데이터 축적 중 |
| 신규상장 구조 | 상장 초기 가격 왜곡 | 부분적 |

## 마켓메이킹이 특히 중요한 이유

24장에서 확인한 **1분 해상도 평균회귀**(`+0.0081`, t `+7.0`/`+6.9`, 60일 59만 건,
국면 무관)는 **마켓메이커가 수익화하는 바로 그 현상**이다.

우리는 그걸 **스프레드를 내면서**(시장가로 건너뛰며) 잡으려 했고, 그래서 비용선의
20~32%에 갇혔다. **스프레드를 버는 쪽에 서면 부호가 바뀐다.**

다만 이때 봐야 할 것이 다르다. 지금 `obook_lab.py` 에 사전등록한 8개는
**"방향을 맞히는 신호"** 를 향해 있다. 마켓메이킹 판단에 필요한 건 그게 아니라

* 어느 가격에 걸면 얼마나 체결되는가(체결 확률)
* 체결됐을 때 얼마나 역선택당하는가(체결 직후 가격이 계속 가는가)
* 재고가 쌓였을 때 어떻게 청산하는가

**이건 별도 측정 설계가 필요하다.** `obook_lab.py` 의 사전등록은 그대로 두고
(11월에 그 8개는 그 8개대로 판정), 마켓메이킹용은 새 하네스로 만든다.

## 권하는 다음 순서

**거래소 간 차익**이 다음으로 좋다고 본다:
* 데이터를 공개 API 로 모을 수 있다(인증 0회)
* 예측이 아니라 **관측**이다 — 가격차는 지금 존재하거나 안 하거나다
* 오늘 만든 홀드아웃·순열 도구를 그대로 쓸 수 있다
* 실패해도 빨리 안다

---

# 6. 도구 현황

| 도구 | 용도 |
|---|---|
| [edge_lab.py](scripts/edge_lab.py) | **방향 신호** 전용. 드리프트중립 + 이중클러스터t + 심볼중앙값 + stride + 이중비용선 |
| [funding_carry_lab.py](scripts/funding_carry_lab.py) | 델타중립 캐리. 방향 예측이 아닌 축의 첫 도구 |
| [edge_signal_holdout.py](scripts/edge_signal_holdout.py) | 고정 규칙의 아웃오브샘플 + **심볼셔플 순열**. 마지막 관문 |
| [edge_phase_holdout.py](scripts/edge_phase_holdout.py) | **선택 자유도**(N개 중 K개 고르기)를 검정 |
| [obook_lab.py](scripts/obook_lab.py) | 호가·주문흐름. 사전등록 8개 고정, 11월에 판정 |
| [edge_delisted.py](scripts/edge_delisted.py) | 상장폐지 심볼 수집. **오늘 최대 살상 요인이었다** |

## 반드시 지킬 것 (오늘 실제로 걸린 함정들)

1. **판정은 `--rescore` 또는 `--phase-sweep` 으로 한다.** `edge_lab` 의 기본 표는
   **stride 위상 0 만** 적용한다. 그걸 판정에 쓰면 위상 운을 엣지로 읽는다
   — 오늘 실제로 `+0.0353`(위상0)을 보고했는데 위상평균은 `+0.0095` 였다.
2. **상장폐지를 반드시 포함한다.** 1h 절제실험에서 35건 -> 1건으로, 단일 요인 중
   가장 크게 후보를 죽인다.
3. **판정 기준을 결과 보기 전에 커밋한다.** 오늘 그렇게 해서 여러 번 골대 이동을 막았다.
4. **여러 후보가 동시에 통과하면 국면 분해부터 한다.** 같은 구간에서 같이 죽으면
   후보가 여러 개인 게 아니라 하나의 국면 요인이다.
5. **조합 전에 중복도부터 확인한다.** 같은 기간의 이동평균을 쓰는 지표끼리는
   필터가 아니라 중복이거나 배타다(오늘 네 번 확인).

---

# 7. 현재 가동 상태

* **기존 e3/v2 라이브 봇은 정지.** 재개 조건(CLAUDE.md 원칙 0 보강 2)을 넘긴 신호가
  아직 없다. 현행 진입 방향은 92만 건에서 유의하게 음수다. e5는 별도 실험 인스턴스로
  운용 중이며, 실전 수익화 승인으로 해석하지 않는다(상세는 8장).
* **호가 기록기만 상주 중.** 런처는 [start_obook_watchdog.ps1](start_obook_watchdog.ps1)
  (구 `start_obook.ps1` 은 죽으면 되살리지 않는다 — 실제로 8.7시간 만에 조용히 죽어
  1.9시간을 잃었다). 5초 버킷 / 83심볼.
  * **분석 시 `last_price` 를 쓰지 말 것.** 체결이 없는 창에 0 이 적힌다(13.8%).
    가격은 항상 **중간가 `(bid+ask)/2`** 를 쓴다.
* 판정 시점은 **11월경** — 표본이 한 달 + 세 국면을 덮은 뒤.

---

# 8. e5 F1 선별 실험 및 현재 상태 (2026-09-02)

## F1/F3 50분위 본 재확인

V3 체결 2,049,139건(81심볼, train 1,211,173 / holdout 837,966), 보유 15봉,
stride 15, 순열 500회 결과:

| 지표 | 선택 분위 | Holdout | Train↔Holdout 상관 | 순열 백분위 |
|---|---:|---:|---:|---:|
| F1 밴드폭 | 47/50 | +0.0549% | +0.12 | 100% |
| F3 역추세강도 | 1/50 | +0.0445% | +0.32 | 91% |

무선별 기준선은 train `+0.0178%`, holdout `+0.0249%`였다. F1/F3 모두 holdout
평균은 현행 maker 비용선 `+0.0400%`를 넘었지만, F1의 상관은 `+0.12`로 약하고
F3은 순열 백분위 95% 기준을 넘지 못했다. 따라서 두 결과는 **수익화 후보 신호**이지
실전 수익성의 확정 증명이 아니다.

F1의 50분위 프로파일은 최상위 50분위에서 train `-0.0349%`로 급락했다. 따라서
F1 효과가 넓고 단조로운 효과가 아니라 특정 극단 구간·저유동성 종목군에 집중될
가능성이 있다. F1과 F3은 같은 저유동성·고변동 집단을 선택하는지 교차 검증이
남아 있다.

## e5 구현

[scripts/scalp_bot_e5.py](scripts/scalp_bot_e5.py) 는 e3 실행·보호주문 배선을 분리
재사용하고 F1 선별 게이트를 추가한다.

* 방향: e3 CM 방향 신호
* 시간봉: **1분봉 신호 / 1분봉 진입 실행**
* F1 선택: 47/50을 운용상 `92~94` 백분위 구간으로 구현
* 분위 기준: 심볼별 최근 1,440개 1분봉의 F1 rolling 분포
* 실행: 단일 진입, 진입 지정가 TTL 45초, 거래소 보호손절
* 슬롯: 최대 **8개**
* 기본 모드: dry-run; `--e5-live` 없이는 실주문 불가
* 원장·상태·PID·로그: e3와 분리

주의: 백테스트의 47/50은 pooled train 분포에서 고른 분위이고, e5 라이브 게이트는
각 심볼 rolling 분포의 92~94 백분위다. 따라서 e5 구현은 실행 가능한 근사이며,
두 규칙이 동일하다고 표현하지 않는다. F3은 현재 e5 진입 게이트에 넣지 않았다.

## 호가·WS 및 실거래 상태

* 5초 호가 기록기: 83~85심볼, 정상 기록 중.
* kline WS worker: 2026-09-02 복구 완료. 최근 85심볼, 최근 60초 메시지 1,515건,
  오류 0건, 평균 지연 약 239ms 확인.
* e5: 실주문 모드로 기동했으며, 1분봉·8슬롯·F1 92~94 백분위 설정.
* 기동 시 포지션 0, 일반 미체결 주문 0, 알고리즘 주문 0.
* 기동 후 확인된 실제 체결 0건. ZKCUSDT 후보 1건은 이미 익절선 위라 진입 차단.
* v2 봇 프로세스는 실행하지 않는다.

현재 e5는 **운영 배선 확인용 실거래** 상태이며, F1의 실제 체결률·역선택·순손익이
확인되기 전에는 수익화 성공으로 판정하지 않는다. 라이브 원장과 5초 호가 데이터가
누적되면 F1 후보→주문시도→체결→미체결취소→청산을 시간당으로 분리해 재평가한다.

## 9. e5 TP 상한 비교 및 2026-09-02 현재 상태

### TP 상한 비교

동일한 `cm_backtest.py --stage 3` 조건(84심볼, 3분봉 4,500개, 약 9.4일,
수수료·슬리피지 포함)으로 TP 상한을 비교했다.

| TP 상한 | SL 상한 | 거래/h | 승률 | 순엣지 |
|---:|---:|---:|---:|---:|
| 4.3% ROE | 8% | 5.9 | 68.5% | -0.1416% |
| 6% ROE | 8% | 4.4 | 62.1% | -0.1598% |
| 10% ROE | 8% | 2.8 | 53.9% | -0.1220% |
| 무제한 | 8% | 1.9 | 47.0% | -0.1000% |

4.3% 상한은 6%보다 거래 빈도·승률이 높아 1분봉 운용에 더 적합한 후보로
선정했다. 다만 순엣지는 모든 설정에서 수수료·슬리피지 포함 음수이므로,
4.3% 적용은 수익성 확정이 아니라 운영 실험이다.

### SL 점검

현재 e5 포지션은 `CLOUSDT LONG`, `PENDLEUSDT LONG` 두 건이며 거래소에 일반
미체결 TP와 조건부 STOP이 각각 실제 등록되어 있다. CLO 손절은 진입가 대비
약 -0.75%(5배 ROE 약 -3.7%), PENDLE 손절은 약 -0.50%(5배 ROE 약 -2.5%)로
확인됐다. 따라서 현재 SL은 과도하게 넓지 않고, 새 TP 상한과 충돌하지 않으므로
이번 변경에서 SL은 손대지 않는다.

### 재시작 보류 및 적용 대기

TP 4.3% 적용 재시작은 포지션이 살아 있어 보류했다. 현재 확인 시점의 거래소
상태는 포지션 2건, 일반 미체결 2건, 조건부 STOP 2건이다. 포지션 종료 후
`e5_age20_20260902`를 `--cm-tp-max-roe 4.3`으로 재기동해야 하며, 기존 포지션의
TP를 임의로 취소·재등록하지 않는다.

## 10. F1 필터 3% 원복 및 e5 재시작 (2026-09-02)

기존 운용 범위 `81~95%`에서 완화 폭 3%p를 원복해 F1 범위를 `84~95%`로
변경했다. 상한 95%는 유지하고 하한만 올렸으므로 극단적인 저유동성 구간의
일부를 다시 제외하는 변경이다.

재시작 전 거래소 확인 결과 포지션 0건, 일반 미체결 주문 0건, 조건부 주문 0건이었다.
이후 기존 e5 프로세스를 종료하고 다음 분리 태그로 실주문 재기동했다.

* 인스턴스/기록 태그: `e5_f1_84_95_20260902`
* F1: `84~95%`
* 시간봉: 1분 신호 / 1분 진입
* 슬롯: 최대 8개
* 재시작 잔고: 109.5249 USDT / 레버리지 5배
* WS: `10/10` 준비 완료, 실시간 스캔 재개 확인
* 로그: `logs/scalp_bot_e3_cm_run_e5_f1_84_95_20260902.log`

재시작 직후 기존 포지션 복구 0건이며, 이후 체결·미체결 취소·청산은 새 태그
원장으로 기존 실험과 분리 집계한다.

---

# 10. e5 실거래 진단 — 원장이 계좌 손익을 담지 못한다 (2026-09-02 23:30)

**분석·제안 전용 기록이다.** 사용자가 명시했다: *"너는 거래를 건드릴 권한이 없어.
개선할 수 있는 방법에 대해서 분석 및 제안만 할 수 있어."* 아래 어떤 항목도
실행 지시가 아니며, 적용 여부는 전적으로 사용자가 결정한다.

## 10.1 ⚠ 가장 먼저 알아야 할 것 — 원장 != 계좌

e5 원장으로 손익을 판단하면 **틀린다.** 두 경로로 실제와 어긋난다.

1. **수동 개입 체결이 원장에서 통째로 빠진다.** `drop_manual_trades` 가
   `clientOrderId` 접두사(`x-`)가 없는 주문의 체결을 봇 성과에서 분리한다.
   실행 로그에 **`수동 개입 체결 ... 제외` 경고가 9회** 기록돼 있다
   (e5 4회 + e5_age20 5회). 즉 **사용자가 직접 한 매매는 원장에 없다.**
2. **청산 직후 체결조회 실패 건이 손익 0 으로 기록된다.** 20건 중 2건이
   `real_net` 정확히 `0.0000` 인데, 실제로는 계좌에 반영됐다:
   ```
   ASTERUSDT  LONG  ROE +4.378%  -> 원장 net +0.0000
   PENDLEUSDT LONG  ROE -1.091%  -> 원장 net +0.0000
   ```

**결론: 계좌 잔고가 실제이고, 원장은 봇 자동매매 부분의 일부만 담는다.**
사용자가 "금일 PnL 이 +" 라고 보고했는데, 원장만으로는 그 차이를 재구성할 수 없다.
차이는 (a) 수동 매매 (b) 미실현 포지션 (c) 위 추정기록분에서 나온다.
**앞으로 e5 성과를 논할 때는 반드시 거래소 계좌 기준을 병기할 것.**

## 10.2 봇 자동매매 부분의 실적 (원장 기준, 한계는 위와 같다)

    e5 + e5_age20 합계 20건 (2026-09-02)
      원장 기록 순손익        -4.9190 USDT
      수동청산 2건 ROE 환산    +0.8676
      추정 실제(봇 부분)      -4.0514 USDT

    증거금 기준 (건당 증거금 중앙 25.81 USDT, 투입 합계 528.22)
      원장 기준        -0.93%
      수동청산 반영    -0.77%

    승률 15.0% (3승 17패) / gross(수수료 전) -3.7573 / 수수료 1.1617

## 10.3 진단 — e3 와 실패의 성격이 다르다

| | e3 (1,017건) | e5 (20건) |
|---|---|---|
| 손절건 MFE 1% 미달 | 73% | **47%** |
| 보유 중앙 | 5~6분 | **2.1분** |
| 진단 | 진입부터 틀림 | **절반은 맞았다가 잘림** |

**손절선이 승자와 패자를 구분하지 못한다.** 실측:

    익절건이 견딘 역행폭   0.20 / 1.24 / 1.85 % ROE
    손절이 발동한 ROE      0.64 / 1.09 / 1.82 / 2.15 / 2.60 / 2.73 / 2.73 / 2.97 / 3.59

**익절건 3개 중 2개가 손절선 깊이까지 밀렸다.** 즉 지금 손절선은 노이즈 구간
안에 있고, 승자 일부를 잘라내고 있을 가능성이 높다.

**그리고 이건 구조적이다** — F1 92~94 백분위는 **밴드폭 상위 = 변동성 높은 심볼**을
의도적으로 고른다. 그런데 손절은 **고정 ROE** 다. 변동성 높은 종목을 골라놓고
고정 손절을 걸면 정확히 그 종목들에서 노이즈에 잘린다.
**선별 기준(변동성)과 리스크 기준(고정)이 서로 다른 단위를 쓴다.**

## 10.4 제안 (실행 아님 — 판단은 사용자 몫)

### 제안 A — 손절을 변동성 스케일로

    현행:  손절 = 고정 ROE
    제안:  손절 = k x 밴드폭(또는 ATR)   k 는 백테스트로 결정

F1 이 이미 밴드폭을 계산하므로 새 지표가 필요 없다. 근거는 10.3 의 겹침이다.

**한계를 분명히 할 것**: 이건 **청산 개선이라 기대값의 부호를 못 바꾼다.**
진입 기대값이 음수면 손절을 고쳐도 덜 잃을 뿐이다(HANDOFF_2026-08-31 20·24장에서
반복 확인). 다만 **승자를 덜 자르는 효과**는 실측 근거가 있다.

### 제안 B — 방향 (측정 진행 중)

가설: **밴드폭이 크다 = 변동성이 높다 = 평균회귀가 강한 국면**이다. 그렇다면
F1 이 고르는 국면과 CM 추세추종은 서로 반대일 수 있다.

`edge_lab` 에 e5 와 동일한 게이트(심볼별 1,440봉 rolling, 92~94 백분위)를
사전등록해 방향만 바꿔 비교 중이다(커밋 19ed760):

    [F1] 선별 + CM추세 (= e5 현행)
    [F1] 선별 + CM반전
    [F1] 선별 + 볼밴역추세
    [F1] 선별만 (선별 자체의 드리프트 확인용)

**이쪽이 부호를 바꿀 수 있는 유일한 축이다.** 결과는 다음 세션에서 확인할 것.

### 참고 — 이미 측정된 관련 사실

* CM 방향은 1분봉 60일 **n=919,019** 에서 `-0.0038|-0.0033` (t `-5.3`/`-5.8`),
  보유 15분은 `-0.0076` (t `-6.6`/`-5.7`), 3구간 전부 음수.
  **F1 은 음수 방향 신호 위에 얹은 필터다.**
* e5 의 TP 상한 비교는 **모든 설정에서 순엣지가 음수**였다
  (4.3% `-0.1416` / 6% `-0.1598` / 10% `-0.1220` / 무제한 `-0.1000`).
  9장이 이를 *"수익성 확정이 아니라 운영 실험"* 이라고 명시하고 있다.
* 봉 기반 방향 신호의 측정된 최선은 `[순수볼밴] 단순터치 역추세` `+0.0127%`
  (비용선 `0.04%` 의 32%)이고, **CM 은 그 반대편이다.**

## 10.5 다음 세션이 할 일

1. **F1 x 방향 비교 결과 확인** (백그라운드 측정, 커밋 19ed760 에 사전등록됨).
2. **계좌 기준 성과 집계 도구가 필요하다.** 원장만으로는 수동 매매와 추정기록분이
   빠져 실제와 어긋난다. 거래소 `userTrades` / `income` 기준 일일 집계를 별도로
   만들어야 e5 판정이 가능하다. **이게 지금 가장 시급한 계측 공백이다.**
3. 현재 인스턴스 3종의 상태 파일이 있다:
   `e5` / `e5_age20_20260902` / `e5_f1_84_95_20260902`.
   원장을 섞지 말 것.
