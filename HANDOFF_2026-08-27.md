# 핸드오프 2026-08-27 — e3 CM 스캘퍼

전날: [HANDOFF_2026-08-26.md](HANDOFF_2026-08-26.md). 여기엔 8/27 분만 적는다.

**하루 요약**: 청산선(SL/TP)을 여섯 번 바꿔도 손익 부호가 안 바뀌었다. 원인을 찾으려
매매를 일시정지하고 3일 × 150심볼(12만건)로 진입 엣지를 직접 쟀다. **엣지는 있으나
거래비용과 같은 크기**였다. 4시간 EMA200 필터가 유일한 개선이었다.

---

# 1. 지금 가동 설정 (20:00~, [start_e3.ps1](start_e3.ps1) / 그림자 [start_e3_shadow.ps1](start_e3_shadow.ps1))

```
--signal-tf-min 3 --tranches 1 --cm-atype 4
--min-leg-margin 30 --max-leg-margin 50
--max-concurrency 10 --max-same-side 3
--min-entry-edge-pct 0
--stop-widen-pct 0.65
--cm-tp-pullback-pct 0.5 --cm-tp-max-roe 4.3
--max-signal-age 10 --watchdog-sec 180 --max-exposure 0.95
--adopt-max-stop-roe 5 --new-max-stop-roe 0
--stop-rr-match 0
--early-adverse-sec 0 --mae-cut-roe 0
--cm-htf-filter --cm-htf-counter-mult 0          <- 오늘 신규
--stop-limit --stop-limit-slip-pct 0.05          <- 오늘 신규
--stop-limit-timeout-sec 20 --stop-limit-fail-pct 0.30
```

**인자는 이제 `start_e3.ps1` 한 곳에서만 관리한다.** 종전엔 재시작할 때마다 손으로
붙였고, 그래서 플래그가 조용히 유실되는 사고가 반복됐다.

---

# 2. 오늘의 핵심 측정 — 엣지와 비용이 같은 크기다

85심볼 × 3일(367,256봉), **신호 봉 다음 봉 시가 진입**(lookahead 차단),
**청산 규칙 미사용**(승률 지표가 청산규칙과 동어반복이 되는 함정 회피).

```
                        3분      10분     15분     30분     건수
CM HullMA20 / 3분봉   +0.0124  +0.0298  +0.0341  +0.0018   78,980   t+18.6
CM HullMA10 / 3분봉   +0.0045  +0.0208  +0.0280  +0.0236   71,438
CM HullMA40 / 3분봉   -0.0002  -0.0077  -0.0244  -0.0907   82,183
CM HullMA20 / 5분봉   +0.0009  -0.0094  -0.0217  -0.0780   46,013
CM HullMA20 / 15분봉  -0.0133  -0.0453  -0.0640  -0.1202   13,150   t-14.0
atype=1 (SMA)         -0.0077  -0.0262  -0.0453  -0.1095   92,043
+ 4h EMA200 필터      +0.0199  +0.0536  +0.0689  +0.0705   46,385   t+28.0
+ 눌림 상한 0.5%      +0.0126  +0.0286  +0.0333  +0.0066   68,652
```

**현재값(3분/HullMA20)이 이미 최적점이다.** 기간을 늘리거나 신호봉을 늘리면 전부
마이너스로 돌아선다. 눌림 상한은 개선이 없다(어제 기각이 맞았다).

## 수수료를 빼면 부호가 갈린다 — 이것이 하루 종일의 원인

실측 왕복 수수료(원장): 익절 0.0276% / 손절 0.0586%. 보유 15분 고정 청산 기준.

```
구성                    건수     총수익률      t      순익(최악)   순익(최선)   승률
1군(1~85위) 필터없음   78,692   +0.0341%  +18.6    -0.0521%    -0.0211%   50.2%
1군 + 4h필터           36,926   +0.0866%  +31.7    +0.0004%    +0.0314%   54.6%
2군(86~150위) 필터없음 59,502   +0.0315%   +9.7    -0.0547%    -0.0237%   49.3%
2군 + 4h필터           28,514   +0.1004%  +20.7    +0.0142%    +0.0452%   53.9%
```

**청산선을 어떻게 배열해도 부호가 안 바뀐 이유가 첫 줄이다** — 엣지가 확실히
플러스(t=+18.6)인데 수수료를 빼면 마이너스다. 승률 60%에도 자산이 줄었다.

---

# 3. 채택

## 4시간 EMA200 필터 (`--cm-htf-filter` + `--cm-htf-counter-mult 0`)

**원칙 0의 나머지 절반이다.** CLAUDE.md는 "15분 HullMA20 방향/교차 + **4시간
EMA200 필터**"인데 이 플래그는 기본값 False라 **한 번도 켜진 적이 없었다.**

- 순익 부호를 바꾼 **유일한** 요소: −0.0521% → +0.0004% (수수료 차감 후)
- `counter-mult 0`이어야 한다. 기본 0.3(크기 축소)은 증거금 하한 30 때문에
  이 잔고대에서 무력하다(32.9 → 30, 실효배율 0.91).
- **판정 불가(None)도 이제 막는다**(`상위추세미확정`). 종전엔 통과시켰는데, 기동
  직후 캐시가 비는 30초 동안 필터가 통째로 꺼진 채 진입한다. 신호가 시간당
  1,097건인데 봇 소화량은 18건(1.7%)이라 건너뛰어도 원칙 1에 영향이 없다.
- 실측(11:09 스킵누적): `상위추세역행 45건`(스킵의 41%) 차단 확인.

**거래수는 줄지 않는다.** 신호 1,097 → 644/h가 돼도 소화량의 36배가 남는다.
어제 이 필터를 "거래수 −40%"로 기각했던 것은 **1신호=1거래로 잘못 계산**한 것이다.

## MAE/MFE 계측 수리 — 봉 복원 (`rebuild_excursion`)

4절 참조. 이것 없이는 "진입이 나쁜가 / 청산이 나쁜가"를 구분할 수 없었다.

## 부분체결 미달분 정리 (`--min-fill-ratio 0.5`)

지정가 진입이 일부만 체결되면 잔량을 취소하므로 포지션이 목표보다 작게 남는다
(실측 REUSDT 309 주문 → 24 체결 = 명목 12.5). **슬롯은 크기와 무관하게 한 자리를
통째로 쓴다.** 체결량이 주문의 50% 미만이면 그 자리에서 시장가로 정리한다.
잔량 취소 → 그 다음 청산 순서를 지킨다(반대면 잔량이 뒤늦게 체결돼 반대 포지션이 열린다).

## 증거금 하한을 축소배율 뒤로 이동

종전 순서가 `max(30, 비율) × 배율`이라 배율이 겹치면 9 USDT까지 떨어졌다
(실측 21:36 REUSDT 31.96 → 9.59). 배율을 다 곱한 **뒤에** 하한을 적용한다.

**부작용을 반드시 알 것**: 하한이 서면 크기 축소라는 수단 자체가 사라진다.
이 잔고대(기준 증거금 32.9)에서 어떤 배율을 넣어도 실효배율이 0.91이다.
배율이 살아나려면 잔고 약 737 USDT 이상이 필요하다.

---

# 4. 사고 기록

## MAE/MFE 계측 — 두 번 틀렸다

**(1) 내 분석 오류**: MAE는 **음수**로 저장되는데(`min(0, roe)`) `> 0`으로 걸러서
"승자 118건 중 MAE 0건 = 완전 고장"이라고 잘못 보고했다. 실제로는 131건 중 48건에
기록이 있었다. **원장 필드의 부호를 먼저 확인할 것.**

**(2) 진짜 결함**: 메인 사이클(분 단위) 폴링에서만 갱신해 **짧은 거래를 통째로
놓쳤다.**

```
보유 0~1분   52건  계측 있음  4건 ( 8%)
보유 1~2분   26건            12건 (46%)
보유 2~5분   35건            25건 (71%)
보유 5~15분  55건            50건 (91%)
보유 15분+   52건            51건 (98%)
```

**누락이 무작위가 아니다.** 짧은 거래는 익절이 몰린 구간이라(TP 보유 중앙 3.9분 vs
손절 7.8분) TP_LIMIT 기록률 55% vs STOP 76%로 **승자만 체계적으로 빠졌다.**
그 데이터로 승자 MAE를 재면 오래 끈 승자만 남아 실제보다 나쁘게 나오고,
손절폭 판단이 바로 그 값에 기대고 있었다. `MFE < 실현 ROE` 모순이 58건.

**수정**: 청산 시점에 1분봉 고가/저가로 되짚어 계산한다(`rebuild_excursion` /
`excursion_for`, 원장 기록 두 경로 모두). WS 캐시가 1분봉 200개를 들고 있어
**REST 호출이 늘지 않는다**. 봉 고저를 쓰므로 1분 폴링보다 오히려 정확하다.

## 그 수정이 **조용히 실패**하고 있었다 (11:00~11:33)

배포 후 원장을 보니 `excursion_src`가 전부 `none`/`poll`이고 `bars`가 0건이었다.

**원인**: `astype("int64")`의 단위는 dtype에 따라 다르다. `get_klines`는
`datetime64[**ms**]`를 주는데 `// 10**9`로 나눠 초로 바꾸려 했다. 결과가
`1787` 같은 쓰레기가 되어 **봉 마스크가 항상 0개**였고, 함수는 예외 없이
`(0.0, 0.0, "none")`을 돌려주며 조용히 실패했다.

```python
# 틀림 — ms 를 1e9 로 나눔
ot = pd.to_datetime(df["open_time"]).astype("int64") // 10 ** 9
# 맞음 — 단위에 의존하지 않게 초로 먼저 내린다
ot = pd.to_datetime(df["open_time"]).astype("datetime64[s]").astype("int64")
```

**교훈 두 가지**

1. **`excursion_src` 같은 출처 필드를 넣어둔 덕에 발견했다.** 값만 봤으면 0이
   "안 움직였다"인지 "못 쟀다"인지 구분이 안 된다. 계측을 고칠 때는 **출처도 함께
   남길 것.**
2. 조사 중에 `get_klines`가 02:29 봉을 준다고 착각해 "9시간 낡은 데이터"로
   오진할 뻔했다. **라벨이 UTC였을 뿐 데이터는 최신이었다**(REST 종가와 동일).
   시각 비교 전에 타임존/단위를 먼저 확인할 것.

수정 후 실데이터 검증: 최근 10분 = 마스크 10봉, 고저 정상.
배포 직후 첫 청산(11:35 UNIUSDT)에서 `excursion_src=bars`, MFE +0.11 / MAE −5.62 —
MAE가 실현 ROE(−4.95%)보다 극단인 것이 물리적으로 맞다(체결 전 더 밀렸다가 잡힘).

## SL이 짧았던 원인은 규칙 **셋이 겹친 것**

사용자 지적("SL 구간이 너무 짧아") 당시 손절폭이 **전부 정확히 ROE 4.00%**였다.

```
--stop-rr-match 1.0   A안. 손절폭을 익절폭(4.3) 이하로 누름
--new-max-stop-roe 4  그 위에서 다시 4 로 자름
--cm-tp-max-roe 4.3   익절폭이 곧 손절폭 상한이 됨
```

EMA25 기준 손절선이 어디에 있든 0.8%로 수렴했다. 겹침(앞의 둘)만 해제하니
심볼별로 다시 갈라졌다: TAOUSDT ROE 9.7% / MAGMAUSDT 3.7% / BTRUSDT 16.3%.

**단일 규칙만 보고 판단하지 말 것.** 상한이 여러 겹이면 가장 조인 것 하나가
나머지를 전부 무의미하게 만든다.

---

# 5. 기각

## 심볼 확대 (86~150위) — 기각

봉 종가 기준으로는 2군이 더 좋았다(15분 +0.1004% vs 1군 +0.0866%). 그런데
**스프레드를 재보니 뒤집힌다.**

```
             심볼수  스프레드 중앙   p90     24h 거래대금 중앙
1군(1~85)      85    0.0179%   0.0627%   55.1M
2군(86~150)    65    0.0386%   0.0648%   11.9M
```

왕복 스프레드 1군 0.0359% vs 2군 0.0772%. 종가 엣지 우위(+0.0138%p)를 스프레드
열위(−0.0413%p)가 압도한다. **거래량 하위 심볼은 종가 기준 백테스트가 낙관 편향된다.**

## 메인 봇 뼈대(볼밴+EMA+RSI) 대안 — 측정 실패, 기각 아님

표본이 18건/121건밖에 안 나왔다. CLAUDE.md의 "+0.446% 승률 52.2%"는 재현되지
않았지만, 내가 재현한 조건(2σ 터치, EMA25 기울기, RSI 35/65)이 메인 봇 실제 구현과
다를 가능성이 크다. **기각된 게 아니라 측정되지 않았다.**

## 4시간 필터 "거래수 −40%"라는 내 판단 — 철회

1신호=1거래로 잘못 계산했다. 실제로는 신호 공급이 소화량의 60배다.

## CM 기반 손절선 — 기각 (논리는 맞으나 기여 0)

**구상**: CM 에서 LONG 무효화 조건은 `close < out1` 또는 `ma_up -> ma_down` 둘뿐이다.
익절선이 이미 "HullMA20 방향 전환 이후 스윙 극값"이므로, 손절선은 **같은 레그의
반대쪽 극값**이 대칭이다. 그러면 익절·손절이 같은 지표·같은 구간에서 나와
계획 손익비가 CM 구조 자체로 결정된다. 지금은 익절만 CM 이고 손절은 EMA25 라
둘이 무관하다.

**테스트**(3일 x 85심볼, 4h필터 ON, 1분봉으로 TP/SL 선착 판정, 같은 봉이면 SL 우선):

```
손절 후보               순익/건      t      TP체결   SL    손절폭ROE
현행 EMA25x1.65      -0.0405%  -6.61    65%    29%    2.71%
CM-A 레그 극값        -0.0316%  -5.73    66%    26%    3.29%
CM-B 레그극값-0.2%    +0.0113%  +1.97    78%    12%    4.30%
[대조] 고정 ROE 3.0   +0.0101%  +2.17    76%    22%    3.00%
[대조] 고정 ROE 4.3   +0.0115%  +2.16    79%    16%    4.30%   <- CM-B 와 동일
[대조] 고정 ROE 6.0   +0.0186%  +3.19    81%     9%    6.00%
[대조] 고정 ROE 8.0   +0.0258%  +4.27    81%     5%    8.00%
```

**대조군이 결론을 냈다.** CM-B(+0.0113%)와 **같은 폭의 고정 손절**(+0.0115%)이
완전히 같다. CM 구조의 기여는 0 이다. CM-B 가 좋아 보인 것은 "CM 이라서"가 아니라
**그냥 손절이 넓어서**였고, 더 넓히면 더 좋아진다.

-> **구조 정합성 자체는 성과를 만들지 않는다.** 오늘 A안(`--stop-rr-match`)을 뺀 것과
같은 결론이다. 정합성은 이해를 돕지만 채택 근거가 못 된다.

## 대안으로 남겨둔 것 — `--stop-widen-pct` 확대

위 표를 세로로 읽으면 **손절이 넓을수록 단조증가**한다(고정 3.0 -> 8.0 에서
+0.0101 -> +0.0258, t +2.17 -> +4.27). SL 체결률이 22% -> 5% 로 떨어지고 그만큼
익절이 살아난다. **현행(EMA25x1.65)이 후보 중 최악**이다 — 손절폭 2.71% 로 가장
좁고 SL 체결률 29% 로 가장 높다. 사용자의 "SL 구간이 너무 짧아"가 실측으로 맞았다.

**그럼에도 지금 채택하지 않는 이유**: 이 우위는 **최대보유 60분 안에서만** 성립한다.
손절폭 8% 면 시간만료가 3% -> 14% 로 늘고, 만료는 시장가라 수수료가 비싸다.
60분을 넘겨 더 밀리는 거래는 이 테스트에 안 잡힌다 — **어젯밤 -18% 손절이 정확히
그 경우다.** 하네스가 못 보는 꼬리가 실제 손실의 대부분을 만들었다.

-> **재검토 조건**: 현재 설정(`--new-max-stop-roe 0`, 상한 없음)으로 실거래 표본이
쌓였을 때, 손절 ROE 분포의 꼬리(-12% 초과)가 5% 미만이면 `--stop-widen-pct` 를
0.65 -> 1.0 으로 올려 재측정한다. 꼬리가 그보다 두꺼우면 올리지 않는다.

## 하네스 한계 (이 표를 인용할 때 반드시 함께 볼 것)

검증 중 두 번 고쳤다. 둘 다 결과를 통째로 뒤집는 오류였다:
1. **눌림 대기 미모델링** — 신호 다음 봉 시가에 진입시켰더니 진입가가 스윙 고점에
   붙어 계획 RR 이 0.14 로 나왔다(라이브 0.67). EMA5 터치 대기를 넣어 고쳤다.
2. **CM 목표 계산 시점** — 신호 시점 값으로 고정했더니 익절폭이 0.99% 로 나왔다
   (라이브 4.00%). 봇은 **주문을 내는 사이클에서** ind 를 다시 계산한다. 체결 봉
   기준으로 다시 재도록 고쳤다.

남은 차이: 하네스 TP 체결률 78% vs 라이브 61%, 익절폭 1.10% vs 4.00%.
**원인은 규명됐다** — 라이브는 CM 익절선이 무효일 때 손익비 폴백(rr=2.0)을 쓰는데
그게 손절선에서 역산되므로 거의 항상 상한에 붙는다(로그 `CM 익절선 무효 - 손익비
폴백`). 하네스는 그 건들을 제외했다. **즉 이 표는 "CM 목표가 유효한 거래"에 대한
결과이고, 라이브의 상당 부분인 폴백 거래는 안 들어가 있다.**

## 볼밴 익절(`tp_bb`) 제거 — 경로 통일 (12:22 배포)

**발견 경위**: 하네스의 볼밴 청산 비중이 23%인데 라이브는 1.5% 로 15배 어긋났다.
41봉 창(라이브가 실제로 보는 창)에 맞춰도 변하지 않아 코드를 뒤졌더니:

```
tp_bb 를 설정하는 곳          4곳
  재시작 채택 / 정상 체결 / 추가차수 / 추가차수 갱신    -> 설정 O
안전망(reconcile_live_positions)                    -> 설정 X (기본 0.0)
```

**안전망은 포지션 유입의 93% 다.** 즉 볼밴 익절은 설계로 끈 게 아니라 **한 경로에서
빠뜨려서** 사실상 꺼져 있었다. 라이브 1.5% 는 나머지 7% 경로에서 나온 것이다.

-> **같은 신호로 진입해도 어느 경로로 들어왔느냐에 따라 청산 규칙이 달랐다.**
오늘 사고 여섯 건이 전부 "한 경로에만 넣고 다른 경로에 빠뜨림"이었는데, 이것이
일곱 번째다.

**실거래 A/B 시도 -> 실패**: 원장을 경로별로 갈라 BB 켜진 그룹/꺼진 그룹을 비교하려
했으나 정상체결 경로가 **3건**뿐이라 판정 불가였다(안전망 223건). 로그상 정상체결
진입 152건 중 대부분은 안전망이 먼저 채간 건이다.

**채택: 빼는 쪽으로 통일(안 2).**
- 원칙 0 지킴 — 익절이 CM 지정가 TP 하나로 통일된다. 볼밴은 e2 잔재다.
- 원칙 1 무관 — 청산선이라 진입 후보 불변.
- 원칙 2 중립 — **실동작 변화가 없다**(이미 93% 경로에서 꺼져 있었다).
- 반대안(안전망에도 tp_bb 추가)은 **기각**. 거의 안 쓰이는 원칙 0 위배 규칙을
  93% 경로로 확대하는 것이고, 근거가 검증 안 된 하네스뿐이다.

제거 위치: Pos 생성 4곳 + 폴링 청산의 `BB` 분기. `tp_rr`(손익비 폴백)은 **남긴다** —
CM 익절선 무효 시 실제로 쓰인다.

**남겨둔 것 — `익절선통과` 진입 게이트(다음 후보)**

```python
tp_bb0 = ... fee_aware_bb_price(entry, ind["bb_u"] ...)   # 볼린저 상/하단
if tp_bb0 and (entry >= tp_bb0):   skips["익절선통과"] += 1; continue
```

CM 과 무관한 볼밴 익절선으로 **진입 자체를 버린다** — 원칙 0·1 동시 위배다.
다만 **하네스에서 차단 건수 0**이고("게이트만 제거"가 현행과 소수점까지 동일),
라이브에서도 10분당 0~18건으로 미미하다. 봇이 EMA5 까지 되밀린 자리에서 진입하므로
진입가가 볼밴 상단을 넘는 일이 구조적으로 거의 없다.
-> **실해가 거의 없으나 위배는 맞다.** 동작을 바꾸는 변경이라 이번 배포에 섞지
않았다(실동작 변화 0 을 유지해야 다음 판정이 깨끗하다). 다음 배포 후보.

## 전환봉 필터 (`--cm-flip-max-bars 2`) — 16:47 배포, 오늘 최대 발견

**계기**: 사용자가 "MOVRUSDT 16:22 -6.63% 빅숏을 놓쳤다. CM 으로 못 잡는 거냐"고 물었다.
확인해보니 **CM 은 잡았다** — 16:18 봉에서 SHORT 가 켜졌고 직후 두 봉에서 -10% 가 났다.
막은 것은 4시간 EMA200 필터였다(가격이 EMA200 위 = 상승국면 = SHORT 차단).
그 예외를 검토하다가 전혀 다른 것이 나왔다.

**측정**(3일 x 85심볼, 보유 15분 고정, 수수료 차감 후 순익). 4h 정합 신호를
"HullMA 방향 전환 후 몇 봉이 지났는가"로 가르면:

```
그룹              건수      총수익률      t      순익(최악)   순익(최선)   승률
정합/전환후0봉    4,872   +0.1164%  +16.0    +0.0302%    +0.0612%  58.3%
정합/전환후1봉    4,329   +0.1332%  +17.5    +0.0470%    +0.0780%  59.3%
정합/전환후2봉    3,917   +0.1442%  +18.5    +0.0580%    +0.0890%  61.8%
정합/지속        23,808   +0.0626%  +17.9    -0.0236%    +0.0074%  51.9%   <- 64%가 여기
역행/전환후0~2봉  13,257   -0.004~+0.021%    전부 마이너스 (수수료 못 넘음)
역행/지속        19,269   -0.0302%   -8.5    -0.1164%
```

**"정합 전체 +0.0004%" 는 좋은 1/3 과 나쁜 2/3 의 평균이었다.** 전환 직후 3개 그룹은
최악 가정에서도 +0.0302~+0.0580% 로 확실한 플러스이고, 전체의 64% 를 차지하는
지속봉은 최악 가정에서 마이너스다.

**역행 예외(당초 가설)는 기각.** 전환 직후여도 수수료를 못 넘는다.
MOVRUSDT 같은 거래는 이 방향으로 못 잡는다 — 4시간 필터의 구조적 비용으로 남는다.

## 원칙 1 검토 — 거래수가 줄어드는가 (가장 중요했던 질문)

어제 이 필터를 "거래수 -40%"로 잘못 기각한 전례가 있어 공급 측을 실측했다.

```
조건                    신호     눌림체결   체결률   실효공급/h   봇 18/h 대비
현행 (전환필터 없음)   37,151   12,369   33.3%      172        9.5배
전환 후 2봉 이내       13,235    5,326   40.2%       74        4.1배
```

**체결률이 오히려 오른다(33.3 -> 40.2%).** "전환 직후는 가격이 빨리 움직여 눌림이
안 온다"는 내 우려는 틀렸다 — 방향이 막 바뀐 자리는 되돌림이 잘 온다.

그리고 **진입 속도를 정하는 것은 공급이 아니라 슬롯이다**: 같은방향 상한(3)에 시간의
73% 를 붙어 있고 동시보유는 평균 2.9/10 이다. 3분봉당 후보가 26 -> 8 개로 줄어도
봇이 3분에 넣는 건 0.9건이라 병목이 옮겨오지 않는다.

**한계**: 4.1배는 9.5배보다 얇다. 저유동성 시간대는 3분봉당 후보 p10 이 2개라
여유가 크지 않다. 그 구간에서는 거래수가 줄 수 있다.

## 결합 검토 — 되돌림 청산을 **꺼야 한다**

전환필터를 넣으면 되돌림 규칙의 결론이 뒤집힌다(하네스, 손절 고정 8.0):

```
구성                                건수     순익/건      t     TP   SL   GB   만료
현행 (전환필터X / arm1.0 frac0.4)  19,056  +0.0120%  +3.11  32%   4%  43%  21%
전환필터X / arm1.5 frac0.5         19,056  +0.0204%  +4.74  38%   4%  28%  30%
전환2봉 / arm1.0 frac0.4            4,859  +0.0377%  +4.78  29%   4%  46%  21%
전환2봉 / arm1.5 frac0.5            4,859  +0.0674%  +7.69  36%   4%  30%  30%
전환2봉 / 되돌림 없음                 4,859  +0.0889%  +9.07  45%   5%   0%  51%  <- 최고
```

**전환 직후 거래는 계속 밀고 나가는 성질**이라 되돌림 청산이 그 승자를 중간에 끊는다
(TP 45% -> 29%). 되돌림은 "애매한 거래"를 겨냥한 규칙인데 **전환필터가 이미 애매한
거래를 걸러내므로 같은 문제를 두 번 푸는 것**이 된다.

-> `--giveback-arm-roe` 를 1.0 에서 **0(끔)** 으로. 오늘 낮에 넣은 규칙을 반나절 만에
끄는 셈이지만, 근거가 바뀌었으면 결론도 바꾸는 게 맞다. 코드는 남겨둔다.

**앞서 원장 324건 재생으로 낸 "arm 1.0 -> 1.5" 권고도 이걸로 대체된다.**
그 재생은 전환필터가 없는 모집단에서 잰 것이었다.

## 배포 상태 (16:47)

```
라이브   --cm-flip-max-bars 2   --giveback-arm-roe 0   --stop-fixed-roe 8.0
그림자   --cm-flip-max-bars -1  (그 외 동일)  <- 전환필터 A/B
```

`flip_age()` 를 새로 만들었다. 판정 규칙은 CM 원본과 같다(`out1 >= out1[2]`).
라이브 데이터로 검증: BTC 전환후 2봉 / ETH 5봉 / MOVR 7봉 / SOL 1봉, 반대방향
조회 시 None 반환 정상.

**남은 위험**: 하네스에서 이 조합의 **만료가 51%** 다. 절반이 60분 만료로 끝나는데
하네스가 60분에 강제 청산하는 가정 때문에 좋아 보일 수 있다 — 오늘 "무손절이 최고"로
나왔던 함정과 같은 구조다. 실거래에서 그 51% 가 어디로 갈지는 모른다.

---

# 5-2. 저녁~심야 (19:00~02:00) — 계측 신뢰 복구와 기각 4건

## 배포 순서 (전부 매매 판정이 아니라 **측정 정확도** 수정)

```
19:42  손익 0 기록 버그 + last_trade_id 버그
20:00  HTF 캐시 스로틀 0.35->0.15초, 청산 라벨을 체결데이터로 판정
20:35  사용자 수동매매 중단 -> **여기부터가 봇 단독 성과**
```

## 버그 A — 손익이 0 으로 기록되던 경쟁 (P0)

청산 직후 수 초간 거래소가 체결을 반환하지 않는다. 그때
`realized_fill_snapshot([])` 이 **정상처럼 보이는 dict**(entry=exit 폴백, roe 0)를
돌려주고 comm=rz=0 이 되어 **손익이 0 으로 기록**된다. 실패 플래그도 안 켜진다.

```
19:26:28  ICPUSDT 스탑-리밋 체결 (실현 -4.018)
19:26:33  봇 조회 -> 빈 배열 -> 원장 0.0000
같은 호출 재현 -> 4건 정상, rz -4.018, ROE -8.34%
```

오늘만 4건(EUL 10:27 / VELVET 10:27 / BLESS 16:30 / ICP 19:26).
**원장이 실제보다 좋게 나와 그날 판정이 낙관 편향된다.**

수정: ① 1.5초 후 1회 재조회 ② 그래도 비면 **실패로 취급**(추정 경로 +
`roe_estimated=True`). 배포 후 원장-거래소 차이가 -9.76 -> -0.30 으로 수렴.

## 버그 B — `last_trade_id` 가 최초 체결을 반환

`futures_account_trades(limit=1)` 은 바이낸스가 **오래된 순**으로 주므로 최신이
아니라 **최초**다(실측 ICPUSDT: 8건 중 가장 오래된 id 반환). 이 값이
`since_trade_id` 로 쓰여 "이전 포지션 체결 혼입"을 막는 방어였는데 **아무것도
거르지 못하고 있었다.** `limit=500` 후 `max(id)` 로 수정. 소급 보정은 불가능하다.

## 버그 C — 청산 라벨 오분류

`pos.tp_order_id` 의 **주문 상태를 다시 물어서** 판정했는데, 청산 직후라 아직
FILLED 로 안 보이면 손절로 분류됐다. 실사고 AKEUSDT 19:55: 지정가 TP 체결
(+0.0305)인데 STOP_EXCHANGE 로 기록. **손절률이 부풀어 --stop-fixed-roe /
--cm-tp-max-roe 판정이 왜곡된다.**
-> **이미 확보한 체결목록(tr)의 orderId 로 판정**한다. 추가 호출 0, 경쟁 없음.
   (`tr` 이 예외 시 미정의가 되는 문제도 `tr = []` 초기화로 함께 막았다)

## 버그 D — 내가 만든 타입 버그로 봇이 25분간 멈춤

```python
pos.tp_limit_price = ex.format_price(sm, mk)   # <- 문자열을 반환한다
```
`sync_tp_limit` 의 `tp_limit_price <= 0` 에서 `str <= int` TypeError.
그 예외를 메인 루프 except 가 잡아 **사이클 전체가 중단**됐다. 15:00~15:16
신규 진입 0, 스킵 카운터가 두 스냅샷에서 완전히 동일했던 것이 증거.
`float(ex.round_price(...))` 로 수정.

**교훈**: 이 버그는 **되돌림이 처음 발동할 때만** 터진다. 배포 직후 몇 분 확인으로는
안 잡힌다(14:45 배포 -> 14:54 첫 발생). 그리고 `[주기오류]` 는 로그에만 남고
워치독은 beat 가 살아 있어 발동하지 않았다 — **25분간 아무 경보가 없었고
사용자가 "거래가 줄었다"고 알려준 것이 유일한 감지 경로였다.**

## HTF 캐시 스로틀 0.35 -> 0.15초

`상위추세미확정` 이 스킵의 28% 로 남아 있었다(12시간 전 33% 에서 안 줄었다).
85심볼 한 바퀴 30초 -> 13초. **판정식·TTL·스캔 루프는 그대로 — 캐시 채우는 속도만.**
배포 후 `상위추세미확정` **34건 -> 0건**, 거래수 5.3 -> 14.2건/h 로 회복.

---

# 5-3. 되돌림 청산 — **네 번 검증하고 기각**

낮에 만들어 켰다가(6건 발동, 전부 이익 합계 +1.20) 16:47 전환필터 배포 때 껐다.
껐던 근거는 하네스였는데, 그 하네스는 이후 라이브 재현 실패가 확인됐다.
그래서 **근거를 실거래로 바꿔 네 번 다시 쟀고, 네 번 다 같은 결론이 나왔다.**

```
① 하네스(전환필터+익절4.3)   되돌림 켜면 +0.0889 -> +0.0674   나쁨
② 실거래 원장 36건 재생       기준 +0.0868 -> arm1.0 -0.0415   나쁨
③ 3일 방향별(SHORT 전용)      SHORT +0.1287 -> +0.0813         나쁨
④ 익절 8.0 조합(원장 55건)    +0.2877 -> arm4.0 +0.1517        나쁨
```

**핵심 이유**: 되돌림이 잡는 것의 대부분이 손절 갈 거래가 아니라 **익절 갈 거래**다.
④에서 SL 은 36% -> 27% 로 9%p 만 줄었는데 **TP 는 55% -> 24% 로 반토막**이다.
"TP 근처까지 갔다가 되돌아 손절"은 실제로 드물고, 대부분은 아직 갈 여지가 있는데
중간에 끊는 것이다.

**SHORT 전용 안도 기각**: SHORT 에서도 나쁘고(+0.1287 -> +0.0813), 방향별 비대칭
규칙은 국면에 과적합된다. 3일 데이터에서는 SHORT 가 LONG 보다 5.5배 좋았고
(+0.1287% vs +0.0232%), 어젯밤 하락장에서는 SHORT +2.41 vs LONG -40.51 로
정반대였다. **방향별 우열은 국면을 따라 뒤집힌다.**

코드는 남겨둔다(`--giveback-arm-roe 0`). 조합이 바뀌면 재검토 가치가 있다.

## 익절선 추적(매분 CM 재계산, 바깥으로만) — 기각

```
익절상한 4.3 / 추적 OFF  3,806건  +0.0890%  TP 90%
익절상한 4.3 / 추적 ON   3,806건  +0.0904%  TP 90%
익절상한 8.0 / 추적 OFF  3,806건  +0.0897%  TP 89%
익절상한 8.0 / 추적 ON   3,806건  +0.0920%  TP 89%
```

차이가 +0.0014~0.0023%p 로 잡음이다. **TP 체결률이 89~90% 라 목표가 자랄 시간이
없다** — 추적이 발동할 기회 자체가 드물다. 표본을 늘려도 결론이 안 바뀐다.

**API 밴은 문제가 아니었다**(지표 재계산은 WS 캐시라 REST 0, 주문 갱신 40건/분은
한도 1,200/분의 3%). 진짜 위험은 ① 취소~등록 사이 무방어 구간이 매분 생기는 것
② `-2021 Order would immediately trigger` 경로가 자주 열리는 것이다.
**손절선 추적은 특히 위험하다** — 가격이 불리하게 가면 지표도 따라가 손절선이
계속 밀리고, 그건 "무손절"에 수렴한다(오늘 이미 기각된 함정).

---

# 5-4. 손익비가 0.42인 이유 — **시장이 아니라 설정이 정한 값**

```
익절 상한  --cm-tp-max-roe   4.3% ROE  (가격 0.86%)
손절 고정  --stop-fixed-roe  8.0% ROE  (가격 1.60%)
설계상 비율 = 0.54   /   실현 0.42
```

실현이 설계보다 낮은 이유: 익절 일부가 상한 미만 체결 / 손절 슬리피지(7.81~8.26) /
수수료 비대칭(익절 maker 0.0276% vs 손절 taker 0.0586%).

**승률 73% 는 대단해 보이지만 본전 승률이 70.3% 다.** 여유 2.7%p —
승률 3%p 만 떨어져도 마이너스다(52건에서 3%p = 거래 1.5건).

```
승률 70% -> 건당 -0.015   승률 73% -> +0.113   승률 75% -> +0.199   승률 80% -> +0.412
```

## 개선 방향 — 익절을 넓히는 쪽 (원장 55건 재생)

```
익절ROE 손절ROE 손익비    건당%      t    승률   TP   SL
   4.3    8.0   0.54  +0.1530  +1.02  73%  73%  27%   <- 현재
   8.0    6.0   1.33  +0.2033  +1.10  55%  47%  45%
   8.0    8.0   1.00  +0.2870  +1.41  64%  55%  36%   <- 최고
   8.0   12.0   0.67  +0.3086  +1.32  69%  60%  25%
   4.3    4.0   1.07  +0.0330  +0.29  53%  53%  47%   <- 손절 조이면 최악
   6.0    4.0   1.50  -0.0232  -0.17  42%  40%  58%   <- 손익비 최고인데 마이너스
```

**손익비는 목표가 아니라 결과 지표다.** 직접 겨냥해 손절을 조이면 순익이 무너진다.

**보류 이유**: ① 55건 t=+1.41 로 판정 불가 ② 익절 8.0 이면 TP 체결률 55% 로
CLAUDE.md 원복 조건(60%)을 밑돈다 ③ 만료가 9% 생기는데 라이브는
`--max-hold-sec 0` 이라 무한정 들고 간다 ④ 하네스는 익절 4.3 과 8.0 이 같다고
말해(+0.0890 vs +0.0897) **도구 간 불일치**가 있다.

-> 표본 200건 쌓으면 익절상한(4.3/6.0/8.0) x 시간만료(없음/30/60/120분) 격자를
   실거래로 다시 잰다.

---

# 5-5. 20:35 이후 성적 (봇 단독, 수동매매 없음)

```
5.24시간  52건  9.9건/h  승률 73%  순 +6.06  건당 +0.117  명목대비 +0.0720%  t=+0.54
지갑 저점 472.29(21:59) -> 479.84  (+7.55)
원장 vs 거래소 차이 -0.83 (미결제 수수료) — 기록 정확 ✅
```

시간당 표(20:00~):
```
시각     전체 승률 손익    LONG 승률 손익    SHORT 승률 손익
20시      8  75%  +1.63     7  71% -0.36      1 100% +1.99
21시     15  60%  -9.43    11  73% +0.80      4  25% -10.23
22시     11  73%  +5.79    10  70% +3.89      1 100% +1.90
23시     10  90%  +4.54     9  89% +2.52      1 100% +2.02
00시      8  50%  -5.75     5  60% +0.30      3  33%  -6.05
01시      3 100%  +4.09     1 100% +1.88      2 100% +2.21
합계     55  71%  +0.87    43  74% +9.03     12  58%  -8.16
```

**마이너스 시간대는 전부 SHORT 가 만들었다**(21시·00시). LONG 은 여섯 시간 중
다섯 시간 플러스. 다만 SHORT 12건이고, 국면이 상승/횡보라 불리한 구간이다.

## t=0.54 의 의미 (판정 기준)

```
n=52  건당 평균 m=+0.0720%  건당 표준편차 sd=0.9548%  se=sd/√n=0.1324%
t = 0.0720/0.1324 = 0.54
95% 신뢰구간: -0.1875% ~ +0.3315%   -> 0 을 포함, 마이너스를 배제 못 함
명목 226 기준 시간당: -4.20 ~ +7.42 USDT
```

**개별 거래의 흔들림(sd 0.95%)이 평균(0.072%)의 13배다.** 그래서 표본이 많이 필요하다:

```
t=1.5 -> 396건 (40시간)   t=2.0 -> 703건 (71시간)   t=2.5 -> 1,099건 (111시간)
```

엣지가 작을수록 필요 표본이 **제곱으로** 늘어난다. 건당 +0.2% 였다면 90건이면 됐다.

## 시간당 +5 USDT 는 불가능하다 (잔고 476 기준)

```
건당 순익 +0.087~+0.201 USDT (명목 226)
+5/h 에 필요한 거래수: 25~57건/h   (현재 9.9건/h)
크기로 달성하려면: 명목 1,295/건 -> 5배 레버리지면 **잔고 2,727 USDT**
```

거래수를 하한 14건까지 회복해도 **시간당 +1.2** 수준(하루 +29, 잔고 대비 6%).
레버리지를 올리면 ROE 기준 손절폭이 가격 기준으로 좁아져 **같은 전략이 아니게 된다.**

---

# 6. 미해결 / 다음 확인

## 원칙 0 문구 개정 — **조건부 예약**

CLAUDE.md 원칙 0은 "**15분** HullMA20"인데 봇은 `--signal-tf-min 3`(3분봉)이다.
**문구 위배이나 측정상 우월**하다(3분 +0.0341% vs 15분 −0.0640%, 15분 t=−14.0).

**사용자 결정(8/27)**: 3분봉을 유지하고, **수익으로 전환되면 원칙 0을 15분 → 3분
으로 개정한다.** 아직 개정하지 않았다 — 수익 전환이 조건이다.

지표 구현 자체는 CM_Ultimate_MA_MTF_V2 원본과 일치함을 확인했다:
`out1 = MA(src,len,atype)` / `ma_up = out1 >= out1[smoothe]`(smoothe=2) /
`cr_up = open < MA and close > MA` / HullMA = `WMA(2*WMA(n/2) − WMA(n), sqrt(n))`.

## 지정가 손절 (`--stop-limit`) — 검증 안 됨

**근거 없이 넣은 유일한 변경이다.** 손절가는 정의상 현재가의 불리한 쪽이라 트리거
순간 호가에 상대가 있으면 그대로 taker로 먹힌다. 절감 기대는 "손절 45% × 0.03%p"
= 0.0135%p가 **상한**이고 실제로는 일부만 얻는다. 대신 미체결 시 무방어 위험이
새로 생겨 `guard_stop_breach`(5초 주기, 마크가격 전 심볼 1회 호출)를 함께 넣었다.

**판정 기준: `지정가 손절 미체결 ... 시장가 전환` 로그가 손절의 20%를 넘으면
절감보다 손해다.** 넘으면 `--stop-limit`을 뺀다(사용자 지시 없이 임의 원복 금지).

## 남은 근본 문제 — 비용

4시간 필터로 순익이 겨우 +0.0004%(최악 가정)다. 명목 160 기준 **건당 +0.0006 USDT**.
슬리피지가 조금만 있어도 다시 마이너스다.

**엣지를 더 올릴 파라미터는 없다**(스윕에서 현재값이 최적). 남은 레버는 비용뿐:

1. **BNB 수수료 할인 10%** — 왕복 0.0862% → 0.0776%. 사용자가 "잔고가 커지면"으로
   미뤄둔 건이지만, **비용이 엣지를 잡아먹는 구조라 순서가 반대일 수 있다.**
2. 지정가 손절(위, 미검증)
3. VIP 등급 — 거래량 기준이라 현재 잔고로는 해당 없음

## 그 밖

- **테스트 스위트가 깨져 있다.** `tests/test_scalp_bot_e3_portfolio.py`가
  `scripts/scalp_bot_e3_portfolio.py` → `expand_range_cycle`를 import하는데 그
  함수가 없다. 수집 단계 ImportError → **pytest가 exit 2로 세션 전체를 중단**한다.
  그 파일을 빼고 돌리면 1,196건 중 76건 실패인데, 76건 중 70건이
  `test_scalp_bot_e3.py`의 **옛 그리드판 e3** 테스트다(현재 코드에 없는 기능).
  낡은 테스트 정리가 필요하다.
- `상위추세미확정`이 11:09 기준 37건(스킵의 33%). 4시간봉 캐시가 차면 0으로
  수렴해야 정상. 안 줄면 백그라운드 워커 스로틀(심볼당 0.35초) 조정.

## 무배포 관측 창 14:45~18:00 (사용자 합의)

**오늘 재시작이 77회다.** 그 자체가 검증을 방해했다 — 되돌림 규칙은 MFE 누적에
의존하는데 `save_state` 가 `max_favorable_roe`/`max_adverse_roe` 를 저장하지 않아
**재시작마다 0 으로 초기화**된다. 규칙을 배포·검증하려고 재시작을 반복한 것이
그 규칙을 작동 못 하게 만들었다(실측: 5초 틱 도입 후에도 발동 0건,
GPSUSDT MFE 3.51% 는 재시작 **전에** 찍힌 값이라 새 프로세스는 못 봤다).

이 창 동안 **어떤 배포도 하지 않는다.** 브리핑은 16:00 / 18:00 두 번(세션 cron).
기준 구간은 14:45(5초 MFE 틱 도입) 이후이며 그 이전과 섞지 않는다.

**대기 중인 변경 — `save_state` 에 MFE/MAE 추가**: 재시작이 MFE 를 지우는 문제.
`swept`/`entered_at` 이 같은 이유로 이미 추가된 전례가 있다.
16:00 브리핑에서 사용자에게 배포 여부를 확인하고, 승인되면 **18:00 에** 배포한다
(16:00 에 배포하면 관측 창이 깨진다).

## 표본 크기에 대한 오늘의 실패 기록

같은 구간을 두 번 재서 부호가 뒤집힌 사례:

```
13:47~ 구간   9건 시점  순 -0.35  건당 -0.039   "오늘 최고 구간"이라 보고함
              15건 시점 순 -2.15  건당 -0.143   t=-0.36 (0과 구별 안 됨)
```

직전 구간도 8건에 -7.88/h 였다. **10건 안팎에서는 부호가 우연으로 뒤집힌다.**
오늘 이 크기로 판정해서 일곱 번 틀렸다. |t|<2 면 "판정 불가"라고 쓰고 넘어갈 것.

---

# 7. 오늘 배운 방법론 (반복 금지 목록)

- **1신호 = 1거래가 아니다.** 필터의 거래수 영향은 신호 감소율이 아니라 **소화율**로
  판단할 것. 실측: 신호 1,097/h, 봇 실행 18/h(1.7%).
- **종가 기준 백테스트는 저유동성 심볼에서 낙관 편향된다.** 스프레드를 반드시 함께
  잴 것(1회 호출로 전 심볼 `futures_orderbook_ticker`).
- **계측에는 출처 필드를 함께 남길 것.** 값 0이 "안 움직였다"인지 "못 쟀다"인지
  구분이 안 되면 조용히 실패한다.
- **원장 필드의 부호를 먼저 확인할 것**(MAE는 음수).
- **짧은 구간(6~30분) 손익으로 설정 변경을 판정하지 말 것.** 오늘 여섯 번 그렇게
  했고 여섯 번 다 틀렸다. 표본이 부족하면 판정하지 말고 관찰로 남긴다.
- **상한이 여러 겹이면 가장 조인 것 하나가 나머지를 무의미하게 만든다.**
- **재시작 자체가 검증을 방해한다.** 상태(MFE, swept, entered_at)가 초기화되는
  규칙은 재시작이 잦으면 영원히 발동하지 않는다. 검증하려면 **가만히 두는 시간**이
  필요하고, 그건 코드 작업보다 우선한다.
- **같은 구간을 두 번 재라.** 9건에서 -0.039, 15건에서 -0.143 로 뒤집혔다.
  한 번 재고 보고하면 그게 곧 오보가 된다.
- **|t|<2 면 "판정 불가"라고 쓰고 넘어갈 것.** 필요 표본은 엣지의 **제곱에 반비례**한다.
  건당 +0.072% 면 t=2 에 703건(71시간)이 필요하다. 10~20건으로 판정하면 안 된다.
- **승률로 판정하지 말 것.** 손절을 넓히면 승률은 자동으로 오르고 본전 승률도 같이
  오른다. 손익비도 목표가 아니라 결과 지표다 — 직접 겨냥해 손절을 조이면
  손익비는 1.5로 좋아지는데 순익은 마이너스가 된다(실측).
- **원장과 거래소를 매번 대조할 것.** 오늘 손익이 0 으로 기록되는 버그로 4건이
  유실됐고, 그 상태로 내린 판정은 전부 낙관 편향이었다.
- **API 밴 걱정과 실제 위험을 혼동하지 말 것.** 지표 재계산은 WS 캐시라 REST 0 이고
  주문 갱신 40건/분도 한도의 3% 다. 진짜 위험은 취소~등록 사이 **무방어 구간**과
  `-2021` 강제청산 경로다.
- **"조건부로만 터지는 버그"는 배포 직후 확인으로 안 잡힌다.** 되돌림 타입 버그는
  규칙이 처음 발동할 때(배포 9분 뒤) 터졌고, 25분간 아무 경보가 없었다.
  `[주기오류]` 는 로그에만 남고 워치독은 beat 가 살아 있어 발동하지 않는다.
