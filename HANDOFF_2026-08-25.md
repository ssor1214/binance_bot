# Binance Futures Bot Handoff - 2026-08-25

## 이번 세션 요약

- 실운영 경로: `C:\Users\lg\Desktop\binance-futures-bot`
- 원칙 0: 볼밴 매매 + EMA5일선 기준 유지
- 거래량 필터는 줄이지 않음. 운영값 `VOLUME_INCREASE_FILTER_ENABLED=true`, `VOLUME_INCREASE_MIN_RATIO=1.02` 유지.
- 지정가 진입 대기시간만 `LIMIT_ENTRY_WAIT_SEC=3.0 -> 5.0`으로 변경 후 재시작.

## 오후 후속 업데이트 (Codex, 2026-08-25 14:40 재시작)

- Claude가 이 문서에 적어둔 오후 세션 업데이트와 운영 원칙/원복 조건을 기준으로 이어서 작업했다.
- 진입 품질 지표(진입 30초/60초 ROE 음수 비율) 개선은 이미 측정된 것으로 보고, 오늘 추가 수정은
  **진입 필터 완화가 아니라 "지정가 미체결 1회 구제 범위 조정"**에 한정했다.
- 기존 코드에는 `LIMIT_ENTRY_AGGRESSIVE_FALLBACK_ENABLED=true`가 이미 있었지만, 실제 발동 게이트가
  사실상 `HIGH_CONFIDENCE_PROBABILITY=0.85`에 묶여 있어 `probability 0.78~0.84` 구간의 통과 후보가
  5초 미체결 후 그대로 버려지고 있었다.
- 그래서 fallback 전용 기준을 분리했다.
  - `LIMIT_ENTRY_FALLBACK_MIN_PROBABILITY=0.78`
  - `LIMIT_ENTRY_FALLBACK_MIN_SCORE=0.72`
  - 기존 `short_reversal_risk`, `negative_ev_symbol` 차단은 그대로 유지
- 의미: passive 지정가 5초 대기는 그대로 두고, **완전 미체결일 때만** 중상급 후보를 반대호가 1회
  재시도 대상으로 넓힌다. 단순 `LIMIT_ENTRY_WAIT_SEC=8~10` 연장보다 슬롯 점유 리스크가 작다.
- 관련 코드/설정:
  - `bot/main.py`: `should_allow_aggressive_limit_fallback()`가 `high_confidence_probability` 대신
    fallback 전용 floor를 읽도록 수정
  - `bot/config.py`, `.env`, `.env.example`: 전용 키 추가
  - `tests/test_limit_entry.py`: 새 게이트 기준 unittest 추가 및 회귀 갱신
- 검증:
  - `.\.venv\Scripts\python.exe -m unittest tests.test_limit_entry -v`
  - 21건 전부 OK

### 재시작/복원 로그

- 2026-08-25 14:05:47 이후 하트비트가 멈춰 있어 인스턴스가 사실상 죽은 상태로 판단했다.
- 2026-08-25 14:40:11 `run_forever.py`를 다시 기동했고, 14:40:13 새 `bot.main` 시작 로그를 확인했다.
- 14:40:19~14:40:20:
  - WS 시장 워커 2개, 계정 스트림 1개, 체결(스파이크) 워커 2개 정상 기동
  - `UAIUSDT` 기존 포지션이 `origin=manual`로 복원됨
  - 포지션 없는 `USUSDT` 고아 트레일링 주문 1건 자동 정리
  - 14:40:30 `UAIUSDT` STOP_MARKET 재등록 확인
- 재시작 직후 하트비트:
  - `heartbeat.txt` 14:40:33
  - `ws_worker_heartbeat_market0/1.txt` 14:40:37~38
  - `ws_worker_heartbeat_user.txt` 14:40:37
  - `ws_trade_worker_heartbeat_trade0/1.txt` 14:40:36~38
- **주의:** `run_forever -> bot.main` 외에 과거부터 중복 supervisor 시작 시도 흔적이 `logs/supervisor.log`
  에 계속 남아 있다. 이번 재시작은 새 인스턴스 1개가 lock을 잡고 올라왔지만, supervisor 중복기동
  원인 자체는 아직 해결하지 않았다.

## 애매한 수익 보호 규칙

기존 `AMBIGUOUS_QUICK_PROFIT_EXIT`에 PROMUSDT 유형의 중간 보유 구간을 추가했다.

- 미무장 상태
- 보유 `180~600초`
- 현재 ROE `0~2.0%`
- 최대 유리 ROE `0.8~2.0%`
- 정점 대비 pullback `0.4% 이상`
- 1분봉 역방향 조건과 기존 수익 하한 조건은 유지

관련 설정:

- `AMBIGUOUS_QUICK_PROFIT_EXIT_MID_HOLD_ENABLED=true`
- `AMBIGUOUS_QUICK_PROFIT_EXIT_MID_MIN_HOLD_SEC=180`
- `AMBIGUOUS_QUICK_PROFIT_EXIT_MID_MAX_HOLD_SEC=600`
- `AMBIGUOUS_QUICK_PROFIT_EXIT_MID_MAX_ROE=2.0`
- `AMBIGUOUS_QUICK_PROFIT_EXIT_MID_MAX_FAVORABLE_ROE=2.0`
- `AMBIGUOUS_QUICK_PROFIT_EXIT_MID_MIN_PULLBACK_ROE=0.4`

테스트: 관련 unittest `25건 OK`, `py_compile` 통과.

## PROMUSDT 복기

사진의 종목은 FROMUSDT가 아니라 `PROMUSDT LONG`이었다.

- 진입 01:36경, 청산 01:41경
- 보유 약 `272초`
- 최대 유리 ROE `+1.37%`
- 최종 손익 약 `-0.10 USDT`, 표시 손익 약 `-1.50%`
- 원장 세부 사유: `EXTERNAL_CLOSE_LOSS / sl_defer_reverted_loss`

최근 12시간에서 같은 조건은 2건, 승률 0%, 합계 `-0.276 USDT`였다. 확장 조건 4건도 승률 0%, `-0.495 USDT`였다.

## 최근 성과와 손실 원인

최근 12시간 봇 거래:

- 73건, 승률 `42.5%`, 순손익 `-0.047 USDT`
- `EARLY_EXIT`: 19건, `-0.63 USDT`
- `UNARMED_MID_HOLD_CUT`: 6건, `-0.16 USDT`
- `AMBIGUOUS_QUICK_PROFIT_EXIT`: 11건, `+0.05 USDT`

최근 5시간:

- 27건, 승률 `44.4%`, 순손익 `+0.204 USDT`
- `EARLY_EXIT`: 7건, `-0.221 USDT`
- 7건 모두 SHORT, 보유 `26~100초`, 최대 유리 ROE `0~0.42%`

## SHORT_EARLY_FAIL 판단

최근 `EARLY_EXIT` 숏은 진입 로그상 모두 `신호=SHORT`였다. 롱 신호를 숏으로 잘못 진입한 방향 반전 버그 증거는 확인되지 않았다.

반복된 패턴:

- 진입 시 `SHORT 되감기 위험(5m_long_lower_wick, 1m_bullish_snapback 등)` 감지
- 위험을 감지했지만 현재 규칙은 비중만 줄이고 진입은 유지
- 이후 최대 유리 ROE가 거의 없고 반대 방향으로 이동
- `SHORT_EARLY_FAIL`이 15~120초 구간에서 손실 청산

운영 판단:

- `SHORT_EARLY_FAIL` 완전 OFF 후 무조건 3~5분 보유는 아직 근거 부족
- 다음 개선 우선순위는 거래량 필터가 아니라 되감기 위험 SHORT 진입 처리
- 거래량 필터는 활성 상태를 유지해야 함
- 후보안: 되감기 위험 SHORT는 진입 보류 또는 최소 60초 보호 후, 회복 가능성이 있는 경우만 3분 관찰

## 재시작/운영 상태

재시작 후 확인:

- WS 시장 워커 2개 정상
- 계정 스트림 정상
- heartbeat 갱신
- 최근 WS 메시지 수천 건, 오류 0건

주의: `run_forever.py -> run_forever.py -> bot.main -> bot.main` 중첩 구조는 여전히 운영 리스크로 남아 있다. 재시작 때 하위 프로세스를 정리해도 상위 supervisor가 재생성하는 현상이 있어, 다음 운영 작업에서 단일 supervisor 트리 정리를 별도 과제로 다룰 것.

## 다음 확인 항목

1. `SHORT 되감기 위험` 진입 보류의 최근 5~12시간 사후 손익
2. `SHORT_EARLY_FAIL`을 60초 이후로 제한했을 때 손실/승률 변화
3. 거래량 필터는 변경하지 않은 상태에서 후보수와 체결수 변화
4. `AMBIGUOUS_QUICK_PROFIT_EXIT` 중간 보유 조건의 표본 확대
5. 손익 판단은 반드시 `trade_ledger.jsonl`의 실거래 원장 기준으로 수행

---

# 오후 세션 (Claude, 12:00~13:30) — 원칙 재정의 + 진입 지각 수정

## 운영 원칙 (확정, `.env` 최상단과 `CLAUDE.md`에 기재)

```
ema5는 3일선이다
원칙 0: 볼밴 매매 + EMA5
원칙 1: 거래는 활발해야 함
원칙 2: 승률·순익을 해치면 안 됨
원칙2 보강 = 애매한 거래는 빠르게 작은 수익으로 익절하고 포지션 종료한다
```

설정 변경 시 세 원칙 각각에 "지킴/해침/무관"을 명시한다. 원칙 1과 2가 충돌하면
`logs/trade_ledger.jsonl` 실거래 원장으로 판정하고, 표본이 구간별 5건 미만이면
채택하지 않고 관찰 대상으로 남긴다. 걸려 있는 원복 조건은 `CLAUDE.md` 참조.

## 가장 중요한 발견: 수수료 빼면 이득이 아니다

최근 7일 봇거래 645건, 청산 수수료는 원장 실측 / 진입은 메이커 0.02% 가정.

```
수수료 전 총손익(gross)   +0.611 USDT   ← 사실상 본전
실제 수수료 합계          -3.410 USDT
수수료 후 실순익          -2.799 USDT
```

건별(net 기준): 승률 `56.7%`, 평균이익 `+2.826%` ROE, 평균손실 `-4.216%` ROE,
**손익비 `0.67`**, 손익분기 승률 `59.9%`, 건당 기대값 `-0.00434 USDT`.

- **TP는 짧지 않다.** 이긴 거래가 고점의 `97.4%`에서 나간다. 익절 타이밍은 문제가 아니다.
- **문제는 손실 크기다.** net ROE `-6%` 이하 77건이 혼자 `-11.2 USDT`로, 전체 이익
  구간(+21.5)의 절반을 지운다. 다음 후보는 `STOP_LOSS_PCT=6.0` 스윕 백테스트.
- LONG/SHORT 둘 다 마이너스라 한쪽만 꺼서 해결되지 않는다.

## 조기컷은 원칙 2를 지키고 있다 (앞선 진단 철회)

`exit_reason`으로 집단을 갈라 비교하면 **선택편향**이다 — 컷은 거래가 안 풀렸기 때문에
발동한다. 청산 이후 실제 가격으로 반사실 검증한 결과(표본 5건, 3분봉 캐시):

```
컷 실현 ROE 평균          -1.553%
안 끊었을 때 30분 후 평균  -8.452%
5건 중 3건이 손절선 터치
```

조기컷이 손실을 5배 줄이고 있다. `UNARMED_MID_HOLD_CUT`의 건당 손실이 가장 큰 건
컷이 나빠서가 아니라 가장 나쁜 거래가 거기 모이기 때문이다. **"고점이 있었으면 끊지
말자"는 안은 폐기했다** — 5건 모두 컷 이후 잠깐 유리한 구간이 있었지만 대부분 손절로 끝났다.
SL(SHORT 4.5%)은 빡빡하지 않다. 실현이 -0.84~-2.96%로 손절선에 닿지도 않는다.

## 진짜 문제: 진입이 구조적으로 늦다

진입 60초 시점 ROE 부호 하나가 최종 성과를 가른다.

| 60초 ROE | 건수 | 승률 | 순손익 |
|---|---|---|---|
| > 0 | 215 (40.7%) | **75.3%** | +4.887 |
| <= 0 | 313 (**59.3%**) | **42.5%** | **-10.956** |

### AAVEUSDT 복기 (12:35~13:00) — 메커니즘

신호를 **못 잡은 게 아니다.** 큰 음봉 구간 내내 SHORT 신호가 잡혔고 확률은 0.876까지 갔다.
필터가 20분간 20번 막았다.

```
12:36~12:44  SHORT 신호 O -> whipsaw 8회 차단
12:45~12:55  SHORT 신호 O -> one_min_noise 9회 + range_position 3회 차단
12:56:27     candidate 통과 -> 12:56:47 진입 -> FIRST_3M_FAIL (-0.206 USDT)
```

원인은 **ATR이 후행지표**라는 것. 급락 직전 ATR이 0.43~0.55%였고, -1.72%/-2.25% 음봉이
나오는 순간에도 ATR은 0.551 -> 0.762%로 하한 `0.787%`를 못 넘었다. ATR이 하한을 넘긴 건
하락이 끝난 뒤다. `one_min_noise`와 `range_position`도 같은 편향 — 움직임 진행 중엔 막고
끝나서 반전될 때 열어준다.

## 오늘 적용한 변경 (전부 원복 조건을 `.env` 주석에 기재)

| 항목 | 변경 | 근거 |
|---|---|---|
| **A안 whipsaw 지각 보정** | `WHIPSAW_IMMEDIATE_VOL_ENABLED=true` | ATR이 하한 미달이어도 최근 2봉 실변동폭이 하한 충족이면 통과. 캐시 재생상 whipsaw 통과 194 -> 478건(+146%). 라이브 13:30 기준 23건 발동 |
| **C안 죽은 상한 되살리기** | 상한 판정을 유효 변동성(max(ATR, 실변동폭))으로 | ATR 기준으론 1,540건 중 **0건** 차단하던 죽은 필터. A안이 연 구멍(실변동폭 8.48배 극단 휩쏘가 상한 통과)을 닫음. 유효 기준으론 0.8%(12건) 차단. 라이브 3건 발동 |
| 볼밴 관여 게이트 | `BB_PARTICIPATION_REQUIRED=true` | 원칙 0 정합. EMA만으로 진입하던 경로 차단. 이벤트만 필수로 하면 신호가 16~18%로 급감해 밴드확장까지 인정(LONG 73.1%/SHORT 64.4% 잔존) |
| EMA5 | `EMA_FAST` 12 -> 5 | 원칙 0의 "ema5는 3일선". 게이트 통과 신호 1,809 -> 1,955(+8.1%, SHORT +27%) |
| 되감기위험 SHORT | `SIZE_MULT` 0.88 -> 0.65, `FLOOR_MULT` 0.94 -> 0.85 | 해당 구간 건당 -0.0079로 순손익 음수. `PRIORITY_PENALTY`는 거래수를 줄이므로 0.025 유지 |
| 스파이크 공격적 체결 | `SPIKE_ENTRY_AGGRESSIVE_FILL=true` 명시 | 게이트가 유실돼 플래그와 무관하게 이미 동작 중이었다. 코드에서 게이트 복원 + 실동작 유지 |
| 최소 증거금 | 10 USDT (5개 키) | 사용자 요청. 하한 10은 이 잔고대 비율사이징(2~9)보다 커서 방어배율이 사실상 무력화된다. 교차점 약 66.7 USDT |
| 슬롯 | 4 -> 6 | 만석이면 60 USDT로 계좌의 97%. 5~6번째는 진입 생략 가능 |
| 거래 목표 | `HOURLY_TRADE_TARGET` 13 -> 16 | 사용자 목표 12~18건/시 |
| whipsaw 기준선 | `MIN_ATR_VS_STOP_RATIO` 0.8 -> 0.7 | 자동정책이 흑자 구간에서 두 번 스스로 선택한 값인데 재시작마다 소실돼 기준선으로 내림 |

## 고친 버그 (전부 회귀 테스트 추가)

1. **먼지 청산 임계가 진입 하한에 묶여 있었다** — `진입하한 x 0.5`라 하한을 10으로 올리자
   임계가 5.0이 되어 **4 USDT짜리 정상 부분체결이 즉시 시장가로 되팔렸다**.
   `DUST_ENTRY_MAX_MARGIN_USDT=1.25` 절대값으로 분리.
2. **완화 원복 오버슈트** — 원복이 "단계수 x 스텝"을 되더해서, 완화가 하한에 막혀 덜
   내려갔을 때 기준선을 넘어 **더 빡빡해졌다**(0.7 -> 3단계 -> 원복 0.8). 기준선 저장 후 복원.
3. **거래 0건 교착** — 완화 조건이 "거래>=3 AND 손익>0"이라 거래가 0이면 영원히 완화가
   안 걸렸다. 거래 0 + 보유 0이면 실현손실도 0이라 원칙 2를 해칠 수 없으므로 예외 허용.
   `RULE1_STARVATION_RELAX_ENABLED=true`, 15분 주기.
4. **스파이크 게이트 유실** — `micro_scalp` lane 추가 때 `cfg.spike_entry_aggressive_fill`
   AND 조건이 떨어져 나갔고, 로그는 `aggressive_fill=False`로 거짓 출력했다.
5. **A안이 whipsaw를 fail-open으로 만들 뻔했다** — 실변동폭 계산이 예외를 던지면 바깥
   `except`가 `return True`(진입 허용)로 삼켜 필터 전체가 무력화된다. 자체 try로 격리.
6. `TINY_BALANCE_SINGLE_SLOT_THRESHOLD`가 `Config`에 없어 **조용히 무시**되고 있었다(배선 추가).
   단 `TEMP_FORCE_MULTI_SLOT_ENABLED=true`인 동안은 여전히 적용되지 않는다.
7. `MTF_AS_BONUS_ONLY` **삭제** — 정의만 있고 코드 어디서도 안 읽는 죽은 설정이었다.

## 새로 배선한 관측 (원칙 2 판정에 직접 쓰인다)

원장 `TradeRecord`에 진입 근거를 추가했다: `entry_score`, `entry_bb_event`,
`entry_width_expanding`, `actual_fill_entry_price`.

- 지금까지 원장에 진입 점수도 볼밴 근거도 없어서 "순수 EMA 진입 vs 볼밴 관여 진입"
  손익 비교가 **불가능**했다. 볼밴 게이트 효과 검증에 직접 쓴다.
- `actual_fill_entry_price`는 654건 전부 null이었다. 원인은 FillTracker가 읽는 status
  파일 덤프 주기가 2초라 체결 직후 조회를 놓치는 것. 즉시 1회 + 다음 점검 주기 backfill
  (진입 후 120초 이내, 방향 일치 확인)로 바꿨다. **진입 경로에서 대기하면 안 된다** —
  슬롯 채우는 루프 안이라 대기가 그대로 다음 후보 진입 지연이 된다.
- **주의: 재시작 시 `reconcile`로 복원된 포지션은 이 필드들이 null이다.** 집계에서 제외할 것.

## 실측으로 확인된 필터 병목 (게이트 통과 신호 1,636건 기준)

```
whipsaw 통과        194건 (11.9%)
하한미달 차단      1,442건 (88.1%)   <- 최대 병목
  └ 실변동폭 충족    284건            <- A안이 살림
상한초과 차단          0건 (0.0%)     <- C안 이전엔 죽은 필터
```

`signal_missing` 상세 분해(최근 1시간 4,271건): EMA 방향 없음 `63.0%` /
**볼밴 게이트 탈락 `33.9%`** / EMA간격 미달 `3.1%`. 완화 사다리 3순위인 `ema_gap`은
실효가 3.1%뿐이라 의미가 없다.

## 다음 확인 항목

1. **A안 원복 조건**: 진입 60초 ROE 음수 비율이 `59%`에서 안 내려가면 `false`로 되돌린다.
2. **B안(대기중)**: `one_min_noise`가 위아래 꼬리를 합쳐 몸통과 비교한다
   (`main.py`의 `passes_one_min_noise_filter`). SHORT에 유리한 윗꼬리까지 벌점을 준다.
   방향별 꼬리로 나누는 수정. A안 결과 확인 후 결정.
3. **`STOP_LOSS_PCT` 6.0 스윕 백테스트** — 손익비 0.67을 1.0으로 올리는 게 핵심.
   손절을 좁히면 whipsaw 문턱(`ATR >= 손절폭 x 비율`)도 같이 내려가 원칙 1도 개선된다.
4. **방향 편향**: 13시경 최근 3시간 봇거래 15건이 전부 SHORT였다.
5. **조기컷 반사실 검증 확대** — 표본 5건은 얇다. 96건 전체로 규칙별 판정.
6. **테스트 5건이 계속 실패 중**(세션 시작 시점부터, 이번 변경과 무관): `pytest` 미설치 1건,
   `sl_defer` 하드코딩 2건(테스트가 기본값 1.0을 박아뒀는데 운영은 0.6), `state_snapshot`
   리팩터로 소스 문자열이 안 맞는 2건. 테스트가 라이브 `.env`를 읽는 구조라 운영 튜닝마다
   깨질 수 있다.

## 오후 후속 업데이트 2 (Codex, 2026-08-25 14:57 재시작)

- `ONGUSDT`는 정식 손절이 아니라 내부 `EARLY_EXIT`로 청산됐다.
  - 로그: `2026-08-25 14:54:25` `[ONGUSDT] SHORT 초반 실패 패턴 감지 — held=3s, ROE=-1.10%, max_favorable_roe=0.00% 로 조기청산`
  - 즉 `SHORT_EARLY_FAIL`이 전역 `EARLY_EXIT_MIN_HOLD_SEC=120` 가드를 우회해 3초에도 발동하고 있었다.
- 수정:
  - `bot/config.py`: `SHORT_EARLY_FAIL_MIN_HOLD_SEC` 추가, 기본값 `15.0`
  - `bot/main.py`: `SHORT_EARLY_FAIL` 조건에 `held_sec >= short_early_fail_min_hold_sec` 추가
  - `.env`, `.env.example`: `SHORT_EARLY_FAIL_MIN_HOLD_SEC=15`
  - `tests/test_early_exit_hold_guard.py`: 3초 조기청산 금지 회귀 테스트 추가
- 검증:
  - `.\.venv\Scripts\python.exe -m unittest tests.test_early_exit_hold_guard -v`
  - 18 tests passed
- 재시작:
  - 재시작 전 다시 `run_forever -> run_forever -> bot.main -> bot.main` 중첩 체인이 관측됐다.
  - 관련 Python 프로세스를 정리한 뒤 `2026-08-25 14:57:23` 새 라이브 프로세스 기동 확인
  - `2026-08-25 14:57:26` 텔레그램 명령 리스너 시작 확인

## 오후 후속 업데이트 3 (Codex, 2026-08-25 15:01 재시작)

- `WS 체결(스파이크) 워커 process_exited` 반복 원인 확인:
  - 워커 자체 크래시가 아니라, **이전 세대 `ws_trade_worker`가 살아남은 상태에서 새 `bot.main`이 같은 샤드를 다시 띄우려다 duplicate-guard에 걸려 즉시 종료**되는 패턴이었다.
  - 로그 증거:
    - `logs/ws_trade_worker_trade0.log`, `trade1.log`에 `체결 워커(샤드 X) 가 이미 PID ... 로 실행 중 — 기동 중단`
    - 같은 시각 `logs/bot.log`에는 새 main이 그 즉시종료를 `reason=process_exited`로 오판해 재시작 루프 기록
  - 특히 `2026-08-25 14:58:17`~`15:00:40` 구간에 5/10/20/40초 백오프 재시작 루프가 확인됐다.
- 코드 수정:
  - `bot/main.py`
    - `_ws_trade_worker_pid_path()` 추가
    - `_terminate_pid_tree()` 추가 (`Windows: taskkill /PID /T /F`)
    - `_cleanup_stale_ws_trade_workers_on_start()` 추가
    - `start_ws_layer()`에서 trade worker spawn 전에 shard별 PID 파일을 읽고 살아있는 이전 세대 `ws_trade_worker`를 먼저 종료하도록 배선
  - `tests/test_ws_trade_worker.py`
    - startup cleanup 호출 존재
    - Windows tree-kill 사용 회귀 테스트 추가
- 검증:
  - `.\.venv\Scripts\python.exe -m unittest tests.test_ws_trade_worker -v`
  - `.\.venv\Scripts\python.exe -m py_compile bot\main.py bot\ws_trade_worker.py`
  - 둘 다 통과
- 재시작/실측:
  - 전체 Python 체인을 정리 후 `2026-08-25 15:01:19` 라이브 재기동
  - `2026-08-25 15:01:23` trade worker 샤드 0/2 pid=`14052`, 샤드 1/2 pid=`15620` 기동
  - worker PID 파일도 새 값으로 갱신:
    - `ws_trade_worker_trade0_pid.json` -> `14120`
    - `ws_trade_worker_trade1_pid.json` -> `18508`
  - `2026-08-25 15:01:23` 이후 최소 1분 이상 `logs/bot.log`에 `WS 체결(스파이크) 워커 ... reason=process_exited` 재발 없음
- 남은 참고:
  - `run_forever -> run_forever -> bot.main -> bot.main` 중첩 자체는 별도 문제로 여전히 다시 보일 수 있다. 이번 수정은 그 상태에서도 `ws_trade_worker`가 중복기동 실패 루프에 빠지지 않도록 막는 보호막이다.
## 오후 후속 업데이트 4 (Codex, 2026-08-25 15:56 재시작)

- 최근 손실 직후 동일 심볼의 음수 EV 재진입을 1회 보류하도록 `bot/main.py` 스캔 루프에 보호를 추가했다.
  - 조건: `negative_ev_symbols` 포함 + `recent_reentry=true` + `loss_streak>0`.
  - 후보 전체나 다른 심볼은 막지 않으므로 원칙 0(볼밴+EMA5)과 원칙 1(활발한 거래)을 유지하고, 원칙 2(승률·순익)를 보호하는 제한이다.
- 최소 증거금 계단은 확인 및 유지했다.
  - 잔고 50~99.99: 10 USDT
  - 잔고 100~199.99: 25 USDT
  - 잔고 200~299.99: 35 USDT
  - 잔고 300~499.99: 40 USDT
  - 잔고 500 이상: 50 USDT
  - 현재 로그의 총자산 약 59 USDT에서 10 USDT가 적용되는 것은 정상이다.
- 회귀 테스트 34건 통과.
- 15:56 실전 재시작 완료: 텔레그램 리스너, 기존 심볼 블락 복원, 시장/체결 WS 워커 기동 확인.
- 재시작 과정에서 외부/이전 감시기가 `run_forever -> run_forever -> bot.main -> bot.main` 중첩을 재생성하는 현상이 재확인됐다. 현재 중복 `bot.main` 정리는 했지만 감시기 중첩 자체는 별도 운영 이슈로 남아 있다.
## 오후 후속 업데이트 5 (Codex, 2026-08-25 16:04 재시작)

- WIFUSDT 복기 결과, 손절폭 문제가 아니라 SHORT 진입 품질 문제가 우선으로 판단됐다.
  - `entry_bb_event=false`, MTF 정렬 1/3, 진입 18초 후 `SHORT_EARLY_FAIL`, ROE -1.26%, 최종 pnl -0.32%.
  - 실제 거래소 STOP_LOSS 체결이 아니라 내부 조기청산이었다.
- 최근 원장 계측에서 SHORT의 `entry_bb_event=false` 표본은 14건 중 10건 손실(71.4%), 평균 손익 약 -0.29%였다.
- LONG은 같은 계측 표본이 충분하지 않아 동일 차단을 적용하지 않고 관찰 대상으로 남겼다.
- 적용한 보호:
  - `SHORT_BB_EVENT_MTF_BLOCK_ENABLED=true`
  - SHORT에서 볼밴 이벤트가 없고 MTF 정렬 비율이 0.5 미만이면 진입 보류.
  - LONG은 기존 진입 경로 유지.
- 관련 테스트 31건 및 `py_compile` 통과.
- 재시작 후 텔레그램/WS 기동 로그는 확인했으나, 외부 감시기 중첩이 재생성되어 `run_forever -> run_forever -> bot.main -> bot.main` 구조가 다시 나타났다.
- 16:04경 WS 시장/체결 워커 `process_exited` 재발 로그가 확인되어, 신규 진입 보호와 별개로 supervisor/worker 중복 문제는 아직 해결되지 않은 운영 이슈다.
## 오후 후속 업데이트 6 (Codex, 2026-08-25 16:10 재시작)

- `run_forever`/`bot.main` 중복 실행 방어를 위해 Windows named mutex를 추가했다.
  - supervisor: `Global\\BinanceFuturesBotSupervisor`
  - bot 본체: `Global\\BinanceFuturesBotMain`
  - 관련 테스트 40건 통과 및 `py_compile` 통과.
- 기존 프로세스 트리를 종료하고 재기동했다.
- 외부에서 기존 supervisor가 재기동되는 현상이 있어 `run_forever -> run_forever -> bot.main` 중첩이 재생성되는지 계속 관찰 필요.
- 현재 직접 `bot.main` 기동 후 텔레그램 리스너와 WS 워커 기동 로그 확인.
- 중복 프로세스가 보이면 가장 최근 기동된 bot.main만 남기고 이전 세대는 taskkill 트리로 정리해야 한다.
## 오후 후속 업데이트 7 (Codex, 2026-08-25 16:16)

- Windows named mutex 구현을 `ctypes.WinDLL(..., use_last_error=True)`와 `ctypes.get_last_error()` 기반으로 보완했다.
- 실제 두 번째 `python -m bot.main` 실행 테스트에서 종료코드 `78`로 중복 차단을 확인했다.
- 작업 스케줄러, Windows 서비스, 시작 폴더에서 이 저장소의 `run_forever.py` 외부 등록은 발견하지 못했다.
- 기존 고아 WS 워커와 중복 supervisor를 정리하고 수정된 supervisor를 재기동했다.
- 현재 남은 위험은 외부 런처의 정확한 출처가 확인되지 않은 상태에서 부모-자식 job tree가 봇 자식까지 함께 종료시키는 현상이다. 운영 중에는 프로세스 트리를 임의로 개별 종료하지 말고 named mutex 로그와 supervisor 로그를 기준으로 단일 체인을 확인한다.
## 오후 후속 업데이트 8 (Codex, 2026-08-25 16:25)

- 재시작 후 후보/거래가 없었던 직접 원인을 확인했다.
  - SHORT 볼밴 보호 코드가 `candidate` 생성 전에 `candidate.get()`을 호출해 `UnboundLocalError` 발생.
  - 이 예외가 해당 심볼 스캔을 중단시켜 후보 수를 비정상적으로 0으로 만들었다.
- 보호 로직을 후보 dict 생성 직후로 이동해 수정했다. LONG 동작은 변경하지 않았다.
- 관련 테스트 31건 및 `py_compile` 통과.
- 수정 후 재시작에서 STORJUSDT LONG 후보 진입 및 기존 포지션 복원 로그 확인.
- `Global\\BinanceFuturesBotMain` named mutex로 두 번째 bot.main 수동 실행이 종료코드 78로 차단되는 것도 확인했다.

## 오후 후속 업데이트 9 (사용자 결정, 2026-08-25 16:56 재시작) — EARLY_EXIT 전면 OFF

### 계기: 16:49~16:54 연속 손실 5건

```
16:49 APTUSDT      LONG  SOFT_STOP    123s ROE -4.02% 고점 0.33%  -1.503U
16:52 SUPERUSDT    SHORT EARLY_EXIT    19s ROE -1.40% 고점 0.00%  -0.587U
16:53 FARTCOINUSDT SHORT EARLY_EXIT    29s ROE -1.18% 고점 0.00%  -0.511U
16:54 PENGUUSDT    SHORT EARLY_EXIT    70s ROE -1.40% 고점 0.00%  -0.587U
16:54 BEATUSDT     SHORT EARLY_EXIT    75s ROE -1.25% 고점 0.05%  -0.534U
```

사용자가 `EARLY_EXIT_ENABLED=false`로 껐다.

### 중요: 이 플래그는 마스터 킬스위치다 (의도보다 넓게 꺼진다)

`bot/main.py`에서 `early_exit_enabled`는 네 규칙을 한꺼번에 게이트한다.

| 라인 | 꺼지는 규칙 |
|---|---|
| 2058 | `SHORT_EARLY_FAIL` |
| 2924 | `FIRST_3M_FAIL` (플래그가 `true`여도 꺼진다) |
| 2957 | `FIRST_60S_FAIL` (플래그가 `true`여도 꺼진다) |
| 2959 | `EARLY_EXIT` 본체 |

즉 `FIRST_60S_FAIL_ENABLED=true`, `FIRST_3M_FAIL_ENABLED=true`가 그대로 남아 있어도
**실제로는 동작하지 않는다.** 진입 후 약 6분간 컷 규칙이 사실상 없고,
`UNARMED_MID_HOLD_CUT`(6~8분) / `SOFT_STOP` / 손절 `4.5%` / 거래소 알고주문만 남는다.

### 원장 근거 (참고 — 결정과 반대 방향이다)

- 15:00~16:54 봇거래 20건 중 `EARLY_EXIT` 7건, 전부 손실. 다만 **7건 모두 고점 ROE가
  0.00%**로, 애초에 유리하게 간 적이 없는 진입이었다. 컷이 원인이 아니라 진입이 원인이다.
- 같은 구간 정식 손절 사례: `15:22 TUTUSDT STOP_LOSS ROE -5.29%`.
  즉 컷을 안 하면 `-1.3%`가 `-4.5~-5.3%`로 갈 수 있다(약 3~4배).
- 조기컷 반사실 검증(표본 5건, 3분봉 캐시): 컷 실현 평균 `-1.553%` vs
  안 끊었을 때 30분 후 `-8.452%`, 5건 중 3건이 손절선 터치.

### 손실 금액이 커 보이는 진짜 이유

같은 ROE인데 USDT 손실이 3~4배로 보인다 — **최소 증거금을 10 USDT로 올려 포지션이
약 4배가 됐기 때문**이다(평균 증거금 2.39 -> 9.99 USDT). ROE는 `-1.2~-1.4%`로 이전과
비슷하지만 금액이 `-0.15U`에서 `-0.55U`로 뛴다. 컷 규칙이 나빠진 게 아니다.

### [원복 조건]

- 컷을 끈 뒤 `STOP_LOSS` / `UNARMED_MID_HOLD_CUT` 건수가 늘고 **건당 손실이
  `-1.3%`보다 커지면** `EARLY_EXIT_ENABLED=true`로 되돌린다.
- 좁혀서 되돌리는 선택지: 전면 OFF 대신 `EARLY_EXIT_MIN_LOSS_ROE`(현재 2.5)를 올리거나
  `SHORT_EARLY_FAIL_MIN_HOLD_SEC`(현재 15초)를 늘려 발동을 줄이면,
  `FIRST_60S_FAIL`/`FIRST_3M_FAIL` 안전망은 유지한 채 조기청산만 완화할 수 있다.

## 운영 상태 경고 (2026-08-25 17:00 확인)

- **supervisor 없이 `bot.main`이 단독 실행 중이다.** 프로세스 트리 최상단이
  `run_forever.py`가 아니라 `python -m bot.main`(PID 11272, 부모 7428, 16:56:50 기동)이다.
  중복기동 문제를 피하려고 직접 띄운 것으로 보이는데, **봇이 죽으면 아무도 되살리지 않는다.**
  named mutex 도입(업데이트 6~7) 이후 `run_forever.py` 경유 기동으로 복귀할지 결정 필요.
- 프로세스 12개(main 2 + ws_worker 6 + ws_trade_worker 4). WS 워커는 정상 기동.

## 오후 후속 업데이트 10 (Claude, 2026-08-25 17:13 재시작) — 조기청산 좁히기 + supervisor 복구

### 1) EARLY_EXIT 전면 OFF -> 좁혀서 완화

전면 차단(업데이트 9)은 마스터 킬스위치라 `FIRST_60S_FAIL`/`FIRST_3M_FAIL`까지 같이 꺼져
진입 후 약 6분간 컷이 사라진다. 그래서 다시 켜고 발동 빈도만 좁혔다.

| 키 | 전 | 후 |
|---|---|---|
| `EARLY_EXIT_ENABLED` | false | **true** (재활성화) |
| `EARLY_EXIT_MIN_LOSS_ROE` | 2.5 | **3.5** |
| `SHORT_EARLY_FAIL_MIN_HOLD_SEC` | 15 | **45** |

- 16:49~16:54 연속 손실 4건은 전부 `SHORT_EARLY_FAIL`이었다
  (SUPERUSDT 19s / FARTCOINUSDT 29s / PENGUUSDT 70s / BEATUSDT 75s, ROE -1.18~-1.40%).
  45초로 올리면 19s/29s 두 건은 발동하지 않는다. 70s/75s는 그대로 발동한다.
- `SHORT_EARLY_FAIL`은 `EARLY_EXIT_MIN_HOLD_SEC`(120초)과 `EARLY_EXIT_MIN_LOSS_ROE`를
  **둘 다 우회하는 별도 경로**다(`main.py` 2058). 초반 컷을 줄이려면 여기를 따로 올려야 한다.
- **주의**: 오늘 손절폭을 6.0 -> 4.5로 좁혔기 때문에 `MIN_LOSS_ROE=3.5`는 손절폭의 78% 지점이다
  (예전 2.5/6.0 = 42%). 발동 구간이 3.5~4.5%p로 1%p밖에 안 남아 **일반 EARLY_EXIT 경로는
  사실상 거의 발동하지 않는다.** 더 온건하게 가려면 3.0(손절폭의 67%)이 대안이다.

### 2) supervisor 복구 — 기동이 막혀 약 10분간 봇이 완전히 내려가 있었다

`run_forever.py`의 `has_same_supervisor_parent()` 오탐으로 **17:04~17:12 사이 6번의 기동
시도가 전부 즉시 종료**됐다(exit 78). 원인은 두 겹이다.

1. 부모의 CommandLine에 `"run_forever.py"`와 `"binance-futures-bot"`이 있으면 무조건 중첩으로
   판정했는데, **사람이 쓰는 런처의 명령줄에도 그 두 문자열이 그대로 들어간다**
   (PowerShell `Start-Process ... -ArgumentList "run_forever.py" -WorkingDirectory "<repo>"`,
   bash `cd <repo> && python run_forever.py`).
2. **Git Bash/MSYS에서 `os.getppid()`는 윈도우 PID가 아니라 MSYS PID를 돌려준다.**
   그 값이 재사용된 PID와 겹치면 엉뚱한 프로세스를 부모로 오인한다
   (실측: `getppid()`가 이미 죽고 재사용된 7428을 돌려줬다).

수정:
- 부모 조회를 `os.getppid()` 대신 **자기 PID로 WMI 레코드를 찾아 `ParentProcessId`를 읽는**
  방식으로 교체(자기 PID는 항상 정확하다).
- 부모 실행 파일명이 `python`으로 시작하는지까지 확인(진짜 중첩은 spawn이므로 부모가 python이다).
- **가장 중요**: 이 검사에 기동 차단 권한을 주지 않는다. 경고만 로그하고 진행한다.
  중복 차단은 커널 named mutex(업데이트 6~7)만으로 판정한다 — mutex는 이런 오탐이 구조적으로
  없고 이미 정확히 동작한다. "봇이 안 뜨는" 실패 모드를 만드는 검사에 권한을 주면 안 된다.
- 회귀 테스트 5건 추가: `tests/test_supervisor_parent_guard.py`
  (PowerShell/bash 런처는 중첩 아님, 진짜 python 부모는 중첩, 조회 실패 시 기동 허용).

### 3) 현재 상태

- `run_forever.py` supervisor 경유로 복구. 프로세스 14개(run_forever 2 + bot.main 2 + WS 10).
  (2개씩 보이는 건 Windows multiprocessing spawn의 재실행 아티팩트로 정상이다.)
- 17:13:34 기동, ERROR/CRITICAL 0건. 손절 -4.5% 반영 확인.
- 전체 회귀 테스트: 기존 실패 5건 유지, 신규 실패 0건.

## 오후 후속 업데이트 11 (Claude, 2026-08-25 17:24 재시작) — SHORT 확률 미세상향 + 볼밴 게이트를 점수로

### EARLY_EXIT 판정: 규칙 위배가 아니다 (반사실 검증 26건)

사용자가 "EARLY_EXIT가 규칙 위배 같다"고 지적해 오늘 EARLY_EXIT 27건 전부에 대해
청산 이후 실제 가격을 30분간 추적했다(3분봉 캐시, 26건 추적 가능).

```
컷 실현 ROE 평균          -1.273%
안 끊었을 때 30분 후 평균  -14.319%   <- 11배
손절 터치했을 건수         12/26
안 끊는 게 나았을 건수      7/26
```

극단 사례: PROMUSDT SHORT 3건은 그냥 뒀으면 각각 `-97.40% / -88.36% / -82.34%`였다.
LITUSDT `-29.93%`, FARTCOINUSDT `-27.02%`. **EARLY_EXIT은 불이 아니라 화재경보기다.**
끄면 오늘 손실이 11배가 됐다. 되돌린 설정(`MIN_LOSS_ROE=3.5`, `SHORT_EARLY_FAIL_MIN_HOLD_SEC=45`)은
그대로 두는 것을 권한다.

반대 사례도 있다(7/26): TUTUSDT는 30분 후 `+32.80%`, VELVETUSDT `+34.67%`였다.
즉 컷이 항상 옳은 건 아니지만 평균으로는 압도적이다.

### 진짜 문제: SHORT가 무너졌다

**오늘 EARLY_EXIT 26건이 전부 SHORT다. 한 건도 예외가 없다.**
14시 이후 방향별: SHORT 18건 건당 `-0.1936` / LONG 4건 건당 `-0.1107`.
반사실 데이터가 이유를 보여준다 — TUTUSDT +36.7%, VELVETUSDT +44.8%, PROMUSDT는 SHORT
기준 -66%(=가격 폭등). **시장이 강하게 오르는 국면에서 SHORT 스캘핑을 계속 시도하고 있다.**

### 적용 1: `SHORT_MIN_ENTRY_PROBABILITY` 0.66 -> 0.70

크게 올리면 원칙 1을 깨므로 4%p만. 퍼널상 probability 단계 차단은 신호의 9.3%였다.
[원복 조건] SHORT 거래수가 급감하거나 SHORT 건당 손익이 플러스로 돌아서면 0.66 복귀.

### 적용 2: 볼밴 관여를 하드 게이트 -> 우선순위 점수로

| 키 | 전 | 후 |
|---|---|---|
| `BB_PARTICIPATION_REQUIRED` | true | **false** |
| `BB_PARTICIPATION_PRIORITY_BONUS` | (없음) | **0.03** |

근거: 게이트로 막으니 신호의 `33.9%`가 통째로 잘렸는데(원칙 1 손해), 원장 실측은
**반대**였다 — `bb_event=True` 17건 건당 `-0.1829` vs `False` 15건 `-0.1360`.
"볼밴 이벤트가 있으면 좋다"는 가정 자체가 아직 검증되지 않았다. 검증 안 된 가정으로
거래를 없애는 대신 순위에만 반영(`관여 +0.03 / 미관여 -0.03`)해 원칙 0을 지키면서
원장 표본을 쌓는다. 슬롯이 비어 있을 땐 순위가 무의미하므로 **거래수는 줄지 않는다.**
[원복 조건] 표본 30건 이상에서 `bb_event=True`가 유의하게 나으면 다시 `true`.

### 브리핑 (17:20 기준) 구간별 성과 (실수수료 차감 net)

| 구간 | 건수 | 빈도 | 승률 | net | 건당 | 손익비 | 평균증거금 |
|---|---|---|---|---|---|---|---|
| 06~11:57 변경전 | 21 | 3.5/시 | 42.9% | **+0.358** | +0.0170 | 2.58 | 2.39 |
| 11:57~13:29 게이트+EMA5 | 16 | 10.4/시 | 50.0% | -0.606 | -0.0379 | 0.75 | 9.75 |
| 13:29~14:01 A+C | 4 | 7.5/시 | 0.0% | -0.444 | -0.1110 | - | 9.99 |
| 14:01~16:56 손절4.5 | 21 | 7.2/시 | **28.6%** | **-3.759** | **-0.1790** | 1.25 | **17.10** |

- 손절 4.5로 좁힌 뒤 **손익비는 0.75 -> 1.25로 개선**됐다(의도대로). 그런데 **승률이
  42.9% -> 28.6%로 폭락**해 손익분기 승률 44.4%에 한참 못 미친다. 순효과는 마이너스.
- **평균 증거금이 2.39 -> 9.75 -> 17.10으로 계속 커지고 있다.** ROE 손실은 -1.0~-1.3%로
  예전과 비슷한데 금액 손실은 10배가 됐다. 오늘 손실의 상당 부분이 여기서 온다.
  **다음 1순위 후보는 포지션 크기 축소다.**
- `actual_fill_entry_price` 정상 기록 중(HEMIUSDT 0.007185 = 지정가 그대로 = 메이커 확인).

### 미판정 (표본 부족)

- **A안(whipsaw 지각 보정)**: 계속 발동 중이나 해당 구간 4건뿐이라 판정 보류.
  원복 조건(진입 60초 ROE 음수 비율 59% 미만)은 그대로 유효.
- **C안(상한 유효변동성)**: 과잉 차단 징후 없음.

## 후속 핸드오프 12 (2026-08-25) — e3 CM 실거래 전환 및 현재 운영 규칙

### 사용자 확정 원칙

- 원칙 0: e3의 진입 지표는 CM 방식. 현재 구현은 `15분 HullMA20` 방향/교차와 `4시간 EMA200` 방향 필터를 사용한다.
- 원칙 1: 거래 활동을 유지하되, 후보 품질과 체결률을 함께 기록한다.
- 원칙 2: 진입 품질과 순손익을 훼손하는 완화는 금지한다.
- e3는 1슬롯 1회 진입만 허용한다. 분할 진입과 순방향 추가 진입은 사용하지 않는다.

### e3 현재 구현

- 파일: `scripts/scalp_bot_e3.py`
- 주문/손절/익절/원장 구조는 e2 주문 엔진을 기반으로 하되, 진입만 CM 방식으로 교체했다.
- 진입은 v2 지정가 흐름으로 연결했다: LONG은 bid, SHORT는 ask에 지정가 제출, 최대 10초 대기, 미체결 시 취소하며 시장가 추격은 하지 않는다.
- WS 시장데이터 워커를 사용한다.
- 슬롯당 첫 진입 최소 증거금은 `--min-leg-margin 35`로 실행한다.
- `--tranches 1`로 실행하며, 코드에서도 이미 포지션이 있으면 추가 진입을 무조건 건너뛴다.
- 조기청산은 e3 `EARLY_ADVERSE`/`MAE_CUT`를 끄고, 메인 봇도 `EARLY_EXIT_ENABLED=false`로 고정했다. 거래소 `STOP_MARKET`과 일반 TP는 유지한다.
- e3 전용 파일을 분리했다: `scalp_bot_e3_cm_ledger.jsonl`, `scalp_bot_e3_cm_state.json`, `scalp_bot_e3_cm_bot_pid.json`.
- 기존 e2 파일과 원장은 삭제하지 않았다. 기존 e3 전략은 `scripts/scalp_bot_e3_legacy.py`로 보존했다.

### UNIUSDT 사건

- 2.20 USDT 증거금 진입은 e3가 아니라 메인 봇의 `FORWARD_SCALE_IN_ENABLED=true` 경로였다.
- 전체 계획의 40%만 1차 지정가로 제출했고 10개 중 2개만 부분체결되어 약 2.20 USDT가 들어갔다.
- `FORWARD_SCALE_IN_ENABLED=false`로 변경 후 메인 봇을 재시작했다. 이후 메인 봇도 순방향 분할을 사용하지 않는다.

### CM 검증 상태

- 로컬 1분봉 단순 비교: 15분 HullMA20 + 4시간 EMA200은 2,100건, 승률 33.33%, 손익비 0.472였다.
- 동일 단순 조건의 EMA5/26 비교는 613건, 승률 24.96%, 손익비 0.358이었다.
- 위 결과는 수수료/슬리피지/실제 슬롯·지정가 체결을 완전히 재현한 결과가 아니므로 방향성 참고용이다.
- TradingView의 `CM_Ultimate_MA_MTF_V2` 공개 설명은 MA 타입과 MTF 기능을 설명하지만, 현재 세션에서 로그인된 차트/Pine 소스에 직접 접근하지 못해 TradingView와의 100건 이상 신호 일치 검증은 아직 완료되지 않았다.
- 따라서 현재 e3는 정식 TradingView 동일 구현이 아니라 CM 설정을 근사한 라이브 실험 버전으로 기록한다. TradingView 차트 신호 데이터 또는 Pine 소스를 확보하기 전까지 동일 지표라고 주장하지 않는다.

## 후속 핸드오프 13 (Codex, 2026-08-25 21:55) — CM 기본값 반영 후 전략 분류 재정리

### TradingView 소스 확보 및 e3 반영 상태

- 공개 TradingView 페이지의 HTML에서 `CM_Ultimate_MA_MTF_V2` Pine 본문을 직접 추출해 기본 입력값을 확인했다.
  - `useCurrentRes=true`
  - `resCustom="D"`
  - `len=20`
  - `factorT3=7`
  - `atype=1`
  - `smoothe=2`
  - `doma2=false`
  - `len2=50`
  - `atype2=1`
- `scripts/scalp_bot_e3.py`에는 이 기본값 기준의 MA/MTF 계산 함수를 추가했고, 현재 e3의 방향 판정은 이 계산 결과를 사용한다.

### 중요한 결론: 텔레그램의 "눌림목매매" 표시는 현재 e3의 정확한 전략명은 아니다

- 텔레그램/시작 문구에 `EMA눌림목`이 남아 있었던 직접 원인은 e2/e3 초기 문구를 그대로 가져온 문자열이었다.
- 하지만 "완전히 틀린 말"도 아니다. 현재 e3는 **CM 방향 신호만 바꿨고, 실제 체결은 여전히 기존 EMA 눌림 진입선**을 사용한다.
- 코드 기준:
  - 방향 판정: `cm_signal_snapshot(...)`
  - 실제 진입 트리거: `tranche_targets(...)`의 `EMA5/EMA10/EMA15` 터치
  - 손절 기준: `EMA25`
- 즉 현재 e3는 아래 둘 중 하나가 아니라 **하이브리드**다.
  - 1. 순수 `CM_Ultimate_MA_MTF_V2` 추세추종
  - 2. 순수 EMA 눌림목

### 운영 판단: 1과 2 중 선택보다 3이 맞다

- **1안(순수 CM 추세추종)**:
  - TradingView 기본 CM 해석과 가장 가깝다.
  - 하지만 현재 e3 체결 로직은 눌림 대기형이라 아직 구조가 다르다.
- **2안(순수 눌림목으로 계속 운영)**:
  - 텔레그램 문구와 체결 방식은 맞지만, CM 기본값을 굳이 가져온 이유가 약해진다.
  - "TradingView 동일 지표 운영"이라는 설명과 충돌한다.
- **3안(전략을 분리 정의)**:
  - `CM 방향 필터 + 눌림 실행`을 별도 전략으로 명시하는 것이 현재 실코드와 가장 정확히 일치한다.
  - 이후에는 목적에 따라 두 갈래로 나누는 게 맞다.
    - TradingView 복제가 목표면: 체결도 CM 기준으로 재설계
    - 미니 스윙 실전 성과가 목표면: 현재 하이브리드의 성과를 별도 표본으로 검증

### 이번 세션 운영 조치

- e3 잔여 포지션(`TRUMPUSDT`, `VELVETUSDT`)을 정리한 뒤, 상태 파일을 백업하고 완전 빈 상태로 재시작했다.
- 백업:
  - `logs/scalp_bot_e3_cm_state.backup_20260825_215142.json`
- 재기동 후 확인:
  - 실포지션 없음
  - `logs/scalp_bot_e3_cm_state.json` 내용은 `{"symbols": [], "entered_at": {}, "legs": {}}`
  - WS 워커 재부착 확인
  - v2 지정가 진입 경로 유지

### 문구 정정

- `scripts/scalp_bot_e3.py` 시작 문구를 `EMA눌림목`에서 `CM방향+눌림실행`으로 수정했다.
- 따라서 앞으로 텔레그램/로그 시작 문구가 현재 전략 구조를 더 정확히 반영한다.

### e3 데이터 확인용 운영 맵

- **전략 코드**
  - `scripts/scalp_bot_e3.py`
  - 현재 전략 정의: `CM 방향 필터 + EMA 눌림 실행`
  - TradingView 기본값 계산 함수와 실제 진입/청산 로직이 모두 이 파일에 있다.
- **원장**
  - `logs/scalp_bot_e3_cm_ledger.jsonl`
  - e3 성과 집계의 단일 기준 파일. 메인 봇 `trade_ledger.jsonl`과 섞지 않는다.
- **상태 파일**
  - `logs/scalp_bot_e3_cm_state.json`
  - e3가 "자기 소유"로 관리하는 심볼, 진입시각, 차수 수를 저장한다.
  - 재시작 후 빈 старт 여부와 기존 포지션 승계 여부를 가장 먼저 확인할 파일이다.
- **PID 파일**
  - `logs/scalp_bot_e3_cm_bot_pid.json`
  - `logs/scalp_bot_e3_cm_ws_pid.json`
  - 현재 e3 본체와 WS 워커 추적용. 프로세스 정리 전후에 함께 확인한다.
- **백업 상태 파일**
  - `logs/scalp_bot_e3_cm_state.backup_20260825_213613.json`
  - `logs/scalp_bot_e3_cm_state.backup_20260825_215142.json`
  - 완전 초기화 재기동 전에 백업해 둔 이전 상태 기록이다.
- **로그**
  - `logs/scalp_bot_e3_cm_stdout.log`
  - `logs/scalp_bot_e3_cm_stderr.log`
  - 기동/오류 확인용 보조 파일이다. 성과 판정 기준은 아니다.

### e3 현재 스냅샷 (2026-08-25 21:56 KST 확인)

- 실행 프로세스
  - e3 본체: `scalp_bot_e3.py` PID `20996`
  - 상위 spawn 부모: PID `12180`
  - WS 워커: `bot.ws_worker` PID `19448`
  - WS 하위 프로세스: PID `18760`
- 현재 실행 인자
  - `--signal-tf-min 3`
  - `--tranches 1`
  - `--min-leg-margin 35`
  - `--early-adverse-sec 0`
  - `--mae-cut-roe 0`
  - `--i-know-it-loses`
  - `--no-buttons`
- 상태 파일 내용
  - `{"symbols": ["TUTUSDT"], "entered_at": {"TUTUSDT": 1787662568.2804668}, "legs": {"TUTUSDT": 1}}`
- 거래소 실포지션 확인
  - `TUTUSDT SHORT`, 수량 `-4557.0`, 평단 `0.04535`, 레버리지 `5`
  - `TRUMPUSDT`, `VELVETUSDT`, `SOLUSDT`, `SPKUSDT`, `XRPUSDT`, `DOGEUSDT`, `HYPEUSDT`, `TACUSDT`는 모두 포지션 없음

### e3 데이터 읽는 순서

1. `logs/scalp_bot_e3_cm_state.json`
   현재 e3가 어떤 심볼을 자기 포지션으로 보고 있는지 먼저 확인한다.
2. 거래소 실포지션 조회
   상태 파일과 실제 포지션이 일치하는지 확인한다. 상태 파일만 비워도 거래소 포지션이 남아 있으면 재기동 시 다시 채택된다.
3. `logs/scalp_bot_e3_cm_bot_pid.json`, `logs/scalp_bot_e3_cm_ws_pid.json`
   현재 인스턴스와 WS 워커가 살아 있는지 확인한다.
4. `logs/scalp_bot_e3_cm_ledger.jsonl`
   진입/청산/손익 집계는 이 파일만 기준으로 본다.
5. `logs/scalp_bot_e3_cm_stdout.log`, `logs/scalp_bot_e3_cm_stderr.log`
   기동 실패나 예외 원인을 보조적으로 확인한다.

### 중요한 운영 주의사항

- `state.json`만 삭제해도 거래소 포지션이 남아 있으면 e3는 재기동 후 다시 채택한다.
- 반대로 거래소 포지션을 먼저 비우고 `state.json`까지 삭제해야만 진짜 빈 старт가 된다.
- 따라서 "완전 새 시작" 판정은 아래 3개를 동시에 만족해야 한다.
  - 거래소 실포지션 없음
  - `logs/scalp_bot_e3_cm_state.json`이 비어 있거나 `{"symbols": [], ...}`
  - 새 `bot_pid/ws_pid`로 재기동 확인

### 다음 복기 기준

- e3는 메인 봇과 원장을 분리해 `scalp_bot_e3_cm_ledger.jsonl` 기준으로만 성과를 집계한다.
- 필수 지표: 시간당 후보수, 지정가 제출수, 완전/부분체결수, 미체결 포기수, 진입 직후 30초/60초 ROE 음수율, 승률, 손익비, 순손익.
- 조기청산 OFF 상태에서 거래소 STOP_LOSS와 TP의 결과를 먼저 100건 이상 확보한 뒤 CM 신호의 라이브 적용 여부를 재판정한다.


## 후속 핸드오프 13 (Claude, 2026-08-25 22:12 재시작) — e3 관측 복구 + 원장 오염 수정

### 전제 정정: 메인 봇(bot.main)은 더 이상 돌지 않는다

21:03 정기점검에서 확인한 사실이다. 프로세스는 `scripts/scalp_bot_e3.py` + `bot.ws_worker` 4개가
전부이고 `run_forever.py` / `bot.main`은 없다. 따라서 오늘 오후에 메인 봇에 적용한 설정들
(볼밴/RSI 점수화, 순방향 분할, 고정 증거금 27.5, whipsaw 하한 0/상한 1.0 등)은
**현재 운영과 무관**하다. `.env`와 `trade_ledger.jsonl`도 e3와 섞으면 안 된다.

원칙 0은 **CM 기준으로 개정**했다(`CLAUDE.md`, `.env` 헤더 갱신 완료).
구 원칙 0(볼밴+EMA+RSI)은 메인 봇으로 복귀할 때만 유효하다.

### 손실 귀속: 90%가 봇 밖이다

| 구간 | 계좌 실현손익 | 거래 |
|---|---|---|
| 최근 1시간 | -1.99 USDT | 21건 |
| 최근 3시간 | **-26.19 USDT** | 37건 |
| 최근 6시간 | -29.50 USDT | 55건 |

같은 구간 e3 원장은 15건 / **-2.54 USDT**. 차액 약 -23.65가 봇 원장 밖(수동매매)이며
19~21시 구간에 집중돼 있다. **봇 규칙을 고쳐서 해결될 손실이 아니다.**

e3 원장 15건: 승률 26.7%, 실순익 -2.5445, 수수료 1.58.
청산사유는 `STOP_EXCHANGE` 13건 / `BB` 2건. 명목 평균 173~207 USDT, 레버리지 5,
증거금 약 35 USDT(`--min-leg-margin 35`와 일치).

### 버그 감사 결과

문제없음으로 확인:
- 상태 파일 <-> 거래소 포지션 **완전 일치**(고아/미추적 0건). 앞선 "ONGUSDT 미추적" 지적은
  읽은 시점 어긋남이었고 철회한다.
- `real_net = rz - comm` 부호 정상(검산: XRPUSDT 0.8154 - 0.1249 = 0.6905).
- 커미션 자산 최근 68건 전부 USDT — BNB 수수료 혼입 없음.
- `since_trade_id` 필터로 같은 심볼 반복 거래 시 이전 체결 오염 차단 중.

**고친 것 1 — e3 실행 로그가 어디에도 안 남았다 (관측 공백)**
`say()`가 `print`만 해서, 런처가 stdout을 파일로 묶지 않으면 로그가 사라졌다.
실측: 21:52 기동분의 `*_stdout.log` / `*_stderr.log`가 20:53에서 멈춰 있었다.
거래가 시간당 40건대인데 사후 복기 수단이 원장 몇 줄뿐이었다.
`log_line()`을 추가하고 `say()`가 항상 파일에 append 하도록 했다 — **런처와 무관하게** 남는다.
- 파일: `logs/scalp_bot_e3_cm_run.log` (20MB 로테이션, 백업 3개)
- 관측 코드가 실매매를 죽이면 안 되므로 예외는 전부 삼킨다.

**고친 것 2 — 외부청산 체결조회 실패 시 원장 ROE 오염**
예전엔 조회 실패 시 `exit_price=entry`, `roe_pct=0`으로 적었는데 `real_net`은 실제값이라
"ROE 0%인데 손익 -1.5 USDT"인 행이 생긴다. ROE 기준 통계가 조용히 오염된다.
이제 마크가로 ROE를 추정하고 **`roe_estimated=true` 플래그**를 남긴다.
집계 시 그 행은 ROE 통계에서만 빼면 되고 손익 통계는 그대로 유효하다.
(현재 원장 15건에는 발현 사례 없음 — 예방 수정)

**확인만 한 것 3 — `sync_stop` 실패 경로는 안전망이 있다**
`place_stop_market` 실패 시 `algoId=0`을 돌려주고 "봇 폴링 손절만 남음"을 경고한다.
폴링 손절(`STOP_EMA25`, `mark <= pos.stop_price`)은 **조기청산 인자와 독립**이라
`--early-adverse-sec 0 --mae-cut-roe 0` 상태에서도 살아 있다. 무방비 구간은 없다.

### 재시작 (22:12:47)

같은 인자로 재기동:
`--signal-tf-min 3 --tranches 1 --min-leg-margin 35 --early-adverse-sec 0 --mae-cut-roe 0
 --i-know-it-loses --no-buttons`

복구 로그 확인:
```
22:12:51 기존포지션 채택 ONGUSDT LONG qty=1663.0 진입0.10626 손절0.102511 - SL 등록
22:12:52 재시작 복구 요약: 채택 1건 / SL성공 1건 / SL실패 0건
22:12:53 WS 준비 완료 10/10 - 실시간 스캔/신규 진입 재개
```
상태 백업: `logs/scalp_bot_e3_cm_state.backup_before_logfix.json`
코드 백업: `scripts/scalp_bot_e3.py.bak_20260825`

### 사용자 결정 사항 (지킬 것)

- **원복 조건이 충족돼도 임의로 되돌리지 말 것.** 알림만 하고 결정을 요청한다.
- 조기청산 off는 의도적이다(필요성 미확인). 켜자고 반복 제안하지 말 것.
- 거래 빈도가 높은 것도 의도적이다(복구 국면).

### 다음 확인 항목

1. `logs/scalp_bot_e3_cm_run.log`로 e3 판단 과정을 추적할 수 있게 됐다 — 다음 이상 발생 시 여기부터 본다.
2. e3 표본이 30건을 넘으면 승률/손익비/건당 기대값을 원장으로 판정한다(현재 15건, 판정 보류).
3. 수동매매 손실(19~21시 -23.65 USDT) 복기는 별도 요청 시 진행한다.

---

## → 이어짐: `HANDOFF_2026-08-26.md`

2026-08-26 작업은 별도 파일로 분리했다. 그날의 핵심:
- 어제 넣은 **안 1(신규 손절폭 ROE 5% 상한)이 `-2021`을 15% 유발**해 무보호 포지션을
  만들었다. 밤샘 손실의 34%가 그 9건에서 나왔다. **원복함**(사용자 승인).
- **무보호 고아 포지션 사고**(진입주문이 재시작으로 증발)와 **봇 12분 freeze 사고**
  (REST 타임아웃 부재)를 발견해 3겹으로 막았다.
- 진입/익절 재설계 4건 채택, **3건 기각**(캐시 시뮬만 믿었다가 원장에서 뒤집힌 사례).

