# 운영 원칙 (모든 판단의 최상위 기준)

원칙 0 / 1 / 2 는 전략·설정·코드 변경을 판정하는 고정 기준이다. 변경 제안은 반드시
세 원칙 각각에 대해 "지킴 / 해침 / 무관"을 명시한 뒤 채택 여부를 결정한다.
("보강" 항목은 각 원칙에 딸린 단서·측정 결과이며 별도 판정 대상이 아니다.)

- **원칙 0 — CM 방향 신호** (2026-08-31 개정): 현재 운영 전략은 `scripts/scalp_bot_e3.py`(e3)이며
  진입 방향은 **CM Ultimate MA MTF**(**3분봉** HullMA20 방향/교차 + 4시간 EMA200 필터)로
  판단하고, 체결은 눌림 대기형이다. 즉 **볼밴+EMA+RSI 뼈대가 아니다** — 그 뼈대는 이전
  메인 봇(`bot.main`)의 것이고, 메인 봇은 현재 실행되지 않는다.
  - **"15분 HullMA20" 이 아니다** (2026-08-31 정정). `--cm-use-alt-resolution` 이 꺼져 있어
    HullMA20 은 신호봉(`--signal-tf-min 3`)에서 계산된다. 3분봉 60분 룩백 vs 15분봉 300분
    룩백은 완전히 다른 신호다. **켜서 15분으로 바꾸려면 `--cm-res-custom 15` 를 반드시
    함께 줘야 한다** — 그 인자의 기본값은 `D`(일봉)다.
  - 두 봇의 원장을 섞지 않는다: e3는 `logs/scalp_bot_e3_cm_ledger.jsonl`,
    메인 봇은 `logs/trade_ledger.jsonl`.

- **원칙 0 보강 — 이 전략에는 검증된 엣지가 없다** (2026-08-31 신설):
  아래는 근거 있는 측정 결과이므로 **"CM 이 옳다"를 전제로 판단하지 말 것.**
  전문은 [HANDOFF_2026-08-31.md](HANDOFF_2026-08-31.md).
  - **CM 방향 신호는 라이브 보유시간(15분) 구간에서 음수다.** 3분봉 31일·81심볼,
    드리프트 중립화 후 건당 `-0.0069%`(심볼중앙값 `-0.0032`, 시각t `-4.7` / 심볼t `-2.1`).
    3구간 중 2구간이 음수 — 방향 엣지가 아니라 국면 베타였다.
  - **종전 이 자리에 있던 "메인 봇 뼈대 볼밴+EMA+RSI 15분 `+0.446%`" 는 삭제한다.**
    재측정에서 재현되지 않고 **부호가 반대**다(`-0.0145%`, t `-2.1`/`-2.1`, 3구간 전부 음수).
    그 수치는 드리프트 중립화를 거치지 않아 상승장 베타를 엣지로 읽은 값이었다.
    **메인 봇 복귀는 근거 없는 선택지다.** 덤으로 그 뼈대의 RSI 조건은 볼밴돌파+EMA 와
    건수·수치가 완전히 동일해 아무 일도 하지 않는다(조건 중복).
  - **네 축이 전부 비용선 미달이다**: 파라미터(22구성) / 신호뼈대(20여 구성) /
    시간대(3분~24시간) / 가격 외 데이터(펀딩캐리·OI·포지셔닝). 비용선을 넘는 구성이
    **하나도 없다**. 2026-09-01 에 비용선을 실측으로 교정하고 전 후보를 재채점했으나
    **기각 목록은 바뀌지 않았다**(아래 보강 3).
  - 8~12시간 보유가 한때 비용선을 넘어 보였으나, 같은 창에서 상장폐지된 심볼 145개를
    되돌려 놓자 중앙값이 `+0.1765` -> `+0.0211` 로 무너졌다. **생존편향이었다.**
  - 남은 미탐색은 **호가·주문흐름** 하나뿐이고, 이력이 없어 2026-08-31부터
    [start_obook.ps1](start_obook.ps1) 로 직접 쌓는 중이다. **2~3개월 전에 중간 결과로
    판단하지 말 것.**

- **원칙 0 보강 3 — 비용선 실측 교정과 재채점 결과** (2026-09-01 신설):
  종전 단일 비용선 `0.11%`(수수료 0.07 + 슬리피지 0.04)는 **체결 방식을 특정하지 않은**
  값이었다. e3 원장 1,017건 실측으로 교정했고, 그래도 결론은 그대로다.
  - **수수료는 하나가 아니라 경로별로 갈린다**: 지정가 TP `0.0401%`(maker+maker) /
    손절 `0.0504~0.07%`(maker+taker). 그런데 [edge_lab.py](scripts/edge_lab.py) 의 측정
    규약(시가 진입 / 종가 청산)은 **둘 다 taker** 라 `0.10%` 를 써야 한다.
    -> 단일 상수를 버리고 **보수선(taker) 0.10+슬립x2 / e3선(maker) 0.04** 두 개로 나눴다.
    두 선 사이는 "체결을 지정가로 만들 수 있으면 산다"는 **조건부** 칸이다.
  - **`real_commission` 에 다리 누락 버그가 있다.** 건의 절반 가까이가 한쪽 다리만
    기록돼 있다(TP_LIMIT 310/500 이 정확히 `0.0200%` = maker 편도 하나).
    보정하면 왕복 `0.0408%` -> `0.0511%`, **e3 순손익 `-169.30` -> `-185.46`**.
    원장으로 손익을 논할 때 이 누락을 먼저 보정할 것.
  - **슬리피지는 실측 불가.** `trade_ledger.slippage_pct` 는 `exit_price` 가 mark
    추정치라 체결가-마크 괴리를 잰다(익절 `+0.68%` / 손절 `-0.72%` 로 부호가 청산방향과
    상관). 실행비용이 아니다. 가정으로 남기고 `--slip` 으로 노출했다.
  - **재채점 결과(`--rescore`)**: 3m/30d 0건. 1h/180d 절제실험이 원인을 짚는다 —
    가드없음 35건 -> **상장폐지 포함만으로 1건** -> stride 만 8건 -> **둘 다 0건**.
    **생존편향이 압도적 주범이다.** 유일 생존자 횡단면 모멘텀 40봉 12시간
    (`+0.0779|+0.0404`)도 stride 에서 죽는다.
  - 덤: 메인봇 뼈대는 가드를 걸면 `-0.31 ~ -0.40%`(t `-2.3`)로 **유의하게 음수**다.

- **원칙 0 보강 4 — 시간대(위상) 계절성도 기각** (2026-09-01 신설):
  `logs/phase_sweep.txt` 에서 24개 위상의 양수 구간(UTC 17~02시)이 흩어지지 않고
  **연속으로 뭉쳐** 보였다. 후보 자체는 792034c 에서 기각됐지만 "시간대 효과가
  실재하는가"는 검증된 적이 없어 [scripts/edge_phase_holdout.py](scripts/edge_phase_holdout.py)
  로 따로 판정했다. **기각이다.**
  - 선결 확인: 1h 패널은 UTC 00:00 시작 + 봉 누락 0개라 **위상 k = UTC k시**가 정확히 성립.
  - **train/holdout 위상 프로파일 상관 = `-0.043`**. 사전등록한 "train 상위 8위상"의
    holdout 성적 `+0.1119` 는 **하위 8위상 `+0.0925`, 전체 평균 `+0.1022` 와 같다.**
  - 순열검정 1,000회: 무작위 8위상의 holdout 평균이 `+0.1002`. 실제 선택은 백분위
    **65.7%**(p=0.34). 즉 holdout 의 `+0.11` 은 위상 선택이 아니라 **그 기간의 드리프트**다.
  - 다만 **신호 간 위상 프로파일 상관은 평균 `+0.342` 로 실재한다.** 즉 위상 구조는
    있으나 **기간 특유의 공통 요인**이지 반복되는 시계 효과가 아니다.
    `phase_sweep.txt` 가 매끄럽고 연속으로 보인 이유가 이것이다.
  - **교훈: 위상 프로파일이 매끄럽다는 것은 실재의 증거가 아니다.** 기간 내 공통
    요인이면 당연히 매끄럽다. 판정은 반드시 홀드아웃 + 순열귀무분포로 한다.

- **원칙 0 보강 2 — 라이브 재개 조건** (2026-08-31 신설):
  자산이 회복돼도 **현 설정으로 그냥 재개하지 않는다.** 아래를 모두 만족하는 신호가
  나온 뒤에 재개한다. 세 개 중 하나라도 못 넘으면 재개하지 않는다.
  1. 드리프트 중립(같은 시각 전 심볼 평균 차감) 건당 엣지가 **평균과 심볼중앙값 모두**
     왕복 비용선 위일 것. 평균만 크고 중앙값이 0 근처면 소수 급등종목의 꼬리다.
  2. **시각 클러스터 t 와 심볼 클러스터 t 가 모두 2 이상**일 것. 순진한 t 는 같은 시각
     심볼 간 상관과 보유구간 겹침 때문에 부풀려진다.
  3. 표본을 국면별로 3등분했을 때 **전 구간 같은 부호**일 것.
  - 측정 도구는 [scripts/edge_lab.py](scripts/edge_lab.py) 를 쓴다(위 셋이 구현돼 있다).
    보유구간 겹침 제거를 위해 **`--stride` 를 보유 봉수와 반드시 같게** 둘 것.
  - 표본에 **상장폐지 심볼을 포함**할 것([scripts/edge_delisted.py](scripts/edge_delisted.py)).
    생존자만으로 시장평균을 내면 기준선이 위로 편향돼 **숏 엣지가 자동으로 과대평가된다.**

- **원칙 1 — 거래는 활발해야 함**: 거래빈도를 떨어뜨리는 변경은 그 자체로 비용이다.
  진입필터를 새로 조이는 제안은 "빈도 손실 대비 순익 개선"이 수치로 입증돼야 한다.
  - **단서** (2026-08-31): 이 원칙과 측정이 충돌한다. 엣지가 **조금이라도** 커지는
    방향은 일관되게 보유시간을 늘리는 쪽이었다(3분봉 15분 `-0.007%` -> 1시간봉
    24시간 `+0.29%`). 그런데 8~24시간 보유는 슬롯 10개 기준 하루 10~30건으로
    현재 200건대에서 한 자릿수 배로 준다. **빈도를 지키는 시간대에는 애초에
    잴 만한 엣지가 없다는 것이 현재까지의 측정 결과다.**
    (그 긴 보유의 엣지마저 생존편향을 걷으면 사라졌다 — 원칙 0 보강 참조.
    즉 지금은 "빈도를 포기하면 이긴다"가 아니라 **양쪽 다 근거가 없는 상태**다.)
    빈도와 보유시간 중 무엇을 포기할지는 측정이 아니라 사용자 결정 사항이므로,
    이 충돌을 만나면 임의로 정하지 말고 물어볼 것.
- **원칙 2 — 승률·순익을 해치면 안 됨**: 빈도를 늘리는 변경도 승률/순손익이
  나빠지면 기각한다. 판단은 **해당 봇의 실거래 원장** 기준으로만 한다 —
  e3는 `logs/scalp_bot_e3_cm_ledger.jsonl`, 메인 봇은 `logs/trade_ledger.jsonl`.
  (2026-08-31 정정: 종전에는 현재 실행되지도 않는 메인 봇 원장만 적혀 있었다.)

- **원칙 2 보강 — 애매한 거래는 빠르게 작은 수익으로 익절하고 포지션을 종료한다**:
  무장(armed)까지 못 가고 방향이 애매한 거래는 큰 수익을 기다리지 않는다.
  유리구간이 있었으면 작게라도 챙기고 끝낸다. "본전 되돌림"은 손실로 취급한다.
  구현 축: `AMBIGUOUS_QUICK_PROFIT_EXIT` 계열.
  - **단서** (2026-08-28 측정, 2026-08-31 재확인): 되돌림·시간 청산은 **다섯 번 기각**됐다.
    보유 중앙값이 익절 5.8분 / 손절 5.2분으로 **승자와 패자가 초반에 구분되지 않기**
    때문에, 진행 중 포지션에 손대는 규칙은 반드시 승자를 더 많이 자른다
    (60분 만료: 손절 2건 `-3.79` 막고 익절 8건 `+13.73` 죽임).
    원칙이 틀린 게 아니라 **적용 대상이 진입 단계에서 이미 걸러진 것**으로 본다.
    **함정**: 손절건에만 되돌림을 적용해 재계산하면 "+105 개선"이 나오는데, 그건
    되돌림이 잘라먹었을 익절건을 빼지 않은 값이다. 낙관 상한이 아니라 방향이 틀렸다.

원칙 1과 2는 서로 당긴다. 충돌하면 **원장 표본으로 판정**하고, 표본이 부족하면
(구간별 5건 미만) 채택하지 않고 관찰 대상으로 남긴다.

## 판정 시 참고

- 손익 판단은 승률 단독으로 하지 않는다 — 승률 지표는 청산규칙과 동어반복이 된다.
  수수료 전 엣지 / 심볼분해 / 사유별 손익을 함께 본다.
- 백테스트 검증은 `offline_backtest.py`의 pending(다음봉 시가 진입) 경로로만 한다.
  **진입 신호의 엣지 자체**를 잴 때는 [scripts/edge_lab.py](scripts/edge_lab.py) 를 쓴다
  (드리프트 중립 + 이중 클러스터 t + 심볼중앙값 + stride). 청산규칙을 넣고 재면
  승률이 청산규칙과 동어반복이 되므로 두 도구를 섞지 않는다.
- 대량 REST 호출(백테스트 등)은 라이브 봇과 API 키가 겹치면 IP 밴 위험이 있다.
  과거 봉·펀딩·OI 수집은 **공개 엔드포인트/덤프 전용 도구**를 쓴다(인증 0회):
  `edge_fetch.py`(봉) / `edge_funding.py`·`edge_funding_dumps.py`(펀딩) /
  `edge_metrics.py`(OI·포지셔닝) / `edge_delisted.py`(상장폐지 심볼).

### 후보가 죽는 방식은 매번 같다 (2026-08-31)

지금까지 기각된 후보 다섯 개가 전부 같은 서명을 보였다. **새 후보를 볼 때 이 셋을
먼저 확인하면 대부분 여기서 걸러진다.**

1. **평균은 좋은데 심볼중앙값이 안 따라온다** — 소수 급등종목의 꼬리다.
   실례: RSI 과매수롱이 120분 `+0.6444%`(t+5.4)로 비용선의 6배로 보였으나
   심볼중앙값 `-0.045`, 84심볼 중 40%만 양수, BTRUSDT 단독 `+36.91`.
2. **표본을 넓히면 사라진다** — 9.4일에서 31일로 늘리자 CM 이 `+0.0106` -> `-0.0069` 로
   부호가 뒤집혔다. 국면 하나짜리 표본은 엣지가 아니라 베타를 잰다.
3. **겹침·유니버스를 안 걷으면 부풀려진다** — 보유구간 겹침(`--stride` 미적용)과
   생존자만의 유니버스, 둘 다 t 를 키우고 숏 엣지를 만들어낸다.

## 걸려 있는 원복 조건 (조건 충족 시 되돌릴 것)

- **`SPIKE_ENTRY_AGGRESSIVE_FILL=true`** (2026-08-25): main.py의 aggressive fill 게이트가
  micro_scalp lane 추가 때 유실돼 플래그가 false인데도 공격적 체결이 나가고 있었다.
  게이트는 코드에서 복원하고, 실동작 유지를 위해 .env에 true를 명시했다.
  근거는 원장 7일 실측(스파이크 lane 36건 건당 +0.0073 vs 비스파이크 -0.0066)이며,
  체결방식과 후보선별이 분리되지 않은 교란값에 표본 36건이라는 한계가 있다.
  **원복 조건: 스파이크 lane 건당 순손익이 마이너스로 돌아서면 false로 되돌린다.**

- **`SHORT_REVERSAL_RISK_SIZE_MULT=0.65` / `FLOOR_MULT=0.85`** (2026-08-25): 되감기위험
  SHORT가 건당 -0.0079로 순손익 음수라 비중만 축소. `PRIORITY_PENALTY`는 거래수를 줄이므로
  0.025 유지. **원복 조건: 되감기위험 SHORT 건당 손익이 플러스로 돌아서면 완화 재검토.**

- **최소 증거금 10 USDT** (2026-08-25): 하한이 이 잔고대의 비율 사이징 결과보다 커서
  방어배율(BTC정렬/방향별성과/최근성과/기대값)이 사실상 무력화된다. 교차점은 약 66.7 USDT
  (10 / 15%)로, 그 위로는 다시 비율 사이징이 지배한다.

- **CM 기반 지정가 TP (`--cm-tp-limit`, 기본 on / `--cm-tp-pullback-pct=0.2`)** (2026-08-25):
  익절선을 볼밴(e2 잔재)에서 원칙 0(CM 최대익절선 = HullMA20 방향전환 이후 스윙 극값)으로
  바꾸고, 거기서 0.2% 앞당긴 값에 reduceOnly LIMIT 을 거래소에 실제로 등록한다.
  근거는 3분봉 캐시 43심볼 동일진입 1,943건 A/B — 건당 ROE -0.35% -> +0.38%,
  승률 52.9% -> 75.6%, 45분내 체결률 43.1% -> 82.5%. 청산이 taker 0.05% -> maker 0.02%.
  한계: 캐시가 5.5시간뿐이고, CM 목표가 진입가를 이미 지난 26.5%는 평균에서 빠져 있다
  (그 26.5%는 기존 볼밴/RR 폴링 익절로 폴백하므로 오늘과 동작 동일).
  **원복 조건: 지정가 TP 체결률이 60% 아래로 떨어지거나 건당 순손익이 볼밴 시절보다
  나빠지면 `--no-cm-tp-limit`.** (원복은 사용자 지시 없이 임의 진행하지 않는다)

## 현재 가동 상태 (2026-08-31)

- **라이브 봇은 정지 상태다.** 잔고 소진으로 e3·e4·htf03·cm05 전부 실행되지 않는다.
  아래 e4 / 원복 조건 문단들은 **다시 켤 때를 위한 기록**이지 현재 동작 설명이 아니다.
- 유일하게 상주 중인 프로세스는 **호가·주문흐름 기록기**([start_obook.ps1](start_obook.ps1))다.
  공개 스트림 전용 / API 키 미사용 / 주문 기능 없음 / 원장·상태 파일 미접근이라
  봇을 다시 켜도 간섭하지 않는다. 산출물은 `logs/obook/` 뿐이다.
- 재개 전에 **원칙 0 보강 2(라이브 재개 조건)** 를 먼저 볼 것.

## 명명 규칙 — e4 (2026-08-27 확정, 현재 정지)

과거 라이브와 A/B 하던 **새 로직의 공식 명칭은 `e4`** 다. 실행 실체는
`scripts/scalp_bot_e3.py` 를 `--dry-run --instance-tag ... --attach-ws` 로 띄운
그림자 인스턴스이고, 라이브(e3)와 다른 인자는 현재 `--cm-flip-max-bars`
(라이브 e3 = `5`, e4 = `-1`) 하나뿐이다.

- 원장을 섞지 않는다: 라이브 e3 = `logs/scalp_bot_e3_cm_ledger.jsonl`,
  e4 = `logs/scalp_bot_e3_cm_ledger_e4.jsonl`.
- **완료(2026-08-28 09:40)**: 그림자 태그를 `shadow` → `e4` 로 바꿔 재기동했다.
  런처는 `start_e3_e4.ps1`(구 `start_e3_shadow.ps1` 은 제거).
  기존 `_shadow.*`(원장 81건 / 로그 659줄 / 상태)를 `_e4.*` 로 이어붙였고,
  원본은 `*.migrated_20260828` 로 은퇴시켜 이중집계를 막았다.

- **⚠ e4(dry-run)로 손익을 비교하면 안 된다.** 거래소 주문이 없어 청산 구조가
  라이브와 다르다: 지정가 TP 가 안 걸려 `RR` 폴링으로 나가고 `--cm-tp-max-roe`
  상한을 안 받아 실측 +18.75% 까지 갔으며, 거래소 손절도 없어 고정 8% 설정인데
  실측 -11.14% 로 끝났다.
  - ✅ 유효: 어떤 신호를 잡는가 / 시간당 몇 건 / 스킵 구성 → **원칙 1 판정용**
  - ❌ 무효: 순손익 · 승률 · 손익비 · 청산사유 분포
  - 청산 비교는 원장 반사실 재생으로 한다.

- **2026-08-28 판정**: e3(flip 5) vs e4(flip -1) 의 손익 차이는 백테스트 하네스에서
  -0.0358% vs -0.0310% 로 **표준오차의 1/5** 이라 결론이 안 난다. 표본을 더 쌓아도
  실용적으로 무의미하므로, e4 는 손익 A/B 가 아니라 **거래빈도 관측용**으로만 볼 것.
  근거: [HANDOFF_2026-08-28.md](HANDOFF_2026-08-28.md) 2장.
