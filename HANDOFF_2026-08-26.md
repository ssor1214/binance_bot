# 핸드오프 2026-08-26 — e3 CM 스캘퍼

전제: 운영 전략은 `scripts/scalp_bot_e3.py`(e3) 단독. 원장 `logs/scalp_bot_e3_cm_ledger.jsonl`,
런로그 `logs/scalp_bot_e3_cm_run.log`. 메인 봇(`bot.main`)은 돌지 않는다.
원칙 0/1/2 는 `CLAUDE.md` 참조. **원복 조건이 충족돼도 임의로 되돌리지 말 것 — 알림만 한다.**

시간순 원문(823줄)은 `HANDOFF_2026-08-26.raw_823lines.md` 에 보존돼 있다. 이 문서는 성격별 재구성본이다.

---

# 1. 지금 상태

```
잔고 약 275(입금 포함) / 오늘 순손익 약 -85 / 원장 약 400건
배포 44회 — 개별 변경의 귀속은 불가능하다

시간당으로 보면: 어제 -4.14/h  vs  오늘 -3.37/h
(총손실이 6.6배인 것은 가동 시간이 8배(2.5h -> 20.3h) 길어서다. 악화가 아니다)

어제 59건 23.6건/h 승률42.4% 건당-0.1753
오늘 341건 16.8건/h 승률51.3% 건당-0.2007
-> **거래는 29% 줄었는데 건당은 나빠졌다.** 오늘 넣은 필터들이 원칙 1 의 비용만
   치르고 원칙 2 의 대가를 못 받았다는 뜻이다. 그래서 저녁에 규칙을 정리했다.
```

## 현재 가동 설정 (20:45)

```
scripts\scalp_bot_e3.py
  --signal-tf-min 3 --tranches 1
  --cm-atype 4                     # HullMA20. 종전 기본값 1(SMA)이었다 — 오늘 최대 수정
  --max-same-side 3                # 유일한 '차단' 규칙
  --min-entry-edge-pct 0.3         # 진입우위 부족 시
  --entry-edge-size-mult 0.3       #   차단이 아니라 0.3배 축소
  --stop-widen-pct 0.65            # 손절폭 하한(가격%)
  --cm-tp-pullback-pct 0.5 --cm-tp-max-roe 4
  --min-leg-margin 30 --max-leg-margin 50
  --max-concurrency 10 --max-exposure 0.95   # 잔고 272 -> 슬롯8 증거금32.3 명목161
  --max-signal-age 10 --watchdog-sec 180
  --adopt-max-stop-roe 5 --new-max-stop-roe 0
  --early-adverse-sec 0 --mae-cut-roe 0
  --i-know-it-loses
```

**설계 원칙: 나쁜 그룹은 차단이 아니라 비중 축소.** 차단은 원칙 1(시간당 14건 하한)을
바로 깨뜨린다. 현재 '차단'하는 규칙은 `--max-same-side` 하나뿐이다.

`bot/exchange.py` 에 상장폐지 예정 심볼 제외(`DELIST_EXCLUDE_DAYS=30`) 추가 — 공유 파일이라 메인 봇에도 적용된다.

## 원칙 0 대조

```
CM HullMA20 방향/교차   O   (--cm-atype 4)
15분봉                  X   3분봉 사용 — 8/21 사용자 승인, 근거 기록됨
4시간 EMA200 필터        X   제거 — 실운영에서 스킵의 85%를 먹어 거래를 2건까지 죽였다
눌림 대기형 체결          O
```

상위 TF 필터는 코드에 남아 있고 기본 off(`--cm-htf-filter`). 장세가 바뀌면 재검토 대상.

## 임계값 상호관계 (예민도)

```
min_risk_pct 0.35(가격%)  = ROE 1.75%   진입 하한(구). stop_widen 0.65% 보다 느슨해 사문화
stop_widen_pct 0.65(가격%) = ROE 3.25%   손절선을 이 거리까지 밀어냄(진입은 막지 않음)
adopt_max_stop_roe 5      -> 재시작 채택분에만 상한
new_max_stop_roe 0        -> 신규 진입 상한 없음 (사용자 승인, 3절 참조)
min_entry_edge_pct 0.3    -> 미달 시 증거금 0.3배 (차단 아님)
```

## 라이브 정합성 (19:00 확인)

```
거래소 포지션 = 봇 상태파일 (완전 일치)
SL 누락 0 / TP 누락 0 / 고아 SL 0 / 고아 TP 0 / 미체결 진입주문 0 / 폐지예정 보유 0
주기오류 0 / 워치독 0 / 세 진입 경로 필수 동작 5/5 일치
```

**표시 주의**: SL 은 CONDITIONAL(포지션 행 "TP/SL" 칸), TP 는 LIMIT reduceOnly(미체결 주문 탭)다.
포지션 행만 보면 TP 가 없어 보인다 — 의도된 설계(청산이 taker 0.05% -> maker 0.02%).

---

# 2. 채택 — 무엇을 왜 넣었나

## 진입 우위 크기축소 (`--min-entry-edge-pct 0.3` / `--entry-edge-size-mult 0.3`)

**오늘 검증한 것 중 유일하게 채택 기준(심볼 반분 A/B 격차 0.01 수준)을 통과했다.**

정의: `우위 = (신호봉 종가 - 진입가) / 신호봉 종가 x 100` (SHORT 는 부호 반대).
CM 신호가 켜진 **그 봉의 실제 종가**보다 얼마나 유리하게 샀는가.

```
우위 >= 0.3%   39건 승률 87.2% 건당 +0.7242 gross/명목 +0.6228%
우위 <  0.3%   72건            건당 -0.2274

구간 교차검증 (전체 -> 우위>=0.3 적용)
  00~09:37  +0.4462%(28) -> +0.6404%(16)  O
  09:37~13  +0.5020%(21) -> +0.8309%(11)  O
  13~17:02  +0.1703%(33) -> +0.2173%(10)  O          개선 3/3

심볼 반분: A +0.6302%(13건) / B +0.6194%(26건)  격차 0.0108
```

**차단이 아니라 축소인 이유** — 사용자 제약 "시간당 14건 이상". 게이트로 쓰면
어떤 임계에서도 못 지킨다(임계 -0.3% 에서도 11.8건/h). 배율이면 거래수 100% 유지:

```
저우위 배율   거래수    합계     현행 대비
  1.0(현행)   111   +11.87    +0.00
  0.3         111   +23.33   +11.46   <- 채택. 차단 효과의 70%
  0.0(차단)    39   +28.24   +16.37   <- 거래수 -65%, 원칙 1 위반
```

**기준을 세 번 바꿔서야 나왔다** — 앞의 둘은 기각됐다:
```
1차  신호 지속 봉수(교차 대비)   중앙 1봉, 효과 없음        기각
2차  HullMA 대비 편차           눌림/추격 성적 차이 없음    기각
3차  신호봉 종가 대비 우위        승률 87.2%                채택
```
**평활선(HullMA)이 아니라 "신호가 켜진 순간의 실제 가격"이 옳은 기준이었다.**
기존 눌림 판정(EMA5 터치)은 추세장에서 현재가를 바짝 따라가 사실상 시장가 진입이 된다
(진입가-EMA5 편차 중앙 +0.084%). 이것이 사용자가 계속 지적한 "추격매매"의 실체다.

원칙 0 **강화**(눌림 대기형 체결을 실제로 구현) / 원칙 1 **무해**(거래수 불변) /
원칙 2 **입증**(3/3 구간, 격차 0.0108).

## HullMA20 (`--cm-atype 4`) — 오늘 최대 발견

`--cm-atype` 기본값이 **1=SMA** 였다. 원칙 0 은 HullMA20 을 요구하는데 SMA20 으로 돌고 있었다.
3분봉 SMA20 의 래그는 약 9.5봉 = **28분**, HullMA 는 거의 0.

실제 진입 312건 시점에 두 MA 신호를 재계산해 대조:

```
일치  (HullMA 도 같은 방향)    93건 승률 72.0% 건당 +0.2270 t=+1.94 합계 +21.11
불일치 (HullMA 는 반대/무신호)  219건 승률 44.3% 건당 -0.2275 t=-2.91 합계 -49.81
   내역: HullMA 무신호 167건 / 정반대 52건
신호율: SMA 79.1% -> HullMA 68.8% (-13%)
```

**오늘 하루의 다른 발견이 전부 이것 하나로 설명된다.** 넣었던 필터들이 같은 말을 하고 있었다 —
눌림 깊을수록 나쁘다 / CM 목표가 지나간 자리는 나쁘다 / 그 방향 첫 진입이 나쁘다 /
TP목표 ROE 멀수록 나쁘다(r=-0.261). **전부 "이미 크게 움직인 자리"** 이고 원인은 느린 MA 였다.
증상을 넷이나 막고 있었는데 원인은 인자 하나였다.

**한계**: "일치 93건 +21.11" 은 실제 HullMA 운영 성적이 아니다. SMA 가 잡은 것 중 HullMA 도
동의한 **부분집합**일 뿐이고, HullMA 로 바꾸면 SMA 가 놓친 새 진입도 생긴다. 상한도 하한도 아니다.

## 손절폭 하한 (`--stop-widen-pct 0.65`)

**`--min-stop-roe`(진입 거부)에서 `--stop-widen-pct`(손절선만 밀어냄)로 바꿨다.**
같은 문제를 다루지만 전자는 거래수 -55%, 후자는 거래수 불변이다(원칙 1).

```
하한(가격%)  =ROE   영향받는 손절   추가손실(확실)   승자 MAE 초과
   0.65     3.2%   96건(48%)     -24.80        2건
   0.80     4.0%  128건(63%)     -46.93        2건
```
**주의: '승자 MAE 초과'가 어느 값에서도 2건뿐이다.** 이긴 거래의 MAE 중앙 1.00%,
p90 2.45% 로 승자는 손절선 근처에 가지 않는다 — 넓혀서 살릴 승자가 거의 없다.
"잘렸지만 넓혔으면 회복했을 거래"는 원장에 없어 편익은 측정 불가다.
가장 작은 값(0.65)을 택한 이유가 이것이다. **효과가 확인되지 않으면 뺄 것.**

### 참고: 원래 근거였던 손절폭 구간별 성적

진입 시점 EMA25 손절선까지의 ROE 로 가르면 **좁을수록 나쁘다**:

```
0~2%    65건 승률40.0% 건당-0.1367 STOP률60%
2~4%   139건 승률41.0% 건당-0.2517 STOP률60%   <- 전체 손실의 57%
4~6%    80건 승률59.2% 건당-0.0942 STOP률43%
6~9%    48건 승률60.4% 건당-0.1150 STOP률40%
9%+     33건 승률57.7% 건당-0.2045 STOP률42%
```

0~4% 구간 204건이 -43.88 로 **손실의 71%**. 좁은 손절은 노이즈에 털린다.

임계값은 **최적화하지 말 것**. 3.0~4.5 스윕 결과 임계 간 차이(-0.033~-0.083)보다
**심볼 반분 A/B 격차(0.36~0.49)가 10배 크다.** 비단조이기도 하다(3.5 상승, 3.7 하락, 4.0 상승).
채택 기준이 됐던 TP 재배치의 A/B 격차가 0.01 이었던 것과 비교하면 35~49배다.
3.5 를 택한 것은 "모든 값이 통계적으로 동일하니 거래를 더 남기는 쪽"이라는 이유뿐이다.

**체결가 기준 재검증도 함께 넣었다.** 게이트는 계획 진입가로 검사하는데 지정가는 나중에
다른 가격에 체결된다(LONG 은 목표가 이하). 체결가가 손절선에 가까워지면 실제 손절폭이 줄어
SL 등록 시점에 이미 지나간 상태가 되고 `-2021` 즉시청산이 난다.
그래서 체결 시점에 체결가 기준 `min_stop_roe` 미만이면 그 거리까지 넓힌다. 진입 여부는 불변.

## 동일방향 3개 제한 (`--max-same-side 3`)

원장 168건 실측: 같은방향 0~2개 보유 중 진입 130건 승률 53.1% 건당 -0.113 vs
3개 이상 38건 승률 36.8% 건당 -0.423. **거래의 23%가 손실의 52%.**
롱에도 건다 — 그 원장 구간이 하락장이라 롱이 몰릴 기회가 없었을 뿐이다.
미체결 진입주문도 센다(안 그러면 주문만 5개 나가고 체결 뒤에 편중이 드러난다).

## CM 지정가 TP 재배치 (`--cm-tp-pullback-pct 0.5`, `--cm-tp-max-roe` 6 -> 4)

사용자 제안에서 나왔다. 캐시 A/B 교차검증 결과 앞당김 0.5% + 상한 ROE 6% 조합이
A +1.05% / B +1.03%(격차 0.01)로 **오늘 잰 것 중 가장 견고**했다. 거래수 불변이라 원칙 1 무관.
근거의 뿌리는 실원장 — 등록 TP 목표 ROE 와 성적의 상관 r=-0.261(t=-2.67, n=100),
목표 ROE 8% 이상 8건은 승률 0%, 건당 -1.611. 먼 목표는 "많이 벌 자리"가 아니라
"도달 못 하고 되돌아올 자리"였다.

**저녁에 상한을 6 -> 4 로 더 조였다.** 목표 ROE 밴드별 실측:
```
0~1%     66건 TP체결86% 건당-0.0062
1~2%     54건 TP체결59% 건당-0.2044
2~4%     58건 TP체결64% 건당+0.1844   <- 유일한 플러스
4~5.9%   28건 TP체결36% 건당-0.3630
5.9~6%   55건 TP체결29% 건당-0.5372   <- 상한에 걸린 건이 최악
```
상한을 8~10 으로 **완화하면 더 나빠진다**(가장 나쁜 그룹의 목표를 더 멀리 둔다).
BB/RR 폴링 익절 단독도 기각 — 손익비는 1.18 로 좋지만 승률이 36.9% 로 무너져
건당 -0.2842(CM TP 는 -0.1581), 구간 교차검증 4/5 에서 CM TP 우세.

**TP 폴백**: CM 익절선이 무효면 종전엔 지정가 TP 를 아예 안 걸고 폴링 익절에만 맡겼다.
거래소 TP 가 없으면 (1) 청산이 taker, (2) 폴링 10초 갭, (3) 봇이 죽으면 익절이 사라진다.
그래서 세 경로 전부에서 손익비 TP(`tp_rr`)로 폴백해 **어떤 경우에도 거래소 지정가 TP 가 있게** 했다.

## 상장폐지 예정 심볼 제외

`status=="TRADING"` 만 보던 필터는 폐지 예정 심볼을 못 거른다(정산 직전까지 TRADING 이다).
구분은 `deliveryDate` — 정상 무기한은 전부 4133404800000(2100-12-25) 센티넬이고
폐지 예정에만 실제 정산 시각이 들어간다. 정산되면 바이낸스가 **마크가격으로 강제 청산**하므로
봇의 SL/TP 가 아예 작동하지 않는다. 거래수 영향 85개 중 1개(-1.2%).

---

# 3. 기각 — 무엇을 왜 뺐나 (재검증 방지)

**새 아이디어가 나오면 여기부터 확인할 것.**

## 손절선(SL)을 뒤로 빼기 — 기각

원장 159건(MAE/MFE 추적분). **손절 90건의 MFE 중앙값이 0.00%, 73%가 MFE<1.0%** —
진입 직후부터 한 번도 유리하게 간 적이 없다. 방향이 틀린 것이라 넓히면 더 크게 잃을 뿐이다.
반대쪽도 확인: 승자 74건의 MAE 중앙 0.31% / p90 2.04% 로 **승자의 90%는 손절선(중앙 -3.04%)
근처에도 안 간다.** 손절폭 +1%p 확대 비용 -18.58 vs 편익 상한근사 +0.14.

**모니터링 포인트**: 손절의 `MFE<1.0%` 비중이 **50% 아래로 내려가면** 손실 성격이
"방향오류"에서 "되돌림"으로 바뀐 것이므로 SL 축을 다시 잰다. 73% 유지면 재론 금지.
`MFE 3.0%+` 그룹(9건 건당 +0.446)이 마이너스로 돌아서면 그건 익절 문제지 손절 문제가 아니다.

**한계**: "잘렸지만 넓혔으면 회복했을 거래"는 원장에 없다. 편익은 측정이 아니라 상한 근사다.

## 손절폭 상한 — 기각 (안 1 이 실패한 진짜 이유)

어떤 상한값도 잔존 건당을 개선하지 못했다(4% -0.2151 / 6% -0.1823 / 9% -0.1724 vs 현행 -0.1748).
상한은 성적 좋은 4%+ 그룹을 자른다. `-2021` 은 증상이었고 **방향 자체가 틀렸다.**
`--new-max-stop-roe 0` 유지가 맞다. 되살리지 말 것.

## 시간 손절 (`--max-hold-sec`) — 기각

`CODEX_HANDOFF_max_hold_sec.md` — 85심볼 10일치 **1초봉**으로 "300초 시점의 실제 가격"을
직접 재서 검증했다. 어떤 값에서도 도움이 되지 않았고 **짧게 자를수록 단조롭게 나빠진다.**
현재 원장에도 TIME_STOP 청산이 **0건**이다(꺼져 있다).

## 사이징 축소로 나쁜 그룹 다루기 — 기각

3개+ 동일방향에 배율 적용 시뮬(190건): 0.25배 -21.73 / 0.50배 -26.90 / 1.0배 -37.24 vs
차단(0배) -16.56. **집단 기대값이 -0.46 이라 선형 축소로는 흑자로 뒤집히지 않는다.**
0.25 는 건당이 같고 거래수 +31% 라 원칙 1 만 보면 공짜지만 총순익이 나빠져 원칙 2 위반.

## 눌림 상한 (`--max-pullback-pct`) — 제거

채택 근거였던 캐시 132건 A/B 가 실원장 355건에서 뒤집혔다.
**단독으로도 역효과** — 유지 그룹 건당이 기준선(-0.1768)보다 나쁜 -0.2131(t=-3.69)이고,
걸러낸 71건의 합계가 -2.26 뿐이다. 아무것도 못 거르면서 좋은 것을 잘랐다.

## CM 익절선 무효 차단 (`--require-cm-tp`) — 제거

단독으로는 유효해 보였으나(229건 건당 -0.1111), 손절폭+동일방향 조합에 얹으면
건당 +0.0367 에서 **-0.0197 로 악화**한다. 손절폭이 이미 담고 있는 정보를 중복으로 거른다.

## 상위추세(4h EMA200) 역행 필터 — 제거

원장에서는 명확했다: 역행 169건 건당 -0.2393(t=-2.71), **전체 손실의 76%**.
그런데 **실운영에서 스킵의 85%(320/376)를 먹으며 거래를 2건까지 죽였다.**

> **교훈: "필터의 과거 통과율"과 "현재 장세에서의 통과율"은 다르다.**
> 원장은 "과거 거래 중 역행이 51%"를 알려주지, "지금 장세에 정합 후보가 몇 개인가"는
> 알려주지 않는다. 4h 추세와 3분봉 신호가 반대인 국면에선 정합 후보가 거의 없어진다.
> **진입 필터를 새로 넣으면 배포 직후 스킵 구성을 반드시 확인할 것.**

이격 임계로 역행을 세분하려 했으나 **역행의 86%가 이미 EMA200 에서 10% 이상 떨어져 있어**
구분되지 않았다(4h EMA200 이 워낙 느린 선이라 그렇다).

## 손절폭 하한을 **진입 거부**로 쓰기 (`--min-stop-roe`) — 제거

구간 교차검증에서 규칙을 얹을수록 좋은 구간과 나쁜 구간이 **같이 증폭**됐다:
```
규칙셋                          악화구간  유지
① 신호만                          -    386건
② +동일방향<3                      1    318건   <- 유일하게 악화 1(노이즈 수준)
③ +동일방향+손절폭>=3.5             2    172건
⑤ +동일방향+손절폭+CM TP            3    120건   <- 좋은 구간 최고, 나쁜 구간 최악
```
전형적인 과적합이다. 그리고 임계값 스윕(3.0~4.5)에서 **임계 간 차이(-0.033~-0.083)보다
심볼 A/B 격차(0.36~0.49)가 10배 컸다.** 소숫점 최적화는 무의미하다.
-> 같은 문제를 `--stop-widen-pct`(거래수 불변)로 다룬다.

## 진입 우위를 **게이트**로 쓰기 — 기각(배율로 전환)

우위 임계 -0.3% 에서도 통과율 70% -> 11.8건/h 로 사용자 하한(14건/h)을 못 지킨다.
어떤 임계도 원칙 1 을 위반한다. -> 차단 대신 0.3배 축소로 전환(2절 참조).

## 심볼 단위 개입 (블락/상한) — 기각

심볼당 일일 상한: 3건 -0.1604 / 5건 -0.1720 / 10건 -0.1638 vs 제한없음 -0.1606.
**건당이 전혀 개선되지 않는다.** 연속 손실 차단도 2연속 -0.1964 / 3연속 -0.1647 /
4연속 -0.1698 로 전부 기준선(-0.1606)보다 나쁘거나 같다.
많이 거래한 심볼이 나쁜 게 아니라 좋은 것과 나쁜 것이 섞여 있다(ONG 24건 +1.32 vs ZEC 17건 -7.25).
지속성 검증도 11개 심볼로 부호일치 73%, r=+0.370 — **유의하지 않다.**

## 그 밖에

- **TP 목표 ROE 2% 미만 진입 포기** — 캐시에선 좋아 보였으나 실원장이 정반대(r=-0.261).
- **사이클 내 후보 스코어링/정렬** — 사이클당 평균 후보가 2.4개뿐이라 경쟁 자체가 없다.
  엄선할수록 나빠짐(K=1 -0.55% / K=6 -0.33%). 대신 절대 임계값이 답이었다.
- **손절 하한 ROE 3%(안 3)** — 오전 판정에서 기각. 다만 그때는 "하한으로 새로 열리는 거래"를
  잰 것이라 각도가 다르다. 지금의 `--min-stop-roe` 와 혼동하지 말 것.
- **CM 목표 ROE 밴드 필터** — 구간을 바꾸면 부호가 뒤집힌다(전체 기준 4%+ -0.4808 t=-2.10 vs
  ④ 배포 후 기준 +0.1503 t=+0.56). 명백한 과적합.
- **빠른 익절(무조건 T% 캡)** — 손절건 전환 이득보다 익절건 깎임이 커서 모든 T 에서 마이너스
  (T=0.5% -3.73 / 1.0% -2.73 / 2.0% -6.40).

---

# 4. 사고 기록 — 무엇이 터졌고 어떻게 막았나

## 오전 P0 3건 — 전부 "손절 없이 포지션을 들고 있었다"

**A. 안 1(신규 손절폭 ROE 5% 상한)이 `-2021` 을 유발** — 손절선을 진입가 쪽으로 당기니
지정가 체결 직후 SL 등록이 `-2021 Order would immediately trigger` 로 거부됐다.

```
SL 등록 실패(-2021)   배포 전 0건/31진입 -> 배포 후 16건/107진입(15%)
손절 -6% 초과 비율    6% -> 18% (최악 -18.76%, 24초 만에 -10.24%)
그 9건 합계 -20.80 = 밤샘 손실 -60.53 의 34%
```

`--new-max-stop-roe 0` 원복(사용자 승인). 채택 경로(`--adopt-max-stop-roe 5`)는 유지.

**B. 체결된 진입주문이 재시작으로 증발 -> 고아 무보호 포지션** — `taskkill /F` 는 atexit 을
건너뛰어 진입 지정가가 살아남고, 재시작 뒤 체결되는데 새 프로세스는 그 주문을 모른다.
진입주문 전체(order_id/side/stop)를 상태파일에 보존하고 기동 시 복원하도록 고쳤다.

**C. 봇이 12분간 freeze — REST 타임아웃이 없었다** — `Client(...)` 에 timeout 이 없어
커넥션이 끊긴 채 응답이 안 오면 영원히 블록된다. 죽지 않으니 아무도 되살리지 않았다.
3겹 방어: REST 타임아웃 10초 / 미추적 포지션 안전망 75초 / 메인 루프 워치독 180초.

## `포기` 판정이 취소 성공을 확인하지 않았다

`_status2` 를 대입만 하고 쓰지 않아 **취소가 실패해도 추적을 끊었다.** 그 주문이 몇 시간 뒤
체결돼 무보호 고아가 됐다(龙虾USDT 09:41 포기 -> 12:14 체결, 2시간 33분 뒤).
오늘 `포기` 28건이 전부 같은 위험이었다.
이제 `CANCELED`/`EXPIRED`/`REJECTED` 확인 시에만 추적을 끊는다.

## 체결 확인이 안전망에 경쟁 패배

```
체결 확인 루프 -> 메인 사이클 (분 정렬로 1분에 1회)
안전망         -> 정렬 대기 루프 (15초마다)
```

경쟁이 안 된다. 13:30 이후 포지션 유입의 **93%(13/14)가 안전망 경로**였고, 그동안
TTL+30초(75초)를 무보호로 기다렸다(BMTUSDT 발견 시 이미 ROE -10.11%).
안전망이 "거래소 포지션 = 체결의 증거"로 보고 유예 없이 가져오되, 주문에 담긴
진입 맥락(stop/legs/since_id)을 살리도록 고쳤다. 보호 지연 75초+ 에서 15초로.

## WS 신선도 기준이 봉 주기보다 짧았다

`ws_kline_max_staleness_sec = 90.0` 고정인데 `is_fresh()` 가 보는 `_last_update_ts` 는
**봉 갱신 시각**이다. 신호봉이 3분이면 그 간격이 180초 — **매 봉 주기의 절반 동안 모든 심볼이
"낡음"** 으로 판정돼 85심볼 전부 REST 폴백(IP밴 위험)했고, 기동 시엔 캐너리 0/10 으로
"강제 진행" 경고를 내며 REST 로 운영됐다. 재시작 성공/실패가 반반이었던 것도 랜덤이 아니라
확인 시점이 봉 주기의 앞쪽이냐 뒤쪽이냐로 갈린 것이다.
실측(14:04 덤프): 85심볼 전부 99봉, 메시지 4651/60초, 에러 0 — **캐시는 정상, 판정만 틀렸다.**
`max(90, signal_tf_min*60 + 120)` (3분봉이면 300초)로 고쳤다. WS 사망 감지는 메시지 흐름
기준이라 봉 주기와 분리해 60초로 따로 잡았다.

## 안 B(swept 제외)가 방향편중 캡에 3시간짜리 구멍을 냈다

```
09:37~12:28 (안B 전)     44건 중 동일방향3개+ 0건 (0%)
12:28~15:45 (안B 적용)   91건 중 23건 (25%)  <- swept 를 카운트에서 빼서 4번째가 통과
15:45~ (swept=not _ours)  19건 중 0건 (0%)   <- 닫힘
```

그 3시간에 23건 -5.56 이 샜다. 안 B 자체가 틀린 게 아니라 **안전망 채택분을 전부 swept 로
찍은 것**이 문제였다. 추가로 `swept` 표식이 상태파일에 없어 **재시작마다 초기화**되던 것도 고쳤다.

## 저녁 — 사용자가 화면에서 발견한 3건

로그는 정상으로 보였다. **거래소 UI 에서만 보이는 것이 있다.**

1. **TP 미등록** — 절반은 표시 문제(SL 은 포지션 행, TP 는 미체결 주문 탭), 절반은 진짜
   (세 경로 중 두 곳에 손익비 폴백이 없었다).
2. **부분체결 잔량 미취소 -> 유령 추가 진입** — VVVUSDT 25.48 주문에서 11.46 부분체결,
   안전망이 채택하며 추적 종료, 잔량 14.02 가 살아남아 나중에 체결(reduceOnly 아님 = 포지션 증가).
   정상 체결 경로에는 이 방어가 **원래 있었다.**
3. **진입 3초 만에 컷** — 체결가 기준 손절폭 미검증(2절 참조). 즉시청산 8건 -8.79 중
   진입 3~6초 컷 3건이 -6.31.

## 싱크버그 — 원복시킨 안 1 이 뒷문으로 부활해 있었다

```
1948행  재시작 채택  -> adopt_max_stop_roe (5)   O
2852행  안전망      -> adopt_max_stop_roe (5)   X  봇 자기 진입에도 적용
3585행  정상 진입    -> new_max_stop_roe   (0)   O
```

안전망이 주 경로(93%)가 되면서 사용자가 원복시킨 안 1 이 **채택용 상한 5% 로 사실상 부활**해
있었다. 상한은 손절선을 진입가 쪽으로 당기므로 `-2021` 의 직접 원인이다.
`_cap = new_max_stop_roe if _ours else adopt_max_stop_roe` 로 경로별 분리했다.

## 그 밖에 고친 것

- **`skips["손절선통과"]` KeyError** — 초기화 dict 에 키가 없어 그 분기를 타면 KeyError 가 났고
  바깥 `except Exception` 이 삼켜서 **그 사이클의 남은 심볼 전체가 건너뛰어졌다**(그 분엔 진입 0건).
  오류가 `print()` 로만 나가 런로그에 흔적조차 없었다. defaultdict + 주기오류를 `log_line()` 으로도.
- **지정가 TP 체결이 `STOP_EXCHANGE` 로 기록** — 외부청산이 전부 손절로 찍혀 손절률은 부풀고
  TP 체결률은 잴 수 없었다. 주문 상태를 조회해 `FILLED` 면 `TP_LIMIT` 으로 기록한다.
- **`signal_bars` 중복 호출** — 호출부와 `cm_signal_snapshot` 이 같은 신호봉을 두 번 만들어
  85심볼 1회전이 2배로 느렸다. 이것이 "스캔 뒷줄 심볼 구조적 탈락"의 원인이었다.
  실측: 신호노후 스킵 13%(25/194)에서 **1.4%(4/280)** 로.
- **`근접손절` 계측 분리** — 봉 기준 / 마크가격 기준 두 분기가 같은 키로 집계됐다.
  분리해보니 마크가격 분기는 280건 중 2건뿐 — 안 3 판정에 기존 수치를 그대로 써도 된다.
- **`roe_marks` 계측 추가** — 경과 60/120/180/300/420/600/900/1200초 시점 ROE 를 원장에 남긴다.
  "애매한 거래를 언제 끊어야 하나"를 추정이 아니라 측정으로 답하기 위함. 아직 판정 전.

---

## 손절선/TP 결정 로직 통합 (21:20~21:38)

세 진입 경로(재시작채택 / 안전망 / 정상체결)에 같은 계산이 복제돼 있어 **오늘만 여섯 번**
같은 유형의 사고가 났다. 사고가 난 자리는 전부 "손절선 결정"과 "TP 결정"이었다.

```python
def decide_stop(entry, side, lev, cap_roe, stop_hint=0.0, ind=None) -> float:
    """기준선 -> 하한(넓힘) -> 방향검증 -> 상한(자름). 순서가 중요하다."""

def attach_tp(pos, entry, lev, ind=None, cm_target=0.0, label="") -> None:
    """CM 최대익절선 -> 상한 -> (무효면) 손익비 폴백 -> 상한 -> 등록."""
```

```
              통합 전   통합 후
widened_stop     4곳  ->  1곳 (decide_stop 안)
cap_stop_roe     4곳  ->  1곳
cap_tp_roe       4곳  ->  1곳 (attach_tp 안)

decide_stop  4경로 호출 (재시작채택 / 안전망 / 스캔발주 / 정상체결)
attach_tp    3경로 호출
```

**`cap_roe` 에 기본값을 두지 않았다.** 경로마다 다른 값(신규 `--new-max-stop-roe` 0 /
채택 `--adopt-max-stop-roe` 5)을 써야 하는데, 그 구분을 놓쳐 사용자가 원복시킨 안 1 이
부활했던 자리다. 호출부가 반드시 명시하게 강제한다.

**정상 체결 경로에도 체결가 기준 재확정을 추가**했다 — 종전엔 안전망에만 있어서
계획가와 체결가가 다르면 손절폭이 어긋났다.

**전체 `register_position` 으로 합치지는 않았다.** SL 등록 방식(① place_stop_market +
기존 SL 사전취소 vs ②③ sync_stop), 부분체결 잔량취소(②), pending 갱신(③),
진입시각 복원(①)은 경로마다 **진짜로 다르다.** 사고가 난 적 없는 부분까지 옮기면
위험만 커진다. 필요해지면 그때 별도 작업으로 한다.

## 그 통합이 봇을 12분 죽였다 (21:19~21:31)

```
21:19:08  시작 [실주문] ...        <- NameError 로 즉사, 로그 3줄만 남김
          12분 공백 (채택 요약도 WS 준비도 없음)
21:31:33  수동 재기동
```

**원인**: 헬퍼를 2660행(`close_breached` 앞)에 정의했는데 **재시작 채택 루프는 2010행**
에서 돈다. 파이썬 중첩 함수는 `def` 문이 실행돼야 존재하므로 채택 루프 시점엔 헬퍼가
없어 `NameError` 가 났다. -> 헬퍼를 1951/1977행으로 옮겨 채택 루프(2014행) 앞에 뒀고,
AST 로 main() 내 모든 중첩함수의 정의/호출 순서를 검사해 다른 위반이 없음을 확인했다.

**검증이 놓친 이유 두 가지 — 둘 다 재발 방지 대상**:
1. **테스트 1,008건은 통과했다.** `main()` 을 실행하지 않으므로 중첩 함수 정의 순서를
   구조적으로 못 잡는다. 단위 테스트로는 영원히 안 잡히는 유형이다.
2. **배포 후 확인을 거래소 상태와 로그 tail 로 했다.** 거래소는 정상이었고 로그 tail 은
   **이전 pid 의 줄**이라, 새 프로세스가 3줄만 남기고 죽은 것을 못 봤다.
   -> **배포 검증은 "새 pid 가 `WS 준비 완료` 까지 로그를 남겼는가"로 해야 한다.**
   -> 이후 배포부터 `-RedirectStandardError logs\e3_crash.log` 로 크래시를 남긴다.

## 오늘 넣은 방어가 실제로 작동한 기록

```
20:09:01  경고 수동 개입 체결 6건 제외 PORTALUSDT - 봇 성과에서 분리
21:09~    체결가 기준 손절폭 부족 ... 4건 발동 (죽어 있던 로직이 살아남)
21:36:34  수량 보정 BTRUSDT(LONG): 2137.0 -> 772.0 (거래소 정본)
```

---

# 5. 방법론 경고 (반드시 읽을 것)

- **캐시(`logs/ws_worker_cache*.json`)는 5.5시간·43심볼뿐이다.** 큰 추세 역행이 없어
  "손절을 안 할수록 좋다" 같은 결론이 나온다. **손절폭·보유시간처럼 꼬리 위험이 걸린 축은
  이 데이터로 판정하면 안 된다.** 실제로 캐시만 믿었다가 뒤집힌 게 오늘만 4건이다
  (TP 하한 / 후보 스코어링 / 20초 게이트 / 눌림 상한).

- **임계값을 고를 땐 심볼 반분 교차검증을 반드시 할 것. 점 추정치가 높은 쪽이 아니라
  A/B 격차가 작은 쪽을 택한다.** 참고 기준: 채택된 TP 재배치의 격차가 **0.01**,
  기각된 `min_stop_roe` 소숫점 스윕의 격차가 **0.36~0.49**.

- **원장으로 확인 불가능하면 "확인 불가"라고 명시하고 추정을 사실처럼 쓰지 말 것.**
  `entered_at` 은 주문 시각이 아니라 체결 확인 시각이라 게이트 품질을 영원히 못 잰다.

- **t 값을 결론에 반영할 것.** 오늘 유의했던(|t|>2) 것은 **전부 손실 방향**이었고,
  플러스로 나온 구간은 한 번도 유의한 적이 없다(t=+0.19 / +0.31 / +0.36 / +0.45).
  "수익 전환됐다", "크기를 올려도 된다" 는 판단이 전부 이 약한 t 위에 세워졌다가 뒤집혔다.

- **수수료 전 gross 엣지가 거의 0 이다.** 규칙 튜닝은 적자를 줄이지 흑자로 바꾸지 못한다.
  흑자로 가려면 **신호 자체를 바꾸는 실험**이 필요하다. 오늘 HullMA 수정이 그 유일한 예다.

- **측정 기준을 바꿔보기 전에 기각하지 말 것.** "추격매매" 가설을 두 번 기각했다가
  세 번째 기준에서 승률 87.2% 로 나왔다. 평활선(HullMA) 대비가 아니라 **신호가 켜진
  순간의 실제 가격** 대비가 옳은 기준이었다. 같은 가설도 기준이 틀리면 안 보인다.

- **"차단"보다 "비중 축소"를 먼저 검토할 것.** 차단은 원칙 1 을 즉시 깨뜨린다.
  오늘 상위추세 필터를 차단으로 넣었다가 스킵의 85%를 먹으며 거래가 2건까지 죽었고,
  진입우위 규칙도 차단이면 14건/h 하한을 못 지켰다. 배율은 거래수를 보존하면서
  차단 효과의 70% 를 준다. 전례: `SHORT_REVERSAL_RISK_SIZE_MULT 0.65`.

- **필터의 과거 통과율은 현재 장세 통과율과 다르다.** 진입 필터를 넣으면 배포 직후 스킵 구성을 확인할 것.

- **경로를 대체할 때는 기존 경로가 하는 일을 전부 목록화해 대조할 것.**
  e3 의 진입 경로는 셋이다(정상체결 / 안전망 / 재시작채택). 대조 항목:
  잔량취소 / `n_entry` / `sync_stop` / `sync_tp_limit` / 손익비 폴백 / 경로별 손절폭 상한.
  오늘 이걸 안 해서 **같은 유형의 버그가 네 번** 났다(동기 REST 정지 / n_entry 누락 /
  잔량 미취소 / 손절폭 상한 혼용).

- **중첩 함수는 정의가 사용보다 앞서야 한다.** `main()` 안의 헬퍼는 `def` 문이 실행돼야
  존재한다. 기동 시퀀스(재시작 채택 등)에서 쓰는 헬퍼를 뒤쪽에 정의하면 `NameError` 로
  즉사한다. **단위 테스트는 `main()` 을 안 돌리므로 이 유형을 구조적으로 못 잡는다.**

- **배포 검증은 "새 pid 가 `WS 준비 완료` 까지 로그를 남겼는가"로 할 것.**
  거래소 상태나 로그 tail 은 이전 프로세스의 것일 수 있어 새 프로세스의 즉사를 가린다.
  실사고: 21:19 배포본이 3줄 남기고 죽었는데 12분 뒤에야 발견했다.

- **스캔 루프에 네트워크 호출을 추가하면 심볼 수를 곱해서 검증할 것.**
  단위 테스트는 3심볼로 0.2초에 끝나 85심볼 누적 지연이 재현되지 않는다.
  워치독도 이 유형을 못 잡는다 — 정렬 대기 루프의 `beat()` 가 메인 사이클 정지를 가린다(미해결).

---

# 6. 사용자 결정 사항 (지킬 것)

- **원복 조건이 충족돼도 임의로 되돌리지 말 것.** 알림만 하고 결정을 요청한다.
- **조기청산(`--early-adverse-sec` / `--mae-cut-roe`) off 는 의도적이다. 켜자고 반복 제안하지 말 것.**
  손실이 `MFE<1.0%` 66건(-73.88)에 몰려 있어 데이터가 그쪽을 가리키지만, 제안하지 않는다.
- 지정가 TP 는 체결률이 원복 조건(60%)에 걸렸으나 **사용자가 유지를 승인**했다.
  미체결의 대부분이 "TP 가 안 붙어서"가 아니라 **손절이 먼저 나서**다.
- **BNB 수수료 할인은 보류.** 잔고가 커지면 이동(지금 옮기면 증거금이 줄어 슬롯이 깎인다).
- 지속 하락 / 버그 / 봇 멈춤이 아니면 사용자가 추가 업데이트를 요청하지 않는다.

---

# 7. 다음 확인 항목

## 21:00 판정 (판정 기준 미리 고정, 즉흥 판단 금지)

스크립트: 스크래치패드 `report_1900.py`

1. **시간당 진입수** — 원칙 1 이 실제로 지켜지는지. **이것만이 원칙 1 판정의 근거다.**
   참고: 피크 28.8건/h(13~14시) / 정리 후 16.2건/h / 동시보유 = 진입율 x 평균보유 / 60.
2. **`손절폭부족` 스킵 비중** — 하한 3.5 가 실제로 얼마나 거르는지.
3. **HullMA 전환 후 성적** — 비교 대상은 SMA 구간(14:42~17:02) 59건 건당 -0.1420.
   판정: n>=40 이고 건당이 개선되면 전환 성공.

## 상시 관찰

- **손절의 `MFE<1.0%` 비중** — 현재 73%. 50% 아래로 내려가면 SL 축을 다시 잰다.
- **`MFE 3.0%+` 그룹** — 현재 9건 건당 +0.446(유일한 플러스). 마이너스로 돌아서면 익절 문제다.
- **롱/숏 비대칭 캡** — `--max-same-side` 근거는 하락장에서만 검증됐다.
  3개+ 45건 중 42건이 SHORT, LONG 은 3건뿐. **LONG 3개+ 표본이 20건 이상 쌓이면**
  롱만 상한을 4~5로 완화할지 판정한다. 그 전까지 현행 대칭 캡 유지.
- **`roe_marks` 2단계(애매한 거래 조기익절)** — 진행 조건 셋 다 충족 시:
  되돌림 손실 10건 이상 / 어떤 (N분, X%) 조합에서 개선 +3.0 이상 / 그 조합에서 큰승자 깎임 3건 미만.
  `bot/config.py` 에 `AMBIGUOUS_QUICK_PROFIT_EXIT_*` 파라미터가 이미 있으나 e3 에는 없다.
  기본값(60~180초)은 메인 봇 1분봉 기준이라 e3(보유 중앙 4~8분)에는 안 맞을 가능성이 크다.

## 계측 부채 (없으면 영원히 추정으로만 답하게 되는 것)

- `ordered_at` — 주문 발주 시각. 없으면 신호 게이트 품질을 못 잰다.
- 손절 후 N분 시점 가격 — 없으면 "넓혔으면 회복했을까"를 측정으로 못 답한다.
- 워치독 하트비트 분리 — "메인 사이클 완주"와 "정렬 대기 중"을 구분해야 정지를 잡는다.
