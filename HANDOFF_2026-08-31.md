# 핸드오프 2026-08-31 — 방향 신호 재측정 (3분봉 30일 + 1시간봉 180일)

전날 핸드오프: [HANDOFF_2026-08-28.md](HANDOFF_2026-08-28.md)

자산 소진으로 라이브를 세운 상태에서, 08-28 이 "다음 단계"로 남긴
**방향 신호 자체의 교체** 를 측정했다. 결론 네 줄.

- **문제는 신호가 아니라 시간대다.** 손익분기 보유시간이 **8~12시간**인데
  라이브 e3 는 12분을 들고 있었다. 비용이 엣지보다 한 자릿수 크다.
  **이것이 "왜 계속 잃는가"의 답이다** (7장).
- **3분봉/15분 구간에는 어떤 신호도 비용선을 못 넘는다.** CM 도 메인봇 뼈대도 음수다.
  08-28 의 "+0.0106 (t+4.0)" 은 상승장 9.4일이 만든 값이었다 (1~5장).
- **CLAUDE.md 원칙 0 의 메인봇 뼈대(+0.446%)는 재현되지 않는다.** 드리프트 중립화하면
  15분 **-0.0145% (t-2.1/-2.1)** 로 부호가 반대다. 복귀 후보에서 제외한다.
- **8~24시간 보유는 한때 비용선을 넘어 보였지만, 생존편향을 걷어내니 사라졌다.**
  펀딩비는 통과했으나(8장), 같은 창에서 상장폐지된 심볼 145개를 되돌려 놓자
  24시간 엣지 중앙값이 +0.1765 -> +0.0211 로 무너지고 두 t 가 모두 죽는다(9장).
- **결론: 세 관문(펀딩·생존편향·백테스트)을 통과한 구성이 하나도 없다.**
  짧게 잡아도(15분) 길게 잡아도(24시간) 비용을 덮는 진입 엣지를 찾지 못했다.

**읽는 순서 주의: 1~5장은 3분봉 한정, 7~8장은 "긴 보유는 되는 것 같다"는 중간 상태다.
9장이 그걸 뒤집는다. 7~8장만 인용하면 틀린 결론이 된다 — 반드시 9장까지 읽을 것.**

라이브 설정·코드는 **아무것도 바꾸지 않았다.** 봇은 정지 상태이고 이 문서는 측정만 담는다.

---

# 1. 표본 — 이번에 처음으로 국면이 둘 이상 들어갔다

08-28 하네스의 가장 큰 한계는 "표본 9.4일이 전부 상승장 한 국면"이었다.
그래서 먼저 데이터를 다시 깔았다.

```
81심볼 x 15,000봉 (3분봉, 31.2일)   2026-07-31 01:51 ~ 08-31 07:48 UTC
구간1  07-31 ~ 08-10     구간2  08-10 ~ 08-20     구간3  08-20 ~ 08-31
```

- 수집기: [scripts/edge_fetch.py](scripts/edge_fetch.py). 공개 klines 전용,
  **인증 엔드포인트 0회 / API 키 미사용**, 호출 간 0.35초, 총 910 호출.
  실행 전 python 프로세스 0개(라이브 정지)를 확인했다 — 라이브와 REST 가 겹치지 않았다.
- 저장은 JSON 이 아니라 **npz(float64 배열)** 다. 30일 x 81심볼 3분봉은 JSON 이면
  파이썬 객체로 GB 단위가 된다. npz 는 24MB / 로드 즉시.
- 측정기: [scripts/edge_lab.py](scripts/edge_lab.py). `cm_signal_edge.py` 의 확장이며
  구 캐시로 대조했을 때 기존 수치를 재현한다(CM 전체 15분 +0.0102 vs 기존 +0.0106).
  HMA/WMA 는 `scalp_bot_e3._series_hma` 와 부동소수 오차(1e-14)까지 일치를 확인했다.

측정 규약은 08-28 과 같다. 신호는 봉 마감으로 확정하고 **진입은 다음 봉 시가**,
청산은 h봉 뒤 종가. lookahead 없음. 청산규칙·슬롯·수수료는 넣지 않는다.

## 이번에 추가한 판정 장치 — t 를 두 축으로 낸다

08-28 의 t 값은 **부풀려져 있었다.** 같은 시각 84심볼은 서로 독립이 아니고
(시장 전체가 같이 움직인다), 한 심볼이 급등하면 그 심볼의 신호 수백 건이
전부 같은 방향으로 맞는다. 그래서 t 를 두 개 낸다.

- **시각 클러스터 t**: 시각별 평균을 낸 뒤 시각 축에서 t. "특정 몇 시간이 다 만든 값인가".
- **심볼 클러스터 t**: 심볼별 평균들 사이의 t. "특정 몇 종목이 다 만든 값인가".
- 함께 **심볼 중앙값**을 낸다. 평균만 크고 중앙값이 0 근처면 소수 급등종목의 꼬리다.

**판정 규칙: 평균과 중앙값이 같은 부호로 붙어 있고 두 t 가 모두 살아 있어야 엣지다.**

이 장치가 실제로 결론을 뒤집었다(3장).

---

# 2. 본 측정 — 라이브 보유시간에서 CM 도 메인봇도 음수다

드리프트 중립(같은 시각 전 심볼 평균 차감), 표기는 `건당평균|심볼중앙값 (시각t/심볼t)`.
전체 표는 `logs/edge_lab_30d.txt`.

```
신호                        건/심볼일        15분                      480분
CM 전체 [현행뼈대]            308.9  -0.0069|-0.0032 (t-4.7/-2.1)  +0.0363|+0.0198 (t+5.0/+2.4)
CM + 4h정합 [라이브]          157.8  -0.0001|-0.0043 (t+1.1/-0.0)  +0.1672|+0.0068 (t+16.9/+2.2)
볼밴돌파+EMA+RSI [메인봇]       16.5  -0.0145|-0.0090 (t-2.1/-2.1)  +0.1244|+0.0163 (t+3.6/+1.9)
EMA추세 단독                 474.8  -0.0003|+0.0007 (t-0.3/-0.1)  +0.0888|+0.0210 (t+14.2/+2.2)
RSI 모멘텀 30/70              39.3  +0.0078|+0.0083 (t+1.0/+0.2)  +0.2411|+0.0191 (t+8.3/+1.8)
횡단면 역추세 5봉             200.4  +0.0171|+0.0157 (t+6.9/+3.3)  -0.0550|+0.0072 (t-4.6/-1.5)
```

라이브 e3 의 실제 보유 중앙값은 12분이므로 **15분 열이 판정 열**이다.

1. **CM 전체가 음수다** (-0.0069, 중앙값도 -0.0032, 두 t 모두 음수로 유의).
   08-28 에 +0.0106(t+4.0) 이던 값이다. 표본을 9.4일에서 31일로 늘리자 부호가 바뀌었다.
2. **메인봇 뼈대는 더 나쁘다** (-0.0145, 중앙값 -0.0090, t-2.1/-2.1).
   CLAUDE.md 원칙 0 에 적힌 "볼밴+EMA+RSI 15분 +0.446%" 는 **재현되지 않는다.**
   그 수치는 드리프트 중립화를 안 거친 값이었고, 부호까지 반대로 나왔다.
3. **긴 지평(480분)은 전부 양수지만 중앙값이 따라오지 않는다.**
   CM+4h정합이 평균 +0.1672 인데 중앙값은 +0.0068 — 24배 차이다. 소수 종목의 꼬리다.

## 부수 발견: 메인봇의 RSI 조건은 아무 일도 안 한다

`볼밴돌파+EMA+RSI` 와 `볼밴돌파+EMA` 의 **건수와 수치가 완전히 동일**하다(41,874건).
상단밴드를 상향 돌파하는데 EMA20>EMA50 이면 RSI14 는 항상 50 위다. 조건이 중복이다.
메인봇으로 돌아가는 판단을 할 일이 있으면 이 축은 없는 것으로 계산할 것.

---

# 3. 판정 장치가 실제로 뒤집은 것 — "RSI 모멘텀" 함정

측정 도중 **비용선을 넘는 것처럼 보이는 신호**가 하나 나왔다. 기록으로 남긴다.
같은 실수를 반복하지 않기 위해서다.

RSI 과매수에 롱 / 과매도에 숏(교과서의 정반대)은 9.4일 구 캐시에서 이렇게 나왔다:

```
RSI 모멘텀 20/80    120분 +0.6444% (시각 t+5.4)    240분 +0.8653% (t+5.3)
```

비용선 0.11% 의 6~8배다. 여기서 멈췄으면 채택했을 것이다. 분해하면:

```
  LONG만   +0.7476 (t+5.1)     SHORT만  +0.1624 (t+1.1)
  심볼 84개 중 34개만 양수 (40%)
  상위 BTRUSDT +36.91, VELVETUSDT +13.57, TRUMPUSDT +9.47
```

**심볼 중앙값은 -0.0452 로 음수다.** 급등한 서너 종목이 평균을 통째로 만들었고
나머지 심볼의 절반 이상은 손해다. 심볼 클러스터 t 는 +0.8 로 죽는다.
30일 표본에서 같은 신호의 중앙값은 -0.0646(120분), -0.1173(480분)으로 더 나빠진다.

**교훈: 평균과 t 만 보면 급등종목 꼬리를 엣지로 오인한다. 중앙값과 심볼 t 를 반드시 함께 볼 것.**
08-28 표의 t 값들도 이 보정을 안 거친 값이라, 그 표를 인용할 때는 감안해야 한다.

---

# 4. 국면별 재측정 — 안정적인 건 하나뿐이고 그것도 비용 미달

31일을 3등분해 같은 측정을 반복했다(`logs/edge_lab_30d_split.txt`). 15분 기준:

```
신호                     구간1              구간2              구간3
CM 전체 [현행뼈대]      -0.0197 (t-8.2)   -0.0049 (t-2.0)   +0.0038 (t+1.3)
CM + 4h정합 [라이브]    -0.0042 (t-0.8)   -0.0019 (t-0.7)   +0.0057 (t+2.5)
볼밴돌파+EMA+RSI        -0.0234 (t-1.6)   -0.0136 (t-2.1)   -0.0068 (t-0.3)
EMA추세 단독            +0.0019 (t+1.1)   -0.0037 (t-2.0)   +0.0008 (t+0.3)
횡단면 역추세 5봉        +0.0301 (t+7.3)   +0.0149 (t+3.7)   +0.0064 (t+1.3)
```

- **CM 은 3구간 중 2구간이 음수**다. 부호가 국면에 따라 바뀐다 = 방향 엣지가 아니라
  국면 베타를 타고 있었다는 뜻이다. 08-28 의 "9.4일이 전부 상승장" 경고가 실증됐다.
- **메인봇 뼈대는 3구간 전부 음수**다.
- **횡단면 역추세(5봉 약세종목 롱 / 강세종목 숏)만 3구간 전부 양수**이고
  평균과 중앙값이 붙어 있고(+0.0171|+0.0157) 두 t 가 모두 산다(t+6.9/+3.3).
  롱/숏도 균형이다(255,090 / 255,099, 둘 다 양수). 심볼 81개 중 43개 양수(53%)로
  꼬리 의존이 없다. **이 표본에서 유일하게 구조적으로 살아남은 신호다.**

## 그런데 그것도 못 쓴다

```
횡단면 역추세 5봉 15분 엣지   +0.0171%  (중앙값 +0.0157%)
왕복 비용                     0.11%     (수수료 0.07 + 슬리피지 가정 0.04)
```

**필요치의 1/6 이다.** 그리고 이건 수수료를 극단적으로 줄여도 안 된다:
진입·청산을 **둘 다 메이커로만** 체결해도 수수료만 0.04%(2 x 0.02)다.
슬리피지를 0 으로 가정해도 0.0171 < 0.04 다. **체결비용 구조 자체가 이 엣지보다 크다.**

---

# 5. 결론과 권고

## 측정이 말하는 것

- 3분봉 방향 신호로 **15분 안팎을 먹는 매매는 이 표본에서 성립하지 않는다.**
  진입 축을 바꾸든(22개 구성, 08-28), 뼈대를 바꾸든(이번 20여 구성) 결과가 같다.
  **지속적 손실의 원인은 튜닝 부족이 아니라 이 시간대에 비용을 덮을 엣지가 없는 것이다.**
- 엣지 후보가 살아나는 유일한 방향은 **보유시간을 늘리는 것**이다(480분에서 평균이
  비용선을 넘는 항목들이 나온다). 다만 그 항목들은 중앙값이 따라오지 않아
  **아직은 꼬리이지 엣지가 아니다.** 재측정 없이 장기보유로 전환하면 안 된다.
- 원칙 1(거래는 활발해야 함)과 이 결과는 정면으로 충돌한다. 빈도를 지키는 시간대에
  엣지가 없다. **원칙 1 을 지키면서 이기는 조합을 아직 못 찾았다** 는 것이 현재 상태다.

## 권고 (사용자 승인 필요, 임의 진행하지 않음)

1. **자산이 회복돼도 현 설정으로 라이브를 재개하지 말 것.** 재개 조건을 숫자로 걸어둔다:
   *드리프트 중립 15분 엣지가 평균·중앙값 모두 +0.04% 이상이고 두 t 가 모두 2 이상이며
   3구간 전부 같은 부호* — 지금 이 조건을 만족하는 신호는 없다.
2. **CLAUDE.md 원칙 0 정정** — 08-28 이 남긴 미처리 항목에 이번 결과를 더한다.
   "15분 HullMA20"은 실제로 3분봉이고(08-28 5-2), 대안으로 적혀 있는 메인봇 뼈대
   "+0.446%"는 재현되지 않는다(이 문서 2장). **두 군데 다 고쳐야 한다.**
3. **다음에 잴 것이 있다면** 진입 신호가 아니라 **시간대**다. 3분/15분이 아니라
   1시간 신호 + 4~8시간 보유 구간을 같은 자로 재는 것. `edge_lab.py --interval` 로
   상위봉 캐시만 새로 받으면 그대로 돌아간다. 이 방향도 비용선을 못 넘으면
   **이 데이터로 이기는 봇은 없다** 가 정직한 결론이다.

## 이 측정의 한계 (인용 시 반드시 함께 볼 것)

- **생존편향**: 심볼 목록이 `logs/ws_worker_cache.json` 기준, 즉 **오늘 살아 있는 심볼**이다.
  기간 중 상장폐지된 것은 빠져 있어 모멘텀 계열이 실제보다 좋게 나온다.
- 31일도 여전히 짧다. 3구간이 서로 다른 국면이라는 보장은 없다.
- **청산이 없는 측정**이다. 손절·익절·슬롯 경쟁을 넣으면 값이 달라진다. 다만 08-28 이
  청산 축을 다섯 번 기각했으므로, 진입 엣지가 음수인 것을 청산으로 메우는 시나리오는 없다.
- 슬리피지 0.04% 는 **가정이지 실측이 아니다**(08-28 과 동일한 한계).
- 횡단면 계열은 **동시에 여러 심볼에 들어가는 것을 전제**한다. 슬롯 10개 제약 아래에서
  실제로 그 분포를 잡을 수 있는지는 별도 문제다.

---

# 6. 이번에 커밋한 것

```
신규   scripts/edge_fetch.py     npz 장기 캐시 수집기 (공개 전용)
신규   scripts/edge_lab.py       다중 뼈대 엣지 비교 + 이중 클러스터 t + 국면 분할
신규   HANDOFF_2026-08-31.md     이 문서
```

`scratch_edge_3m_30d.npz`(24MB) 와 `logs/edge_lab_*.txt` 는 커밋하지 않는다 — 재생성 가능하다.
재생성: `python scripts/edge_fetch.py --days 30` 후 `python scripts/edge_lab.py --split 3`.

---

# 7. 추가 측정 — 시간대를 바꾸니 결론이 바뀐다 (5장 권고 3 실행)

5장에서 "다음에 잴 것은 진입 신호가 아니라 시간대"라고 적었고, 그대로 실행했다.
**이 장이 이 문서에서 가장 중요하다. 1~5장의 "엣지가 없다"는 3분봉 한정 결론이었다.**

## 표본 (둘 다 새로 수집, 공개 전용 / 라이브 정지 상태에서 실행)

```
1h  x 180일   77심볼 x 4,500봉   2026-02-24 ~ 08-31   326 호출
15m x  90일   80심볼 x 9,000봉   2026-06-02 ~ 08-31   246 호출
```

## 겹침 제거를 먼저 했다 — 안 하면 또 속는다

24시간 보유를 1시간마다 표본추출하면 이웃 표본끼리 23시간이 겹친다. 독립이 아니다.
`--stride` 로 **보유시간 간격으로만 표본추출**해 겹침을 제거했다. 효과는 컸다:

```
볼밴돌파+EMA 24시간   겹침 허용: +0.5282|+0.2290 (t+4.1/+3.3)  n=11,695
                      겹침 제거: +0.2206|nan     (t+0.3/nan)   n=560
```

**겹침을 제거하면 메인봇 뼈대는 표본이 560건으로 무너져 판정 불가가 된다.**
아래 수치는 전부 겹침 제거(stride=보유시간) 기준이다.

## 핵심 결과 — 손익분기 보유시간은 8~12시간이다

1h 신호, 180일, `건당평균|심볼중앙값 (시각t/심볼t)`:

```
보유      EMA추세 단독                    횡단면 모멘텀 20봉             CM 전체 [현행]
 4시간   +0.0622|+0.0388 (t+4.6/+4.1)  +0.1042|+0.0607 (t+3.8/+2.9)  +0.0041|+0.0176 (t+0.1/+0.3)
 8시간   +0.1317|+0.0847 (t+4.8/+4.6)  +0.1696|+0.1362 (t+3.0/+3.2)  +0.0185|+0.0332 (t-0.1/+0.7)
12시간   +0.1368|+0.1041 (t+3.3/+3.2)  +0.2181|+0.0815 (t+2.5/+2.4)  +0.0831|+0.0669 (t+1.0/+1.9)
24시간   +0.2999|+0.2681 (t+3.3/+3.8)  +0.5583|+0.2275 (t+3.0/+2.9)  -0.1279|+0.0091 (t-1.3/-1.3)
```

15m 신호 90일로 **독립 재현**(다른 봉·다른 기간·다른 심볼 집합):

```
보유      EMA추세 단독                    CM 전체 [현행]
 2시간   +0.0353|+0.0232 (t+3.3/+3.3)  -0.0103|-0.0140 (t-1.3/-1.1)
 4시간   +0.0676|+0.0447 (t+3.1/+3.7)  +0.0032|-0.0100 (t-0.0/+0.1)
 8시간   +0.1153|+0.1094 (t+2.6/+3.1)  -0.0270|-0.0234 (t-0.5/-0.6)
```

읽는 법:

1. **보유시간이 길어질수록 엣지가 커지고, 비용선 0.11% 을 넘는 지점이 8~12시간이다.**
   4시간 이하는 두 표본 모두 중앙값이 비용선 아래다. 3분봉 15분(1~5장)은 말할 것도 없다.
   **이것이 "왜 계속 잃는가"의 답이다 — 신호가 나쁜 게 아니라 보유시간이 비용보다 짧다.**
2. **작동하는 신호는 CM 이 아니라 그냥 추세다**(EMA20>EMA50). CM 은 두 표본, 모든 보유시간에서
   0 이거나 음수다. 24시간에서는 -0.1279 로 유의하게 나쁘다.
3. 1h/180일과 15m/90일이 **서로 다른 데이터인데 같은 모양**을 낸다. 우연이기 어렵다.

## 그래도 채택하면 안 되는 이유 세 가지

**(1) 국면 1/3 에서 죽는다.** 180일을 60일씩 3등분(24시간 보유):

```
                     구간1(02-24~04-28)   구간2(04-28~06-29)   구간3(06-29~08-31)
EMA추세 단독         +0.0917|+0.0083     +0.3392|+0.3132     +0.4686|+0.1452
횡단면 모멘텀 20봉    +0.1228|-0.1313     +0.7440|+0.2251     +0.8081|+0.2617
```

구간1 에서 EMA추세는 중앙값 +0.008(사실상 0), 횡단면 모멘텀은 **중앙값 음수**다.
2/3 국면에서 벌고 1/3 국면에서 못 번다. **추세추종의 정상적인 성질**이지만,
잔고가 얇을 때 그 1/3 을 먼저 만나면 끝난다.

**(2) 엣지가 거의 전부 숏에 있다.** EMA추세 단독 24시간 분해:

```
LONG만   +0.3145|중앙값 -0.0055 (t+3.5/+2.3)   <- 중앙값이 0
SHORT만  +0.2865|중앙값 +0.4870 (t+2.5/+3.0)   <- 여기에 다 있다
심볼 77개 중 57개 양수 (74%) — 꼬리 의존은 없다
```

롱 쪽 중앙값이 0 이다. **하락추세 알트를 숏으로 끌고 가는 것**이 수익원이라는 뜻인데,
라이브 e3 는 4시간 필터 때문에 신호의 **85% 가 롱**이었다(08-28 3장). 정확히 반대편에 서 있었다.

**(3) 생존편향이 이 측정에 가장 위험하게 작용한다.** 심볼 목록이 오늘 살아 있는 것들이고
180일 치가 있어야 통과하므로, 기간 중 상장폐지·급락소멸한 심볼이 통째로 빠졌다.
**숏 엣지는 그 빠진 심볼들에 더 크게 있었을 것**이므로 이번 수치는 과소평가일 수도,
반대로 롱 생존자 편향으로 과대평가일 수도 있다. **부호는 몰라도 크기는 못 믿는다.**

또한 이 측정에는 **펀딩비가 들어 있지 않다.** 8시간마다 정산되므로 8~24시간 보유
전략에서는 무시할 수 없다(숏은 보통 받는 쪽이라 유리할 가능성이 높지만 국면에 따라 반대).
채택 검토 시 반드시 펀딩 실측을 붙여야 한다.

## 이 결과가 원칙과 충돌하는 지점

- **원칙 1(거래는 활발해야 함) 과 정면 충돌한다.** 8~24시간 보유 x 슬롯 10개면
  하루 진입이 10~30건이다. 현재 e3 는 시간당 9.3건, 하루 200건대다. **한 자릿수 배로 준다.**
  원칙 1 은 "빈도를 떨어뜨리는 변경은 그 자체로 비용"이라고 하지만, 이번 측정은
  **빈도를 유지하는 시간대에는 애초에 벌 수 있는 돈이 없다**고 말한다.
  둘 중 하나는 포기해야 하고, 이건 측정이 아니라 **사용자의 결정 사항**이다.
- **원칙 0(CM 방향) 과 충돌한다.** CM 은 이번 두 표본에서 0 이거나 음수다.

## 다음 사람이 할 일 (승인 없이 진행하지 않음)

1. **펀딩비를 붙여 재측정.** 8~24시간 보유의 손익 부호를 바꿀 수 있는 유일한 미계측 항목이다.
2. **상장폐지 심볼을 포함한 표본으로 재측정.** 생존편향 크기를 재기 전에는 숏 엣지 크기를
   숫자로 인용하지 말 것.
3. 그 둘이 통과하면 **8~12시간 보유 / EMA추세 / 숏 편향** 구성을 백테스트 하네스
   (`cm_backtest.py` 계열)에 얹어 청산·슬롯·수수료까지 넣고 재검증한다.
   그 전까지 이 장의 수치는 **진입축 상한**이지 예상 손익이 아니다.

---

# 8. 펀딩비 재측정 — 관문 1 통과 (7장 "다음 사람이 할 일" 1번)

7장이 남긴 미계측 항목 중 **손익 부호를 바꿀 수 있는 유일한 것**이 펀딩비였다. 측정했다.

## 수집

- [scripts/edge_funding.py](scripts/edge_funding.py) — `/fapi/v1/fundingRate` 는 **공개**
  엔드포인트다. 인증 0회 / API 키 미사용, 123 호출, 77심볼 70,481건.
- **정산 주기가 심볼마다 다르다**: 8시간짜리와 4시간짜리가 섞여 있다(ZKC/BLESS 등 4h).
  그래서 주기를 가정하지 않고 실제 `fundingTime` 을 그대로 누적했다.
- 부호 규약: `fundingRate > 0` 이면 롱이 숏에게 준다. 롱 손익 = -rate, 숏 손익 = +rate.
  구현은 가격수익률에서 "롱 기준 비용"을 빼기만 하고 방향 부호는 `stats()` 가 곱하므로
  롱/숏 양쪽이 자동으로 맞는다.

## 결과 — 펀딩비는 이 전략을 죽이지 않는다

```
보유시간   평균 펀딩(롱 기준)   EMA추세 단독 차감 전 -> 차감 후
 8시간      -0.0046%          +0.1317|+0.0847 -> +0.1286|+0.0824
12시간      -0.0066%          +0.1368|+0.1041 -> +0.1327|+0.1001
24시간      -0.0122%          +0.2999|+0.2681 -> +0.2929|+0.2384
```

- 24시간 보유에서도 평균 **0.012%** 다. 엣지(0.24~0.29%)의 **1/20 수준**이라 부호를 못 바꾼다.
- 이 기간 알트 평균 펀딩률이 **약간 마이너스**였다 — 즉 평균적으로 **롱이 받는** 쪽이었다.
  따라서 펀딩은 숏에 불리하게 작용하는데, 엣지가 숏에 있으므로 그쪽을 따로 봤다:

```
EMA추세 단독 24시간   차감 전                  차감 후
  LONG만            +0.3202|중앙값 -0.0181    (변화 미미, 중앙값 여전히 0)
  SHORT만           +0.2680|중앙값 +0.4870 -> +0.3962 (t+2.3/+2.8)
```

**숏 중앙값이 0.487 -> 0.396 으로 0.09%p 깎였지만 여전히 비용선의 3.6배다.**
7장의 "엣지는 거의 전부 숏" 구조도 그대로 유지된다.

## 다만 국면 1 은 펀딩 반영 후 마이너스로 넘어간다

```
EMA추세 단독 24시간   구간1(02-24~04-28)   구간2                구간3
  차감 전            +0.0863|+0.0083      +0.3392|+0.3132     +0.4686|+0.1452
  차감 후            +0.0863|-0.0178      +0.3349|+0.3096     +0.4574|+0.1338
```

구간1 중앙값이 +0.008 -> **-0.018** 로 부호가 바뀐다. 7장에서 "구간1 은 사실상 0"이라고
썼는데, 펀딩까지 넣으면 **약한 마이너스**다. 결론은 같지만 더 나쁜 쪽으로 확정됐다.

## 판정

- **관문 1(펀딩비) 통과.** 7장 수치는 펀딩 반영 후에도 살아남는다. 손익분기 보유시간은
  여전히 8~12시간이다(12시간 중앙값 +0.1001 이 비용선 0.11 과 거의 같다).
- **관문 2(생존편향)는 아직 안 열렸다.** 이게 남은 최대 미지수이고,
  숏 엣지에 가장 크게 작용할 항목이다. 상장폐지 심볼을 포함한 표본으로 재측정하기 전에는
  **숏 엣지의 크기를 숫자로 인용하지 말 것.**
- 여전히 **채택 권고가 아니다.** 국면 1/3 에서 마이너스이고, 청산·슬롯·슬리피지가
  안 들어간 진입축 상한값이다.

---

# 9. 생존편향 재측정 — **관문 2 실패. 7~8장의 엣지는 대부분 생존편향이었다.**

7장이 남긴 마지막 관문이자 "숏 엣지에 가장 크게 작용할 항목"이라고 적어둔 것이다.
결과는 나쁘다. **7~8장의 채택 후보는 여기서 죽는다.**

## 상장폐지 심볼을 어떻게 구했나

REST `/fapi/v1/klines` 는 **현재 상장된 심볼만** 응답한다. 그래서 지금까지의 모든 측정에서
기간 중 사라진 심볼이 통째로 빠져 있었다. 우회로는 바이낸스 **공개 데이터 덤프**다.

```
data.binance.vision 버킷 목록      역대 UM 선물 심볼 1,018개 (USDT 864)
현재 상장 USDT 무기한               525
역대 USDT 중 현재 없음              339
그중 2026-03-01 에 거래 중이었던 것  145   <- 우리 창에서 사라진 심볼
```

**표본에 있던 생존자가 77개인데 같은 창에서 죽은 심볼이 145개다.** 빠진 쪽이 두 배다.

- [scripts/edge_delisted.py](scripts/edge_delisted.py) — 월별 klines zip 을 받아 npz 로 합친다.
  전부 공개 CDN, 인증 0회, fapi 레이트리밋과 무관. 145심볼 전부 수집 성공(SKIP 0).
- 월별 파일은 달이 끝나야 올라오므로 **비교 창을 2026-02-25 ~ 08-01 로 잘라 맞췄다**
  (`--start-date` / `--end-date`). 양쪽 다 3,768봉으로 **완전히 동일한 창**이다.

## 결과 — 엣지가 1/6 로 줄고 유의성이 사라진다

24시간 보유, 겹침 제거, 동일 창(3,768봉), `건당평균|심볼중앙값 (시각t/심볼t)`:

```
신호                    생존자만 (77심볼)              생존자+상폐 (222심볼)
EMA추세 단독          +0.1249|+0.1765 (t+1.5/+1.7)  +0.0217|+0.0211 (t+0.4/+0.6)
횡단면 모멘텀 20봉     -0.0320|-0.0764 (t-0.2/-0.3)  -0.0155|-0.0091 (t-0.2/-0.4)
CM 전체 [현행뼈대]     -0.1388|-0.0183 (t-1.1/-1.5)  -0.0523|-0.0211 (t-0.7/-0.3)
```

**EMA추세 단독의 중앙값이 +0.1765 -> +0.0211 로 떨어진다.** 비용선 0.11% 의 1/5 이고
두 t 가 모두 1 아래로 죽는다. **더 이상 엣지가 아니다.**

주식·상품 무기한(TSLA/XAU/COIN 등 12종)이 섞여 결과를 흐린 게 아닌지도 확인했다.
빼고 재측정해도 **+0.0310|+0.0276 (t+0.6/+0.8)** 로 결론이 같다.

## 왜 숏 엣지가 사라지는가 — 메커니즘이 분명하다

7~8장의 수익원이라고 했던 숏 쪽을 분해하면:

```
EMA추세 단독 24시간 SHORT만   생존자만: 중앙값 +0.4060   생존자+상폐: 중앙값 +0.0620
```

**"죽어가는 코인을 숏 치면 좋다"는 직관과 반대로 나온다.** 이유는 두 가지다.

1. **드리프트 중립화의 기준선이 위로 편향돼 있었다.** 살아남은 심볼만으로 시장평균을
   계산하면 그 평균이 실제보다 높다. 숏 수익은 `-(수익률 - 시장평균)` 이므로
   **기준선이 높을수록 숏이 좋아 보인다.** 죽은 심볼 145개를 넣어 평균을 정상화하니
   그 착시가 걷혔다. 이게 생존편향이 작동한 정확한 경로다.
2. 상폐로 가는 코인은 이미 무너진 저유동성 종목이라 후반부가 추세가 아니라
   난폭한 되돌림·숏스퀴즈다. 추세추종 숏이 계속 먹을 수 있는 구간이 아니다.

## 이 측정 자체의 한계 (반대 방향으로 작용하는 것)

- 상폐 심볼의 데이터는 **상장폐지 시점에 끝난다.** 마지막 붕괴 구간이 표본에 없고,
  이건 숏에 **유리하게** 빠진 부분이다. 다만 실제로도 상폐 직전 종목은 정상 체결이
  안 되므로, 그 구간을 넣어 계산한 수익을 실현 가능한 것으로 보면 안 된다.
- 145개 중 상당수는 봇이 애초에 담지 않을 저유동성 종목이다. "봇의 실제 유니버스"는
  이 둘 사이 어딘가다. 다만 **77개 생존자만 보는 것이 그 유니버스라는 근거도 없다** —
  그 77개는 "오늘까지 살아남았다"는 사실로 선택된 집합이기 때문이다.

## 종합 판정 — 채택 후보는 없다

```
관문 1 펀딩비      통과  (24시간 0.012%, 엣지의 1/20)
관문 2 생존편향    실패  (엣지 1/6 로 축소, 두 t 모두 소멸)
관문 3 백테스트    도달하지 못함
```

**7장의 "손익분기 보유시간 8~12시간"은 생존자 표본에서만 성립한다.**
죽은 심볼을 되돌려 놓으면 24시간 보유에서도 비용선을 못 넘는다.
1~5장(3분봉)과 합치면 결론은 하나다 — **이 데이터·이 신호군에서 비용을 덮는 진입 엣지를
찾지 못했다.** 짧게 잡아도(15분) 길게 잡아도(24시간) 안 된다.

## 그래서 다음은

**파라미터·신호·시간대 세 축이 모두 닫혔다.** 남은 방향은 성격이 다른 것들뿐이다.

1. **가격 외 데이터.** 오더북 불균형, 청산(liquidation) 데이터, 펀딩률 자체를 신호로,
   미결제약정(OI) 변화 — 전부 klines 에 없는 정보다. `edge_lab.py` 의 판정 장치
   (드리프트 중립 + 이중 클러스터 t + 중앙값 + stride)를 그대로 재사용할 수 있다.
2. **비용 자체를 낮추기.** 엣지가 0.02~0.03% 수준이라면 왕복 0.11% 를 0.04% 로
   낮춰도 여전히 부족하다. 이 축 단독으로는 답이 안 된다.
3. **매매를 하지 않는 것도 결론이다.** 세 관문을 통과한 구성이 하나도 없다는 것이
   지금까지의 측정 결과다. 잔고가 없는 지금은 이 사실을 확인하는 데 비용이 들지 않았다.

**어느 쪽이든 라이브 재개는 5장의 수치 조건을 만족하는 신호가 나온 뒤의 일이다.**

---

# 10. 가격 외 데이터 (A안) — **마지막 축도 닫혔다**

9장에서 "남은 방향은 klines 에 없는 정보뿐"이라고 적었고, 그걸 쟀다.
**결과: 통과한 신호 없음.** 이 문서의 모든 축이 이로써 닫힌다.

## 무엇을 구할 수 있었나

```
data.binance.vision 공개 덤프        생존자    상폐 145개 중
  월별 fundingRate                    O        59개 (41%)
  일별 metrics (OI/포지셔닝)          O        70개 (48%)
  일별 aggTrades (주문흐름)          O        O   <- 심볼당 하루 31MB, 규모상 불가
  bookTicker (호가)                   X        X   <- 이력 없음
```

- **호가·주문흐름은 못 쟀다.** bookTicker 덤프는 존재하지 않고, aggTrades 는
  심볼당 하루 31MB 라 200심볼 x 120일이면 테라바이트급이다. 이 둘을 재려면
  **지금부터 실시간으로 몇 달 기록**하는 수밖에 없다.
- 신규 수집기: [scripts/edge_funding_dumps.py](scripts/edge_funding_dumps.py),
  [scripts/edge_metrics.py](scripts/edge_metrics.py). 전부 공개 CDN, 인증 0회.
- metrics 는 5분 간격의 OI·상위트레이더 롱숏비·전체계정 롱숏비·taker 매수/매도비다.
  봉 시각 **이전의 마지막 관측**만 써서 lookahead 를 막았다.

**커버리지 한계를 먼저 적는다**: 상폐 심볼의 펀딩 41% / metrics 48% 만 남아 있고,
그 가용성 자체가 생존과 상관됐을 수 있다. **9장에서 결론을 뒤집었던 생존편향 보정이
이 축에서는 절반만 적용된다.** 즉 아래 수치는 여전히 낙관 쪽으로 기울어 있다.

## 결과 — 펀딩 캐리는 오히려 마이너스다

222심볼(펀딩 136 / metrics 126), 2026-04-02~08-01, 겹침 제거(stride=보유시간),
드리프트 중립 + **펀딩 차감 후**. 캐리는 받는 펀딩이 수익의 절반이라 반드시 차감 후로 본다.

```
신호                        8시간 보유                     24시간 보유
펀딩캐리 횡단면(고펀딩 숏)   -0.0824|-0.0277 (t-2.3/-2.6)  -0.1965|-0.0620 (t-3.3/-2.3)
펀딩캐리 절대 0.03% 이상     -0.0912|+0.0157 (t-1.3/-1.0)  -0.2277|+0.0471 (t-2.3/-1.0)
```

**교과서적 캐리가 유의하게 음수다.** 펀딩을 받아도 가격이 그보다 더 반대로 간다.
"남들이 돈을 내겠다는 쪽의 반대에 선다"는 경제적 이야기가 이 표본에서는 성립하지 않는다.

## 그 반대(펀딩 모멘텀)와 포지셔닝 — 문턱에서 걸린다

부호를 뒤집은 쪽(고펀딩 롱 = 군중과 같은 방향)은 살아 있는 것처럼 보인다:

```
펀딩캐리 역방향 24시간   +0.1965|+0.0620 (t+3.3/+2.3)   구간1 +0.1617|+0.0587 / 구간2 +0.2333|+0.0098
  LONG만  +0.2621|중앙값 -0.0921      SHORT만 +0.1245|중앙값 +0.1423
  심볼 128개 중 72개 양수 (56%)
```

평균은 비용선 0.11% 를 넘고 두 t 도 산다. **그런데 중앙값이 +0.062 로 비용선 아래고,
롱 다리의 중앙값은 -0.0921 로 음수다.** 평균만 4배 큰 3장의 함정과 같은 서명이다.
구간2 중앙값은 +0.0098 로 사실상 0. **판정 규칙(평균·중앙값 모두 비용선 초과)에서 탈락.**

포지셔닝 지표도 같은 자리에서 걸린다:

```
전체계정 롱숏비 순방향 8시간   +0.0476|+0.0160 (t+1.1/+1.8)
   구간1 +0.1205|+0.0407  /  구간2 -0.0262|-0.0238   <- 부호가 뒤집힌다
   LONG만 +0.1171|중앙값 -0.0233 / SHORT만 -0.0216|중앙값 +0.0330  <- 앞뒤가 안 맞는다
상위트레이더 포지션        전 구성 t < 1
taker 매수우위            전 구성 t < 1.6, 부호 불안정
OI 증가율 / OI+가격 조합   평균과 중앙값의 부호가 반대
```

**주의(측정 함정 하나 더)**: 이 신호는 stride=24 로 8시간 보유를 재면
+0.1499|+0.0807 (t+1.8/+2.9) 로 훨씬 좋아 보였다. 보유시간과 stride 를 맞추지 않으면
(8시간 보유는 stride=8) 표본이 3분의 1로 줄어 그 부분표본의 잡음을 엣지로 읽는다.
**stride 는 반드시 보유 봉수와 같게 둘 것.**

## 종합 — 네 축이 전부 닫혔다

```
축                          결과
1. 파라미터 (08-28, 22구성)  전부 표준오차 안
2. 진입 신호 뼈대 (1~5장)    3분봉/15분에서 비용선 미달, CM·메인봇 모두 음수
3. 시간대 (7~8장)            8~12시간에서 넘는 듯했으나 -> 9장에서 생존편향으로 판명
4. 가격 외 데이터 (10장)     캐리는 음수, 나머지는 중앙값·국면 안정성에서 탈락
```

**네 축 모두에서 비용선을 넘는 구성을 찾지 못했다.** 그리고 후보가 죽는 방식이
매번 같았다 — 평균은 좋은데 **중앙값이 안 따라오고**, 표본을 넓히거나 국면을 나누면
사라진다. 이건 신호를 더 찾으면 해결되는 종류가 아니라, **왕복 0.11% 를 내는 참가자가
공개 데이터로 방향을 맞혀 이길 자리가 아니라는 신호**로 읽는 것이 맞다.

## 아직 안 닫힌 것 (정직하게)

- **호가·주문흐름**: 이력이 없어 못 쟀다. 재려면 지금부터 WS 로 몇 달 기록해야 한다.
  이 저장소에는 이미 WS 레이어가 있으므로 **기록만 시작해두는 것**은 비용이 거의 없다.
- **메이커 스프레드 수취**(방향 예측이 아니라 체결 제공): 성격이 다른 사업이고
  이 문서의 어떤 측정도 여기에 답하지 않는다. 다만 역선택 관리가 핵심이라
  호가 데이터 없이는 착수 자체가 불가능하다.
- 위 둘 다 **자산이 있어야 검증되는 것이 아니라 데이터가 쌓여야 검증된다.**
  잔고와 무관하게 지금 시작할 수 있는 유일한 일이다.

---

# 11. 호가·주문흐름 기록기 가동 (10장의 유일한 미탐색 축)

10장에서 "호가·주문흐름은 이력이 없어 못 쟀다, 지금부터 기록하는 수밖에 없다"고 적었다.
**기록기를 붙이고 상주시켰다.** 이건 측정이 아니라 **몇 달 뒤에 측정할 수 있게 하는 작업**이다.

## 무엇을 남기나

[scripts/obook_recorder.py](scripts/obook_recorder.py) — 83심볼, 5초 버킷, 심볼별 한 줄:

```
bid/ask + 각 잔량        -> 스프레드, L1 호가 불균형
5호가 매수/매도 잔량합    -> depth5 불균형
taker 매수/매도 체결량    -> 주문흐름 불균형
체결 건수, 마지막 가격
```

가동 확인(60초): 83심볼 / 체결이 있는 버킷 90% / 5호가 100% /
**평문 122MB·압축 40MB per day** (한 달 약 1.2GB).
런처는 [start_obook.ps1](start_obook.ps1), 산출물은 `logs/obook/` 뿐이다.

## 안전 성질 (라이브와 완전히 분리)

- **공개 스트림만. API 키 미사용, 인증 0회, 주문 기능 없음.** 라이브 봇과 자격증명이
  겹치지 않으므로 [[backtest-ip-ban-incident]] 유형의 사고와 무관하다.
- 원장·상태 파일을 건드리지 않는다. 봇을 다시 켜도 간섭하지 않는다.
- python-binance 를 쓰지 않는다. read-loop freeze 버그가 있는데 이 프로세스는
  몇 달을 무인으로 돌아야 하므로, websockets 로 직접 붙고 **30초 무응답 -> 재접속**을
  명시적으로 넣었다(실제로 첫 시험에서 한 번 끊겼고 자동 복구됐다).

## 만들면서 밟은 함정 세 개 (같은 실수 방지)

1. **이 거래소는 `@aggTrade` 를 서빙하지 않는다.** 구독은 조용히 성공하고 메시지가
   0건 온다. 10초 동안 BTC 체결이 0건이라 발견했다. `@trade` 를 쓴다.
   `@kline_1m`·`@markPrice`·`@forceOrder` 도 마찬가지로 비어 있다.
   살아 있는 것은 `@bookTicker` / `@trade` / `@depth5@100ms` 다.
2. **`--seconds 0`(무한) 경로가 즉시 종료됐다.** 대기 없이 곧장 task cancel 로
   떨어졌다. 런처로 띄웠을 때 "프로세스가 사라진다"로 나타난다.
3. **당일 파일을 gzip 으로 열어두면 읽을 수가 없다** (EOFError: ended before
   end-of-stream marker). 몇 달 쌓는 기록기에서 "오늘 데이터를 못 본다"는 치명적이라,
   **당일은 평문 CSV, 날짜가 바뀌면 압축**으로 바꿨다. 전원이 나가도 평문은 남는다.

## 이걸로 나중에 무엇을 재나

`edge_lab.py` 의 판정 장치를 그대로 재사용한다 — 드리프트 중립 + 이중 클러스터 t +
심볼 중앙값 + stride=보유봉수. 후보 신호는 L1/depth5 호가 불균형, 주문흐름 불균형,
스프레드 확대/축소, 체결 강도다.

**재기 전에 최소 2~3개월은 쌓여야 한다.** 그 전에 중간 결과를 보고 판단하지 말 것 —
이 문서가 기록한 실패 다섯 개가 전부 "표본이 작을 때 좋아 보였다"였다.
그리고 이 축도 **생존편향 보정이 안 된다**(기록 시작 시점에 살아 있는 심볼만 담긴다).
판정할 때 그 점을 9장과 같은 무게로 볼 것.

---

# 12. 비용선을 실측으로 바꾸니 후보가 하나 나왔다 (11장 이후)

10장까지의 모든 판정에 **내 쪽 결함이 하나 있었다.** 비용선 0.11% 를
**전 심볼에 똑같이** 적용했다. 그런데 0.11% 는 실측이 아니라 가정(수수료 0.07 +
슬리피지 0.04)이었고, 실제 비용은 심볼마다 다르다. 11장 기록기가 그걸 잴 수 있게 했다.

## 실측: 비용이 심볼 간 3.7배 차이난다

`logs/obook/` 21,331버킷(83심볼) 기준 스프레드 중앙값:

```
BTCUSDT 0.0001%  ETHUSDT 0.0004%  BNBUSDT 0.0015%   -> 왕복 0.040% (사실상 수수료만)
중앙값  0.0231%                                      -> 왕복 0.063%
VELVETUSDT 0.1077%  OPGUSDT 0.1066%                  -> 왕복 0.148%
```

**유동성 좋은 심볼의 진짜 비용은 0.11% 가 아니라 0.04~0.05% 다.** 엣지가 0.01~0.15%
스케일인 판에서 비용 가정을 2~3배 틀리게 잡으면 결론이 통째로 바뀐다.
지금까지 "비용선 미달"로 기각한 것들은 **유동 심볼에 대해서는 과도하게 불리한 판정**이었다.

## 유동 심볼만 4.1년으로 다시 재니 후보가 나온다

스프레드 0.010% 이하 19심볼을 뽑아 1시간봉 4.1년(2022-07~2026-08)을 받았다.
표본이 8배로 늘어 검정력이 산다. 겹침 제거(stride=24), 펀딩 차감 후:

```
보유 24시간, 비용선 0.05%          건당평균|심볼중앙값 (시각t/심볼t)
횡단면 모멘텀 20봉(강세롱)          +0.1482|+0.1199 (t+3.2/+2.1)   <- 후보
EMA추세 단독                      +0.0621|+0.0201 (t+2.8/+1.8)
CM 전체 [현행뼈대]                 -0.0233|-0.0384 (t+0.4/-1.2)
```

**횡단면 모멘텀 20봉이 이 문서에서 처음으로 세 관문을 모두 넘는다.**
20시간 룩백으로 상대강도 상위 20% 롱 / 하위 20% 숏, 24시간 보유.

## 관문 검사 — 9장에서 후보를 죽인 검사를 통과했다

**생존편향**: 19심볼은 "오늘 유동적인" 심볼이라 최근 상장 성공작(ENA/HYPE/HUMA/TAO)이
섞여 있다. 2022-07부터 전 기간 존재하는 **13심볼만** 남기고 재측정하면:

```
                      전체 19심볼                  전 기간 13심볼만
횡단면 모멘텀 20봉   +0.1261|+0.0835 (t+3.1/+3.2)  +0.1261|+0.0835 (t+3.1/+3.2)
  LONG만                                          +0.1397|+0.1038 (t+2.3/+1.8)
  SHORT만                                         +0.1119|+0.0813 (t+2.1/+1.9)
  심볼 13개 중 9개 양수 (69%)
```

**빼도 안 죽는다.** 9장에서 8~12시간 후보를 죽였던 바로 그 검사인데 여기서는 통과한다.
롱/숏 양쪽이 모두 양수이고 중앙값도 따라온다 — 9장의 "숏에만 몰린 꼬리"와 구조가 다르다.

**국면 4등분**(각 1년): `+0.0398|+0.0320` / `+0.1450|+0.0952` / `+0.1142|+0.1070` /
`+0.2664|+0.1478` — **4구간 전부 평균·중앙값 양수.** 다만 구간1 은 중앙값이 비용선 아래다.

**펀딩**: 24시간 보유 평균 `+0.0033%`. 엣지의 1/25 라 무관하다.

## 그런데 채택하면 안 된다 — 세 가지가 걸린다

**(1) 파라미터 표면이 매끄럽지 않다.** 룩백을 바꿔보면:

```
 5봉 +0.0519|+0.0442 (t+1.3/+1.5)
10봉 -0.0142|-0.0009 (t-0.2/-0.1)   <- 음수
20봉 +0.1216|+0.0868 (t+3.0/+3.2)   <- 후보
40봉 +0.0913|+0.0319 (t+2.4/+2.0)
80봉 +0.0559|+0.0212 (t+1.5/+0.9)
```

진짜 요인이라면 이웃한 룩백들이 완만하게 이어져야 한다. **10봉이 음수인 건 경고다.**
20·40봉이 함께 살아 있는 건 위안이지만, 20봉의 강도 일부는 운일 수 있다.

**(2) 전부 in-sample 이다.** 이 4.1년을 보면서 신호를 골랐다. 신호군 20여 개 x 지평
여러 개를 쟀으므로 **다중검정만으로도 t~3 하나쯤은 나온다.** 남은 진짜 검증은
과거 데이터에 없다 — **앞으로 나올 데이터뿐이다.**

**(3) 원칙 1 과 정면 충돌한다.** 13심볼 x 0.3건/심볼일 = **하루 4건 안팎**이다.
현재 200건대에서 두 자릿수 배로 준다. 그리고 횡단면 신호라 **여러 심볼을 동시에
들고 있어야** 성립하는데, 슬롯 10개·`--max-same-side 3` 구조와 맞지 않는다.

## 그래서 다음은 — 종이 검증(forward test)

**자산이 없는 지금이 오히려 맞는 타이밍이다.** 이 후보에 필요한 건 돈이 아니라
**보지 않은 데이터**다. 오늘부터 신호를 매일 기록하고 24시간 뒤 결과를 채점하면,
2~3개월 뒤에 **진짜 out-of-sample 표본**이 생긴다. 비용 0.

판정 기준은 미리 못 박는다 — 나중에 좋아 보이는 쪽으로 기준을 옮기지 않기 위해서다:

```
채택: 종이 검증 60건 이상에서 건당평균·심볼중앙값 모두 +0.05% 이상,
      시각t·심볼t 모두 2 이상, 롱/숏 양쪽 모두 양수
기각: 위 중 하나라도 미달. 특히 중앙값이 비용선 아래로 내려가면 즉시 기각한다.
```

11장 호가 기록기와 함께 돌리면 된다. **이 두 개가 지금 잔고 없이 할 수 있는 전부이고,
동시에 지금 해야만 하는 전부다.**

---

# 13. 12장 후보 기각 — 내 하네스에 숨은 자유도가 있었다

**12장에서 "처음으로 세 관문을 넘었다"고 쓴 횡단면 모멘텀 20봉은 기각한다.**
관문을 넘은 게 아니라 내가 24방향 탐색의 승자를 보고했다.

## 어떻게 발견했나

종이 검증기([scripts/paper_track.py](scripts/paper_track.py))를 만들어 재구현이 맞는지
과거 구간으로 대조하다가 모순을 만났다.

```
edge_lab --split 8 의 구간8 (2026-02~08)        +0.4053|+0.3262 (t+2.2/+3.8)
edge_lab --start-date 2026-03-01 (같은 구간)     -0.0897|+0.1170 (t-0.5/-0.3)
```

같은 데이터, 같은 신호, 같은 기간인데 부호가 반대다. 차이는 하나였다 —
**`--stride` 표본추출의 위상.** `keep[::24]` 는 항상 위상 0 을 고르는데,
전자는 4.1년 패널의 0행부터, 후자는 2026-03-01 부터 세므로 **하루 중 진입 시각이
서로 다르다.**

## 위상 24개를 전부 재보니

```
횡단면 모멘텀 20봉 / 24시간 / core13 / 4.1년
  건당평균     위상평균 +0.0390%   범위 -0.0409 ~ +0.1498
  심볼중앙값   위상평균 +0.0499%   범위 -0.0420 ~ +0.1796
  시각t        위상평균 +1.18      범위 -0.7 ~ +3.7
  비용선 0.05% 를 넘는 위상  7/24        t>=2 인 위상  5/24
```

**내가 보고한 `+0.1261 (t+3.1)` 은 24개 중 최상위권이었다.** 위상평균은 `+0.0390%` 로
비용선 0.05% 아래이고 평균 t 는 1.18 이다. 세 관문을 넘은 적이 없다.

원인은 명확하다. 나는 위상을 고르지 않았다고 생각했지만 `keep[::stride]` 가 나 대신
위상 0 을 골랐고, 그게 우연히 가장 좋은 위상이었다. **선택한 줄 모르는 선택이
다중검정에서 가장 위험하다** — 보정해야 한다는 자각조차 없기 때문이다.

## 하네스 수정

`--phase-sweep` 을 추가했다. `--stride` 를 쓸 때 **전 위상을 돌려 평균과 범위를
함께 낸다.** 앞으로 stride 를 쓰는 측정은 이 옵션으로만 판정한다.

위상평균으로 전 신호를 다시 세우면(24시간 보유, core13, 4.1년):

```
신호                        위상평균|심볼중앙값   평균시각t    위상 범위
RSI 모멘텀 40/60            +0.0676|+0.0717      +2.11    +0.0103 ~ +0.1193
횡단면 모멘텀 40봉           +0.0653|+0.0327      +1.68    +0.0316 ~ +0.1111
횡단면 모멘텀 80봉           +0.0540|+0.0276      +1.46    +0.0184 ~ +0.1016
횡단면 모멘텀 20봉 [기각]     +0.0390|+0.0499      +1.18    -0.0409 ~ +0.1498
EMA추세 단독                +0.0377|+0.0351      +1.72    +0.0175 ~ +0.0547
CM 전체 [현행뼈대]           +0.0269|+0.0326      +0.98    -0.0037 ~ +0.0732
```

**RSI 모멘텀 40/60 이 유일하게 평균·중앙값 모두 비용선 위이고 24개 위상이 전부 양수다.**
다만 **이걸 새 후보로 올리지 않는다.** 이유는 12장에서 배운 것과 같다 —
신호 30개 남짓을 훑어 평균 t 2.11 짜리 하나를 고르는 것은 다중검정 그 자체다.
12장에서 정확히 그렇게 해서 틀렸다. 이건 **다음에 검사할 대상**이지 후보가 아니다.

## 종이 검증기는 남겨둔다

[scripts/paper_track.py](scripts/paper_track.py) 는 만들었고 재구현 정확성도 확인했다
(edge_lab 과 같은 창에서 `-0.0897|+0.1170` vs `-0.1165|+0.1369`, 분위 보간 차이).
초판에 **횡단면 순위·드리프트 기준선을 13심볼로 계산하는 버그**가 있었고
(19심볼로 내야 한다) 고쳤다. 지금 추적 대상 신호가 기각됐으므로 **돌리지 않는다.**
새 후보가 살아남으면 규칙만 바꿔 재사용한다.

설계상 좋은 점 하나는 남긴다: **상주 프로세스가 아니다.** 봉은 나중에도 받을 수
있으므로 규칙을 동결한 커밋 시각만 증명되면 되고, 아무 때나 돌려도 전 구간을
재구성한다(멱등). 표본이 비는 사고가 구조적으로 불가능하다.

## 12장의 판정 기준도 틀렸다

"종이 검증 **60건** 이상"이라고 썼는데 하루 5건이면 12일에 채워지고 그건 독립 관측
12개다. 같은 문서가 "시각 클러스터 t 로 판정한다"고 해놓고 건수로 세면 모순이다.
**"독립 관측 60일 이상"으로 고쳤다**(paper_track.py 독스트링에 반영).

## 현재 상태 — 후보 0개

```
축                          결과
1. 파라미터                  전부 표준오차 안
2. 진입 신호 뼈대             3분봉/15분 비용선 미달
3. 시간대                    8~12시간 후보 -> 생존편향 (9장)
4. 가격 외 데이터             캐리 음수, 나머지 중앙값·국면에서 탈락
5. 비용선 실측 + 유동심볼      횡단면 모멘텀 후보 -> 위상 artifact (이 장)
```

**다섯 번째 후보도 죽었다.** 그리고 이번 것은 시장이 아니라 **내 측정 방법**이 만든
가짜였다. 앞의 넷과 종류가 다르다 — 데이터가 아니라 하네스를 고쳐야 했다.

교훈을 하나 추가한다. 이 문서의 판정 규칙은 이제 넷이다:
평균과 중앙값이 붙어 있을 것 / 두 클러스터 t 가 모두 살 것 / 국면을 나눠도 부호가
같을 것 / **표본추출에 숨은 자유도가 없을 것(위상·유니버스·기간 시작점).**

---

# 14. 원칙 0 재조합 — 부호를 뒤집는 요소를 특정했다 (2026-09-01)

문서와 코드가 어긋났던 유일한 축이 원칙 0 이다(15분 HullMA20 이라고 적혀 있었지만
실제로는 3분봉). 그래서 **"의도했던 조합"은 한 번도 제대로 측정된 적이 없었다.**
08-28 의 3분vs15분 대조는 생존편향 표본이었고 위상 스윕도 없었다. 다시 쟀다.

## 표본과 규약

메이저 19심볼(스프레드 0.010% 이하) 1년치를 3분봉·15분봉으로 새로 받고,
신호는 상장 전 기간 존재한 **core13** 에서만 낸다(순위·기준선은 19심볼 유지).
전 측정 **위상평균**(13장의 수정 사항). 사전 등록한 6개 분해만 본다.

CM 진입 규칙은 두 조건의 곱이다: **(가) HullMA20 기울기 방향** AND
**(나) 종가가 HullMA 위/아래**. 이 둘을 따로 잰 적이 없었다.

## 3분봉 (현행 코드) / 보유 15분 — 라이브가 실제로 하던 것

```
                          위상평균|중앙값   평균시각t   위상 범위
CM 전체 [현행]            -0.0024|-0.0013    -3.01    -0.0033 ~ -0.0015   24위상 전부 음수
[분해] 가격위치만          -0.0020|-0.0009    -3.36    -0.0028 ~ -0.0015   <- 음수의 주범
[분해] 기울기만            -0.0011|-0.0005    -1.79
[분해] 눌림(기울기↑&종가<HMA) +0.0014|+0.0010   +0.56    +0.0009 ~ +0.0019   24위상 전부 양수
[분해] 눌림 + 4h정합       +0.0030|+0.0021    +0.99    +0.0026 ~ +0.0036   24위상 전부 양수
```

**CM 을 음수로 만드는 것은 "종가가 HullMA 위일 때 롱"이라는 조건이다.**
그 조건 하나만 떼어내도 `-0.0020 (t-3.36)`, 24위상 전부 음수로 유의하게 나쁘다.
**부호를 뒤집으면(종가가 HMA 아래일 때 롱 = 눌림) 양수가 된다.**

이건 우연이 아니라 구조적이다. 24개 위상이 전부 같은 부호이고, 방향이
**라이브 e3 가 실제로 체결하는 자리와 일치한다** — e3 는 CM 신호 후 EMA5 눌림을
기다렸다가 들어간다. 즉 **봇의 진입 판정(추격)과 체결 방식(눌림)이 서로 반대였고,
돈을 벌어준 쪽은 체결 방식이었다.**

## 15분봉 (문서가 의도했던 것) / 보유 60분

```
CM 전체                  +0.0057|+0.0065    +0.76    +0.0043 ~ +0.0086   전부 양수
[분해] 기울기만            +0.0050|+0.0058    +2.26    +0.0035 ~ +0.0068   전부 양수
[분해] 눌림 + 4h정합       +0.0110|+0.0082    +2.30    +0.0095 ~ +0.0131   전부 양수
[분해] CM + 4h역행         -0.0002|+0.0025    -0.76
```

**3분봉에서 `-0.0024 (t-3.01)` 이던 CM 전체가 15분봉에서는 `+0.0057` 로 부호가 바뀐다.**
문서가 기술한 대로 돌렸다면 최소한 음수는 아니었다. 08-28 의 "3분봉이 더 낫다"는
결론(`+0.0142` vs `+0.0068`)은 **생존편향 표본 + 위상 미보정 때문에 뒤집힌 것**이다.

## 그래서 최선 조합은

```
15분봉 HullMA20 / 기울기↑ & 종가<HMA(눌림) / 4h EMA200 정합 / 60분 보유
   +0.0110%  (심볼중앙값 +0.0082%, 평균 시각t +2.30, 24위상 전부 양수)
```

세 관문(평균·중앙값 동부호 / 두 t / 위상 안정)을 **처음으로 깨끗하게** 통과한다.
그런데도 **채택하지 않는다.**

```
최선 조합의 엣지          +0.0110%
비용선 (메이커 왕복)       0.04%      <- 1/4
비용선 (테이커 왕복)       0.10%      <- 1/9   (edge_lab 측정규약은 이쪽이다)
```

**부호는 고쳤지만 자릿수가 모자란다.** 이 조합으로 돌려도 진다.

## 이 장의 값어치 — 진단이지 처방이 아니다

- **원칙 0 의 두 구성요소 중 "가격위치"가 부호를 뒤집는 범인이다.** 3분봉에서
  `t-3.36` 으로 유의하게 음수다. 라이브가 8월 내내 잃은 것과 부호가 일치한다.
- **신호봉을 15분으로 올리면 부호가 뒤집힌다.** 문서가 옳고 코드가 틀렸던 셈인데,
  그 차이는 손실/이익이 아니라 **덜 나쁨/조금 나음** 수준이다.
- **다시 켤 일이 있으면** `--cm-res-custom 15 --cm-use-alt-resolution` 이고,
  진입 판정을 추격이 아니라 눌림으로 뒤집어야 한다. 다만 그렇게 해도 기대값은
  여전히 비용 아래이므로 **이건 "덜 잃는 설정"이지 "버는 설정"이 아니다.**

## 판정

후보 여섯 번째도 기각이다. 다만 앞의 다섯과 성격이 다르다 — 이건 가짜 엣지가 아니라
**진짜지만 너무 작은 엣지**다. 24위상 전부 같은 부호, 두 t 생존, 평균과 중앙값이 붙어
있다. 없어서 못 버는 게 아니라 **있는데 비용의 1/4 라서** 못 번다.

---

# 15. 성분 조합 — 사전등록 train/holdout 실험, 기각 (2026-09-01)

기각된 후보 여섯 개는 전부 **단독** 신호였다. 그런데 14장에서 `눌림 단독 +0.0038`
-> `눌림 + 4h정합 +0.0110` 으로 조건을 얹자 3배가 됐다. 성분이 서로 겹치지 않으면
합이 부분보다 크다 — 이건 안 해봤다. [scripts/edge_combo.py](scripts/edge_combo.py).

## 이번엔 방법을 먼저 묶었다

12·13장에서 두 번 당했으므로 실행 **전에** 못 박고 시작했다.

1. 성분 5개를 먼저 고정(실행 후 교체 금지) 2. **가중치를 적합하지 않는다**(동일가중, 자유도 0)
3. **holdout 뒤쪽 25% 를 먼저 잘라두고 마지막에 한 번만 본다** 4. 위상 전수 평균으로만 판정
5. 비용선은 P0 보정 후 값 `0.0511%`(진입 다리 소실 수정 반영 — 종전 `0.0408%` 는 낙관)

성분: C1 추세(EMA20>50) / C2 모멘텀(RSI 60·40) / C3 상대강도(40봉 횡단면) /
C4 CM기울기 / C5 눌림. 메이저 core13, 1시간봉, 24시간 보유.

## 결과

```
[TRAIN 3.1년]              위상평균|중앙값    시각t   심볼t
C1 추세                    +0.0180|+0.0254   +0.76   +0.58
C2 모멘텀                  +0.0455|+0.0694   +1.35   +0.61
C3 상대강도                +0.0161|+0.0206   +0.55   +0.62
C4 CM기울기                +0.0181|+0.0226   +0.77   +0.60
C5 눌림                    +0.0120|+0.0117   +0.18   +0.29
** 합성 (>=3)              +0.0385|+0.0574   +1.13   +0.75

[HOLDOUT 375일]
C1 추세                    +0.0968|+0.0740   +1.96   +2.17
C2 모멘텀                  +0.1304|+0.1082   +1.73   +0.99   <- 성분 최고
C3 상대강도                +0.0370|-0.0056   +0.71   +0.46
C4 CM기울기                +0.0408|+0.0138   +0.81   +0.68
C5 눌림                    +0.0325|-0.0040   +0.51   +0.24
** 합성 (>=2)              +0.1116|+0.0732   +1.68   +1.30
** 합성 (>=3)              +0.1351|+0.0506   +1.66   +1.14
```

**기각이다. 두 가지 이유가 각각 독립적으로 치명적이다.**

**(1) 합성이 최고 성분을 못 이긴다.** 사전등록한 판정 기준이 "holdout 에서 합성이
성분 최고치보다 크고 비용선을 넘을 것"이었다. 합성(>=3) 중앙값 `+0.0506` 은
C2 단독 `+0.1082` 의 절반이다. **성분을 섞을수록 나빠졌다** — 정보가 독립이 아니라
서로 상쇄한다는 뜻이다. 조합 가설 자체가 틀렸다.

**(2) holdout 이 좋아 보이는 건 신호가 아니라 기간이다.** train -> holdout 에서
**다섯 성분이 전부 4~5배로 뛴다**(C1 +0.018->+0.097, C4 +0.018->+0.041).
서로 다른 정보를 쓰는 신호들이 나란히 같은 배율로 좋아졌다면 그건 각 신호의 힘이
아니라 **그 기간이 방향성 신호 전반에 유리했다**는 뜻이다. 9장의 생존편향, 13장의
위상과 같은 계열의 착시다.

**그리고 가장 중요한 것**: 표본이 훨씬 큰 TRAIN 3.1년에서 **다섯 성분과 합성 전부
t < 1.4, 심볼t < 0.8** 이다. 3년을 봐도 **0 과 구분되지 않는다.**

## 방향 축에 대한 현재 결론

일곱 번째 기각이다. 그런데 이번 것은 앞의 여섯과 달리 **"조합하면 될까"라는
마지막 구조적 가능성**을 닫았다. 남은 것은 개별 지표를 더 훑는 것뿐인데,
그건 이 문서가 일곱 번 보여준 다중검정 그 자체다.

정리하면 방향 축에서 관측된 사실은 하나로 수렴한다 —
**공개 봉 데이터로 얻는 방향 정보는 3년 표본에서 0과 구분되지 않고,
구분되는 것처럼 보일 때는 항상 표본·유니버스·위상·기간 중 하나의 착시였다.**

---

# 16. 비용선을 실측으로 교정했다 — 기각 목록은 그대로였다 (2026-09-01)

종전 단일 비용선 `0.11%`(수수료 0.07 + 슬리피지 0.04)는 **체결 방식을 특정하지 않은**
값이었다. e3 원장 1,017건으로 교정했다.

## 수수료는 하나가 아니라 경로별로 갈린다

건별 왕복 수수료율이 두 봉우리로 갈린다.

| 청산 | 낮은 무리 | 정상 무리 |
|---|---|---|
| TP_LIMIT | `0.0200%` n=310 | `0.0401%` n=190 (maker+maker) |
| STOP_EXCHANGE | `0.0199%` n=61 | `0.0504%` n=374 (maker+taker) |

낮은 무리는 **한쪽 다리만 기록된 것**이다(18장). 보정하면 왕복 `0.0408% -> 0.0511%`,
e3 순손익 `-169.30 -> -185.46`.

그리고 `edge_lab` 의 측정 규약(시가 진입 / 종가 청산)은 **둘 다 taker** 라 `0.10%` 를
써야 한다. 그래서 단일 상수를 버리고 두 선으로 나눴다.

    보수선(taker/taker) = 0.10 + 슬립x2   (기본 0.14)
    e3선  (maker/maker) = 0.04

두 선 **사이**는 "체결을 지정가로 만들 수 있으면 산다"는 **조건부** 칸이다.
이 칸이 없어서 후보가 통째로 기각되고 있었다는 것이 가설이었다.

## 슬리피지는 실측하지 못했다

`trade_ledger.slippage_pct` 는 `exit_price` 가 mark 추정치라 **체결가-마크 괴리**를 잰다.
방향보정해도 익절 `+0.68%` / 손절 `-0.72%` 로 부호가 청산방향과 상관된다 — 실행비용이
아니다. 가정으로 남기고 `--slip` 으로 노출했다.

## 재채점 결과 — 조건부 칸도 비어 있었다

`edge_lab --rescore`. 1h/180d 절제실험이 원인을 정확히 짚는다.

| 구성 | OO | ·O | 계 |
|---|---|---|---|
| 가드 없음 | 9 | 26 | **35** |
| + 상장폐지 포함 | 0 | 1 | **1** |
| + stride(위상평균)만 | 6 | 2 | **8** |
| 둘 다 | 0 | 0 | **0** |

**생존편향이 압도적 주범이다.** 유일 생존자(횡단면 모멘텀 40봉 12시간
`+0.0779|+0.0404`)도 stride 에서 죽는다. 3m/30d 는 0건.
덤: 메인봇 뼈대는 가드를 걸면 `-0.31 ~ -0.40%`(t `-2.3`)로 **유의하게 음수**다.

# 17. 시간대(위상) 계절성 — 홀드아웃 + 순열검정으로 기각 (2026-09-01)

13장에서 위상 후보를 기각했지만 **"시간대 효과가 실재하는가"는 검증된 적이 없었다.**
`phase_sweep.txt` 의 양수 구간(UTC 17~02시)이 흩어지지 않고 **연속으로 뭉쳐** 보였기
때문이다. `scripts/edge_phase_holdout.py` 로 따로 판정했다.

선결 확인: 1h 패널은 UTC 00:00 시작 + 봉 누락 0개라 **위상 k = UTC k시**가 정확히 성립.

* **B-1 홀드아웃 실패** — train/holdout 위상 프로파일 상관 `-0.043`.
  사전등록한 train 상위 8위상의 holdout `+0.1119` 는 하위 8위상 `+0.0925`,
  전체 평균 `+0.1022` 와 **사실상 같다.**
* **B-2 순열검정 실패** — 무작위 8위상 1,000회의 holdout 평균이 `+0.1002`.
  실제 선택은 백분위 **65.7%**(p=0.34). holdout 의 `+0.11` 은 위상 선택이 아니라
  **그 기간의 드리프트**다.
* **B-3 은 통과** — 신호 간 위상 프로파일 상관 평균 `+0.342`. 위상 구조 자체는 있다.

**결론: 위상 구조는 실재하지만 기간 특유의 공통 요인이지 반복되는 시계 효과가 아니다.**
`phase_sweep.txt` 가 매끄럽고 연속으로 보인 이유가 이것이다.
**교훈: 프로파일이 매끄럽다는 것은 실재의 증거가 아니다.** 기간 내 공통 요인이면
당연히 매끄럽다. 판정은 반드시 홀드아웃 + 순열귀무분포로 한다.

# 18. P0 — 진입 다리가 원장에서 통째로 빠지고 있었다 (2026-09-01)

원장 1,017건 중 **377건(37%)** 이 `real_commission` 에 한쪽 다리만 기록했다.
그중 **345건이 `realizedPnl != 0`** — realizedPnl 은 청산 체결에만 붙으므로
**빠진 쪽은 진입 다리**다.

## 원인

`scalp_bot_e3.py` 3596행에서 `entered_at = now_ts`, 즉 **폴링이 포지션을 발견한 시각**을
쓴다. 바로 아래 주석이 이 경로를 "주 진입 경로(유입의 93%)" 라고 적고 있다.
청산 때 체결조회 창이 `entered_at - 5초` 뿐이라, 눌림 지정가가 수 분 대기 후 체결되면
**진입 체결이 창 앞으로 빠진다.**

배제한 가설: `adopted`(21건뿐) / `drop_manual_trades`(로그 46건뿐) / 시간 국소성(없음,
25%->46% 점증).

## 수정

`trades_start_ms()` 로 창을 6시간으로 넓혔다. 이 포지션의 체결만 고르는 **진짜 필터는
`since_trade_id`** 이고 그건 진입 주문을 내기 **전**에 잡히므로(4520행) 넓혀도 안전하다.
없으면(dry-run·조회실패) 종전대로 5초를 유지한다.

**반대 방향 회귀도 막았다**: 바이낸스는 체결을 오래된 순으로 주고 `limit` 을 앞에서
자른다(462행). 창만 넓히고 기본 500 을 두면 이번엔 **청산 다리를 놓친다.**
`limit=1000` 을 명시했다.

## 두 번째 영향 — 계측에 남아 있다

`excursion_for -> rebuild_excursion` 의 MAE/MFE 창도 `entered_at` 에서 시작한다.
**한다리 377건은 거래 초반이 계측에서 빠져 있다.** 과거 원장 분석은 왕복 수수료율이
정상인 **640건(지연<=5초)** 으로 좁힐 것.

다만 그 640건으로 좁혀도 진단은 유지되고 오히려 강해진다:

| 표본 | 손절 MFE 중앙 | MFE<1% 비율 |
|---|---|---|
| 전체 1,017건 | +0.089 | 70% |
| **두다리 640건** | **+0.000** | **73%** |

**승패가 진입 직후에 갈린다**는 진단은 계측 결함의 산물이 아니다.

# 19. 원칙 0 의 "교차"는 코드에 없었다 — 재보니 넣을 이유도 없었다 (2026-09-01)

원칙 0 은 "3분봉 HullMA20 **방향/교차** + 4시간 EMA200" 이라고 적혀 있었는데,
**코드는 교차를 한 번도 쓰지 않는다.** `cr_up`/`cr_down`/`crossed` 는 계산되어 반환
dict 에 담기기만 하고 어디서도 소비되지 않는다(각 2회 등장 = 정의 + 반환).
실제 규칙은 1250행 `long_ok = ma_up and close > out1` — **상태(state)이지 교차가 아니다.**

함께 확인된 것: **"MTF" 기능도 꺼져 있다**(`use_current_resolution=True`). 4시간 필터는
지표의 MTF 가 아니라 따로 덧붙인 EMA200 이다. 두 번째 MA(`len2=50`)도 `doma2=False` 라
판정에 안 들어간다. **실제로 쓰는 것은 HullMA20 하나뿐이다.**

문서를 코드에 맞추기 전에, 그러면 한 번도 측정된 적 없는 축이 되므로 먼저 쟀다
(원본 Pine 정의 `cr_up = open<hma and close>hma`, 3개 사전등록):

    [교차] 관통 단독        -0.0091|-0.0049 (t-3.7/-4.3)   <- 현행 -0.0069 보다 나쁘다
    [교차] 관통+방향정합     -0.0045|-0.0037 (t-2.6/-1.2)
    [교차] 관통+방향+4h정합  -0.0026|+0.0013 (t-1.2/-0.5)

가드 후 `+0.0104` 로 양수처럼 보이는 조합이 있으나 심볼중앙값 `+0.0003`, 두 t 모두 1 대다.
그리고 원칙 1 쪽이 더 결정적이다: **5.8 건/심볼일 vs 현행 311.5 — 54배 감소.**
엣지도 없는데 빈도만 죽는다. -> **(B) 코드를 문서에 맞추는 선택지는 측정으로 닫혔다.**

# 20. "체결은 눌림"은 절반만 맞았다 (2026-09-01)

14장이 CM 의 부호를 뒤집는 것은 `close > HullMA20`(추격) 조건임을 특정했다. 그래서
"판정은 추격인데 체결은 눌림이니 버는 쪽은 체결 방식"이라는 해석이 있었다.
**그러나 두 눌림은 같은 것이 아니다.** 원장 체결가를 3분봉 HullMA20 과 직접 대조했다
(`scripts/analyze_fill_vs_hma.py`, 신뢰 가능한 478건).

    추격/방향정합  248건 (51.9%)  건당 -0.7299
    눌림/방향정합  187건 (39.1%)  건당 -0.3540
    => 눌림 자리 체결 비율 45.6%

* **EMA5 눌림은 얕다.** 절반 이상이 여전히 HullMA20 **위**에서 체결된다.
* 방향은 14장과 일치 — 눌림 자리가 **건당 2배 덜 잃는다.** 그러나 **눌림도 음수다.**
* 반사실: 478건이 **전부** 눌림/방향정합이었어도 `-266.85 -> -169.21`.
  개선 `+97.64` 에 그치고 여전히 깊은 적자이며, 빈도는 2.6배 준다(원칙 1 위반).
* 그 반사실조차 **낙관 상한**이다. 눌림을 강제하면 표본이 바뀐다 — HullMA 위로
  달아나는 거래(승자 다수)는 애초에 체결되지 않는다.

**결론: 진입을 눌림 쪽으로 옮기는 것은 수익 전환 경로가 아니다. 덜 잃을 뿐이다.**

# 21. 외부 매매법(블로그) 기각 — 그리고 네 후보가 사실 한 몸이었다 (2026-09-01)

사용자가 제공한 네이버 블로그의 `CM_Ultimate_MA_MTF_V2` "스나이퍼 콤보"를 사전등록해
쟀다. 라이브와 **네 군데가 다르다**: 차트 15분봉(라이브 3분) / HullMA 색 **전환** /
캔들 **관통** 필수 / 스무딩 3~5(라이브 2).
(원문 오류 정정: "Use Different Timeframe 을 **체크 해제**하고 240" 은 틀렸다.
해제하면 상위봉을 안 쓴다. 4h EMA200 을 얹으려면 체크해야 한다.)

* **문자 그대로의 규칙은 표본이 0건이다** — 색전환과 캔들관통이 같은 봉에 겹치지 않는다.
* **블로그 권장 스무딩 구간에서 부호가 뒤집힌다**: smoothe 2 `+0.0136` / 3 `-0.0461` /
  5 `+0.1199`. 신호가 아니라 노이즈다.
* 모든 블로그 신호 t<2. 원칙 1 도 충돌(0.1~0.3 건/심볼일).
* **다만 "보조지표로 쓸 때 값어치가 있다"는 주장 자체는 데이터와 맞는다** —
  색전환 단독 `-0.0199` -> +4h정합 `-0.0025`, 관통 단독 `-0.0123` -> +방향+4h `+0.0059`.
  CM 은 단독 진입으로는 음수, 필터로 얹으면 일관되게 개선된다.
  **그런데 개선을 만드는 것은 HullMA20 이 아니라 4시간 EMA200 쪽이다.**

## 이 장의 핵심 — 네 후보가 하나였다

15m 에서 조건부 통과한 네 개를 국면 3등분하니:

|  | 구간1 | 구간2 | 구간3 |
|---|---|---|---|
| [분해] 눌림+4h정합 | +0.2137 | **-0.0697** | +0.0909 |
| EMA추세 단독 | +0.1161 | **-0.0228** | +0.1095 |
| RSI 모멘텀 40/60 | +0.0720 | **-0.0088** | +0.1242 |
| 횡단면 모멘텀 80봉 | +0.2636 | **-0.0091** | +0.2087 |

**넷이 전부 양-음-양이고 구간 2 에서 동시에 죽는다. 하나의 국면 요인이다.**

그리고 상장폐지 15분봉 143심볼을 새로 받아(`edge_delisted.py --interval 15m`)
223심볼로 재측정하자 **통과 0건**으로 확정됐다.

| | 생존자만 80심볼 | +상장폐지 223심볼 |
|---|---|---|
| [분해] 눌림+4h정합 | +0.0999 / +0.0500 | +0.1075 / +0.0445 (t+1.7) |
| EMA추세 단독 | +0.0734 / +0.0448 | +0.0459 / +0.0120 |
| RSI 모멘텀 40/60 | +0.0948 / +0.0475 | **+0.0145 / +0.0120** |
| 횡단면 모멘텀 80봉 | +0.1707 / +0.0804 | +0.0672 / +0.0378 |

**생존편향은 1h 만의 문제가 아니라 전 시간대의 문제다.**

**교훈: 여러 후보가 동시에 통과하면 기뻐할 게 아니라 국면 분해부터 하라.
같이 살고 같이 죽으면 후보가 여러 개인 게 아니라 하나다.**

# 22. 호가 축 — 기록기가 죽어 있었다. 워치독과 사전등록 하네스 (2026-09-01)

## 기록기가 8.7시간 만에 죽었다

`2026-08-31 22:15 UTC` 에 멈춰 발견까지 **1.9시간을 잃었다.** stderr 0바이트 /
stdout 이 정상 사이클 중간에 끊김 / 종료 메시지 없음 -> 예외가 아니라 **강제 종료**다.

**진짜 문제는 죽은 게 아니라 아무도 되살리지 않는다는 것이다.** 구 `start_obook.ps1` 은
`Start-Process` 한 번뿐이다. 이 데이터는 2~3개월을 쌓아야 하는데 **한 번만 죽어도
나머지가 통째로 비고 아무도 모른다.** 실제로 하루도 못 갔다.

-> **`start_obook_watchdog.ps1` 을 쓴다.** 프로세스 사망과 하트비트 정체를 둘 다 감시하고
기동/사망을 `logs/obook_watchdog.log` 에 남긴다.

만들면서 낸 버그 둘(재발 방지로 기록):
1. **낡은 하트비트로 오판** — `heartbeat.txt` 는 이전 실행이 남긴 값이라 갓 띄운
   기록기를 즉시 정체로 보고 죽였다(무한 재기동 실측). 기동 시각을 잡아 두고
   하트비트가 그보다 오래면 '아직 안 쓴 것'으로 처리한다.
2. **임계가 하트비트 주기보다 짧았다** — 하트비트는 `--status-sec`(600초)마다만
   쓰인다. 임계 300초면 **건강한 기록기를 10분마다 죽인다.** 1800초(3주기)로 올렸다.

## 데이터 품질은 좋다. 지뢰 하나만 빼고

8.72시간 / 83심볼 / 521,157행. 필드는 최우선호가·수량, 5호가 누적, 매수·매도 체결량,
체결건수 — OBI 와 TFI 를 만들기에 충분하다.

    15초 넘는 공백  0건   (재접속 1회에도 끊김 없음)
    bid/ask = 0     0건 / 크로스 0건 / depth5 = 0  0건
    last_price = 0  13.8%   <- 지뢰

**`last_price=0` 은 "그 5초에 체결 없음"인데 0을 가격처럼 적는다.** 이걸로 수익률을
계산하면 -100% 가 섞여 3개월치가 통째로 오염된다. **반드시 중간가 `(bid+ask)/2`** 를
쓸 것(bid/ask 는 결측 0건). CSV 는 append + UTC 롤오버 + 지난 날짜 자동 gzip 이라
재시작해도 안전하다(중복 0행 확인).

## 사전등록 하네스 — `scripts/obook_lab.py`

**데이터가 8.7시간뿐인 지금 규칙을 못박아 둔다.** 16~21장에서 여덟 번 기각하며 매번
걸린 함정이 **결과를 본 뒤 규칙을 고르는 것**이었기 때문이다. 호가는 마지막 축이므로
그 함정을 원천 차단한다.

* 자(尺)는 `edge_lab` 과 동일하다 — `edge_lab.stats` 를 그대로 임포트한다.
  드리프트 중립 + 이중 클러스터 t + 심볼중앙값 + stride + 이중 비용선.
* **신호는 8개뿐이고 고정이다**: OBI1/OBI5/TFI 순방향, OBI5·TFI **역방향**,
  OBI5+TFI 동의, OBI5·TFI 횡단면 극단 10%.
  역방향을 미리 넣는 이유 — 마이크로구조는 평균회귀가 흔해 부호를 단정할 수 없다.
  순방향만 등록하면 음수가 나왔을 때 "뒤집으면 되잖아"가 **사후 선택**이 된다.
* 지평 1/5/15/60분(15분 = 라이브 보유). stride 는 보유 버킷수와 같게.
* 판정은 원칙 0 보강 2 **세 조건 전부** + 비용선.
* **표본이 최소 한 달 + 세 국면을 덮기 전에는 판정하지 않는다.**

배관 점검(11.6시간, 60초 버킷)은 정상 동작을 확인했다 — 순방향/역방향이 정확한
거울상이고 전 신호가 0 근처에 t<1.5, 표본 부족 경고가 뜬다. **판정이 아니다.**

## 오늘의 종합

수익 쪽 전진은 **0** 이다. 여덟 개를 더 기각했고 새 후보는 없다. 대신 얻은 것은:

* P0 버그 1건 수정(진입 다리 소실) — 원장이 9.5% 낙관 편향돼 있었다
* 문서 오류 3건 정정(교차 / MTF / 비용선)
* 데이터 수집 인프라 복구 + 워치독
* 호가 축 사전등록 완료

**앞으로의 측정이 정직해졌다는 것이 오늘의 실질 성과다.** 호가 축은 이제 시간이
일한다 — 11월경 표본이 서면 위 하네스를 그대로 돌린다.

---

# 23. 다시간대 스윕 — 처음으로 성격이 다른 기각 (2026-09-01)

"1/3/5/15분·1시간봉을 전부 확인하면?" 이라는 질문을 실행했다. 1분봉(14일)·5분봉(30일)
캐시를 새로 받아 다섯 시간대를 다 쟀다. **통과 0건.** 그런데 이번 것은 앞의 아홉 번과
성격이 다르므로 따로 남긴다.

## 실행 전에 판정 규칙을 못박았다 (커밋 efe43c5)

신호 39 x 지평 4 x 시간대 5 = **780회 검정**이다. 세 조건이 엄격해도 순수 잡음의
통과확률을 0.3%로만 잡으면 **기대 거짓양성 2.3개**다. 그래서 결과를 보기 **전에**
CLAUDE.md 원칙 0 보강 10 으로 다음을 커밋했다.

* 생존자 1~3개는 발견이 아니다. 잡음이 만드는 개수와 같다.
* 스윕은 판정이 아니라 **선별**이다. 생존자는 후보가 아니라 **홀드아웃 대상**이다.
* 생존자가 4개 이상이고 **국면 분해에서 서로 다른 시점에** 죽어야 볼 값어치가 있다.

이 사전등록이 이번 장의 해석을 지탱한다. 없었으면 아래 결과를 보고 규칙을 고르게 된다.

## 1분 해상도에서는 시장이 평균회귀한다

추세추종 계열이 **전부 유의하게 음수**다(1분봉 14일, 보유 5분, stride 5).

    CM 전체        -0.0072  (t-4.1/-5.0)
    볼밴돌파        -0.0212  (t-4.3/-3.2)
    RSI 모멘텀      -0.0115  (t-4.9/-3.8)
    횡단면 모멘텀    -0.0128  (t-4.8/-5.2)

**라이브 e3 는 정확히 그 반대편에 서 있다.** 1분 해상도에서 지는 쪽에 걸고 있었다.

## 역추세는 조건 2·3 과 심볼 집중도까지 통과한다

    볼밴 역추세 (1분봉, 보유 5분)
      전체    +0.0212|+0.0137  (t+4.3/+3.2)   <- 두 t 모두 2 이상
      구간    +0.0195 / +0.0263 / +0.0177     <- 3구간 전부 양수
      심볼    81개 중 59개(73%) 양수. 상위 +0.25 / 하위 -0.17 로 대칭
      LONG만  +0.0306 (t+3.2/+3.1)   SHORT만 +0.0122 (t+2.4/+1.4)

    횡단면 역추세 20봉 (1분봉, 보유 5분)
      전체    +0.0128|+0.0121  (t+4.8/+5.2)
      구간    +0.0075 / +0.0160 / +0.0149

지금까지 후보는 전부 조건 2(t)나 조건 3(국면)에서 죽었다. **이건 조건 1(비용선)에서만
막힌다.** 오늘 처음이다.

**LONG 이 SHORT 보다 강한 것도 의미가 있다.** 생존편향은 숏 엣지를 부풀리는데
여기선 반대 패턴이다 — 생존편향 산물이 아니라는 추가 증거다.
(실제로 상장폐지 145심볼의 2026-08 1분봉 덤프가 0개다. 전부 그 전에 폐지됐고,
14일 창 안에 죽은 심볼이 사실상 없어 이 표본은 그 편향에 거의 노출되지 않는다.)

## 구조가 시간대에 걸쳐 단조롭다

보유를 15분으로 고정하고 신호봉만 바꾸면:

| 신호봉 | 볼밴 역추세 | 횡단면 역추세 20봉 |
|---|---|---|
| 1m | +0.0158 (t+1.6) | +0.0039 |
| 3m | +0.0065 (t+0.8) | +0.0119 (t+2.3) |
| 5m | +0.0031 (t+0.4) | +0.0057 |
| 15m | +0.0027 (t+0.4) | -0.0004 |
| 1h | **-0.0611** | **-0.0421** (t-4.8) |

신호봉이 짧을수록 역추세가 강해지고 1시간에서 **모멘텀으로 부호가 뒤집힌다.**
한 시간대의 우연이 아니라 다섯 시간대에 걸친 일관된 구조다.

## 그런데 벽이 두 겹이다

**첫째, 비용선.** 최대치 `+0.0212` 가 maker 선 `0.04%` 의 53% 다. 그리고 엣지가 커지는
방향(더 짧은 신호봉·더 짧은 보유)이 곧 **거래 횟수가 늘어 비용이 더 지배하는 방향**이다.
효율적 시장의 정확한 서명 — 효과는 실재하되 거래비용 안쪽에 갇혀 있다.

수수료 등급을 낮춰서 닿을 수 있는 값이 아니다(슬리피지 0 가정):

    현행 maker+maker   왕복 0.0400%   여유 -0.0188
    + BNB 10%          왕복 0.0360%   여유 -0.0148
    VIP1               왕복 0.0320%   여유 -0.0108
    VIP1 + BNB         왕복 0.0288%   여유 -0.0076
    VIP3 + BNB         왕복 0.0252%   여유 -0.0040
    VIP5 + BNB         왕복 0.0216%   여유 -0.0004   <- 여기까지 가도 본전

**둘째, 그리고 이쪽이 더 무겁다 — 비교 자체가 관대하다.**
`edge_lab` 규약은 **다음봉 시가 진입 / 종가 청산 = 둘 다 taker** 다. 그 값을 maker 선
`0.04%` 와 비교한 것이므로 이미 봐준 비교다. 측정된 그대로 taker 로 체결한다면
정확한 비용선은 `0.14%` 이고 **엣지는 그 15% 에 불과하다.**

maker 선을 쓰려면 체결을 지정가로 바꿔야 하는데, 그러면 **체결되는 거래의 집합이
바뀐다** — 밴드를 뚫고 그대로 달아나는 거래는 체결되지 않는다. 20장의 "체결은 눌림"
분석에서 확인한 것과 정확히 같은 함정이다. 즉 `+0.0212` 는 **taker 로 체결했을 때의
값**이고, maker 로 바꾸면 그 숫자가 유지된다는 보장이 없다.

## 60일로 늘려 확인했다 — 둘로 갈렸다

14일 표본은 기각 서명 2번("표본을 넓히면 사라진다")에 정면으로 노출돼 있었다.
그래서 1분봉 60일(81심볼 x 87,037봉)을 새로 받아 다시 쟀다.

| 신호 | 14일 | 60일 | 판정 |
|---|---|---|---|
| 볼밴 역추세 | +0.0212 (t+4.3/+3.2) | **+0.0059 (t+1.7/+2.0)** | **기각** — 28%로 소멸, 시각 t<2 |
| 횡단면 역추세 20봉 | +0.0128 (t+4.8/+5.2) | **+0.0081 (t+7.0/+6.9)** | 조건 2·3 통과 |

**볼밴 역추세는 그 14일의 것이었다.** 열 번째 기각 서명이 또 재현됐다.

**횡단면 역추세 20봉은 살아남았다.** 그리고 살아남은 방식이 중요하다 —
표본이 4배 늘었을 때 **크기는 줄었는데(0.0128 -> 0.0081) t 는 오히려 강해졌다
(5 -> 7).** 노이즈였다면 t 가 유지되거나 줄어든다. 크기가 준 것은 14일 기간이
유리했던 만큼이고, 남은 `+0.0081` 이 안정된 실체다.

    횡단면 역추세 20봉 (1분봉 60일, 보유 5분, stride 5)
      전체    +0.0081|+0.0063  (t+7.0/+6.9)   n=592,091
      구간1   +0.0077|+0.0090  (t+4.8/+7.3)
      구간2   +0.0074|+0.0035  (t+3.5/+4.6)
      구간3   +0.0093|+0.0065  (t+4.1/+4.6)

조건 2 통과(두 t 가 7 대). 조건 3 통과(3구간 전부 양수, 각 구간 t 도 전부 3 이상).
**조건 1 만 못 넘는다 — `+0.0081` 은 maker 비용선 `0.04%` 의 20% 다.**

## 그리고 현행 전략이 60일 표본에서 유의하게 음수임이 확정됐다

    CM 전체 [현행뼈대] (1분봉 60일, 보유 5분)
      전체    -0.0038|-0.0033  (t-5.3/-5.8)   n=919,628
      구간    -0.0048 / -0.0041 / -0.0025     <- 3구간 전부 음수

**표본 92만 건, 두 t 가 -5 대, 3구간 전부 음수.** 이제 "표본이 작아서"라는 변명이
불가능하다. 라이브 e3 의 진입 방향은 반복 가능하게 손해다.

## 이 장의 결론

**열 번째 기각이다. 그러나 처음으로 "실재하지만 작다"가 확인됐다.**

지금까지 아홉 번은 전부 **착시**였다(표본·유니버스·위상·기간·중복 중 하나).
이번 것은 다르다 — 1분 해상도 역추세는 60일 59만 건에서 t+7 로, 국면을 넘어
반복된다. **실재한다. 다만 비용선의 20% 다.**

수수료로 닿을 수 있는 거리가 아니다. VIP5+BNB(왕복 `0.0216%`)까지 가도
`+0.0081` 은 못 넘는다. 그리고 그 비교조차 관대하다(위의 taker/maker 논점 참조).

**대신 이 결과는 마지막 남은 축과 처음으로 연결된다.** 1분 해상도에서 평균회귀가
실재한다면, 그보다 **12배 미세한 5초 호가·주문흐름 데이터**는 같은 현상을 더
날카롭게 — 즉 비용 안쪽으로 — 잡아낼 여지가 있다. 지금 쌓고 있는 것이 정확히
그 데이터다. 오늘 처음으로 호가 축을 볼 **구체적인 이유**가 생겼다.

다만 순서를 지킬 것: `obook_lab.py` 의 사전등록(22장)은 이 결과를 보기 전에
못박은 것이므로 그대로 쓴다. "역추세가 될 것 같으니 역추세 신호를 더 넣자"는
사후 선택이고, 이미 등록된 8개에 OBI/TFI **역방향**이 포함돼 있다.

---

# 24. 볼밴을 진입 트리거로 — 그리고 위상 0 을 판정에 쓴 오류 (2026-09-01)

20장에서 "눌림 자리가 추격보다 건당 2배 덜 잃는데 EMA5 눌림은 얕아서 45.6%만
눌림에 든다"가 나왔다. 그러면 **더 깊은 눌림선**을 쓰면 어떤가 — 볼밴 하단/중심선을
진입 트리거로 사전등록해 쟀다. 방향은 CM **기울기만** 쓴다(CM 전체는 `close > hma`
를 포함해 볼밴 하단과 논리적으로 배타다, 보강 9).

## 중간 보고가 틀렸다 — 위상 0 을 판정에 썼다

처음에 이렇게 보고했다: `[볼밴눌림] 기울기 + 중심선 + 4h` 가 `+0.0353|+0.0273`
(t `+3.1`/`+5.3`) 으로 **"오늘 세션에서 가장 강한 후보"**. **틀렸다.**

    위상 0 만        +0.0353|+0.0273  (t+3.1/+5.3)
    위상 4개 평균     +0.0095|+0.0077  (평균 시각t +0.88)
    위상 범위        -0.0090 ~ +0.0353          <- 부호가 갈린다

**위상 0 이 네 위상 중 최대였다.** 기존 `[분해] 눌림 + 4h정합` 도 같다
(위상0 `+0.0191|+0.0066` vs 위상평균 `+0.0093|-0.0008`, 중앙값 음수).
위상평균끼리 비교하면 `+0.0095` vs `+0.0093` 으로 **사실상 차이가 없어,
"볼밴 중심선이 개선 방향"이라는 결론도 근거가 약하다.**

### ⚠ 원인은 도구 사용이다 (다음 세션이 반드시 읽을 것)

`edge_lab` 의 기본 표(`table()`)는 **stride 위상 0 만** 적용한다.
위상 평균은 `--rescore` / `--phase-sweep` 에만 들어 있다.
`--split` 으로 국면을 보면서 그 표를 판정에 쓰면 **위상 운을 엣지로 읽는다.**

12장 후보를 죽인 것이 바로 이 함정이었고(792034c), 코드 주석에도
"한 시점만 좋은 신호는 엣지가 아니라 위상 운이다. 판정은 위상평균으로 한다"고
적혀 있다. **그 함정에 이번엔 도구 사용 방식으로 다시 걸렸다.**

**규칙: 판정은 반드시 `--rescore` 또는 `--phase-sweep` 으로 한다.
기본 표는 탐색용이지 판정용이 아니다.**

## 아웃오브샘플 확정 기각

규칙이 이미 고정돼 있어 "고를 것이 없으므로" 위상 검정과는 다른 도구가 필요했다.
[scripts/edge_signal_holdout.py](scripts/edge_signal_holdout.py) 를 새로 만들었다
(판정 기준을 실행 **전에** 커밋 — 52d8047).

15m 90일 + 상장폐지 223심볼, train 60% / holdout 40%, 위상 전수:

    train     +0.0131|+0.0177  t +0.64/+1.49
    holdout   +0.0042|-0.0099  t +0.60/+0.39    <- 심볼중앙값 음수
    3등분     -0.0036 / +0.0166 / -0.0004       <- 부호 갈림

**심볼 셔플 순열검정 500회** — 각 시각의 신호 개수와 시간 구조는 보존하고
"어느 심볼인가"라는 정보만 파괴한다:

    귀무분포 평균 -0.0003   95분위 +0.0161   최대 +0.0289
    실제 신호     +0.0042 -> 백분위 69.4%  (p=0.306)

**무작위로 심볼을 뒤섞어도 같은 성적이 난다.** 무작위 셔플의 95분위가 실제 신호의
4배다. 이 규칙이 "지금 이 종목을 사라"고 말하는 부분에 정보가 없다는 뜻이다.
네 관문 전부 실패, 열두 번째 기각.

## 이 장에서 얻은 것

1. **`edge_signal_holdout.py`** — 고정된 규칙을 아웃오브샘플 + 심볼셔플 순열로
   판정하는 도구. 위상 검정(`edge_phase_holdout.py`)이 **선택**을 검정한다면
   이것은 **정보**를 검정한다. 앞으로 어떤 후보든 마지막 관문으로 쓴다.
   호가 축 후보가 나왔을 때도 그대로 쓸 수 있다.
2. **빈도 계산의 정정**: 신호 빈도(건/심볼일)와 실거래 빈도는 다르다.
   라이브는 CM 신호 하루 25,854개 중 **203건(0.8%)만** 소화한다 — 슬롯 10개가
   병목이지 신호가 병목이 아니다. 볼밴눌림 신호 75개/일은 슬롯 용량(하루 960건)
   안이고 실거래 기준 3배 감소일 뿐이다. **"346배 감소"라고 쓴 것은 신호빈도끼리
   비교한 오해를 부르는 수치였다.** 원칙 1 을 논할 때는 실거래 환산으로 볼 것.
3. **판정 도구를 잘못 쓰면 가드가 있어도 소용없다.** 오늘 가드(상장폐지·stride·
   드리프트·이중t)는 전부 걸려 있었는데도 위상 0 하나 때문에 4배 부풀려진 값을
   후보로 보고했다. 가드는 켜는 것으로 끝나지 않고 **읽는 방법까지 맞아야 한다.**

---

# 25. 방향 예측을 버리고 구조적 프리미엄으로 — 세 축 (2026-09-01~02)

## 이 장이 생긴 계기

24장까지 열다섯 번을 기각한 뒤 "매매법 자체가 틀린 것인가"를 정리했는데,
사용자가 이렇게 지적했다.

> **"남들은 어떻게든 봇으로 수익을 만들고 있어 우리가 못 찾았을 뿐이야"**

**맞는 말이었다.** 그때까지 기각한 것은 전부 *"공개 봉 데이터로 **방향**을 맞히는
것"* 이지 자동매매 전반이 아니다. 실제로 수익을 내는 봇들은 대개 그 조합 밖에 있다
(마켓메이킹 / 레이턴시 / 캐리 / 거래소 간 차익). **탐색 범위가 좁았던 것이다.**

그래서 **예측을 하지 않는 축**으로 방향을 틀었다. 사용자 우선순위:
1) 거래소 간 차익  2) 베이시스 캐리  3) 펀딩 캐리.

## 예비 발견 — 펀딩 캐리가 처음으로 홀드아웃을 통과했다

`edge_lab` 은 방향 신호를 잰다. 그래서 펀딩도 "펀딩 높으면 숏" 같은 **방향 예측**
으로만 테스트됐고, **델타 중립 캐리**(현물 롱 + 선물 숏, 펀딩만 수취)는 방향을
예측하지 않아 edge_lab 이 **구조적으로 측정할 수 없었다.** 한 번도 잰 적 없는 축이다.

[funding_carry_lab.py](scripts/funding_carry_lab.py) 를 사전등록 후 실행
(136심볼 = 생존 97 + 상장폐지 39, 3,458회 정산, train 60%/holdout 40%):

    관문2 train->holdout 상관       +0.211                    통과
    관문3 holdout 3등분    +0.0023/+0.0014/+0.0023 전부 양수  통과
    관문4 심볼셔플 순열 1,000회  백분위 100.0% (p=0.000)      통과
    관문1 비용차감 후 총수익        +2.567%  (연 +2.2%)       통과

**관문 4 가 특히 강하다** — 무작위 15심볼 평균이 `-0.00137` 인데 실제 선택은
`+0.00203` 이고 1,000회 중 **한 번도 못 넘었다.** 24장의 볼밴눌림이 백분위 69.4%
(p=0.31)로 죽은 것과 정반대다.

**왜 다른가**: 펀딩이 양수인 이유가 예측이 아니라 **시장 구조**(레버리지 롱 수요
과잉, 76.2%의 시간 동안 양수)이기 때문이다. 구조는 지속되므로 train->holdout 상관이
남는다. 13장의 위상은 `-0.043` 이었다.

## ① 거래소 간 차익 — 기각

[xex_fetch.py](scripts/xex_fetch.py) / [xex_lab.py](scripts/xex_lab.py) 신설
(사전등록 후 실행). Binance x Bybit, 27심볼 7일, 1분봉 종가.
사용자 요청으로 **관측주기 1/3/5/15분**을 전부 봤다.

    |가격차| 중앙 4.4bp / p90 20.0 / p99 147 / 최대 772
    비용선 20bp 초과 시간비율        약 10%           관문1 통과
    최대 수렴폭 +5.84bp (관측 15분)                   관문2 통과
    비용 차감   -14.16bp                             관문3 실패
    train +5.89 / holdout +5.39bp                    관문4 실패

**괴리는 벌어지고 수렴도 하는데 수렴폭이 비용의 29% 다.** 20bp 괴리가 0 으로 가는
게 아니라 **15bp 괴리로 옮겨갈 뿐**이다 — 두 거래소 사이에 구조적 가격차가 남는다.

**관측주기가 길수록 수렴폭이 컸다**(1분 +2.82 -> 15분 +5.84). 예상과 반대다.
속도 경쟁 영역이면 1분이 가장 좋아야 하는데 그렇지 않다는 것은
**빠른 기회가 이미 다 걷혀 있고 남은 것은 느린 구조적 괴리**라는 뜻이다.

## ② 베이시스 캐리 — 기각. 그리고 구조적으로 알트 불가

바이낸스 USDⓈ-M 분기물은 **BTC·ETH 2종뿐**이다(만기 2026-09-25 / 12-25).
사용자 요구가 "비트 포함 **거래량 많은 알트**"이므로 이 축은 요구를 만족할 수 없다.

    BTC 베이시스 +4.38%/년 - 펀딩 +2.58%/년 = +1.80%  비용 0.42% -> +1.38%/년
    ETH 베이시스 +2.14%/년 - 펀딩 +1.76%/년 = +0.38%  비용 0.42% -> 적자

자본이 두 다리에 묶이므로 BTC 도 **총자본 대비 약 +0.7%/년**. 무위험 이자율 아래다.

**베이시스와 펀딩이 거의 같은 크기인 것이 핵심**이다. 둘 다 레버리지 비용의
표현이라 차익거래가 정렬시킨다. 그 차액만 남는데 그게 비용 수준이다.

## ③ 펀딩 캐리에 유동성 필터 — 기각

보강 17 의 최대 우려(**상위 펀딩 심볼이 저유동성 소형주**: KOMAUSDT/CLOUSDT/
AKEUSDT)와 사용자 요구("거래량 많은 알트")가 같은 것을 가리켜 재측정했다.
거래대금은 1분봉 60일 캐시의 `종가 x 거래량` 중앙값으로 산출(81심볼).

    필터 없음(136심볼)  K=15  연 +2.22%   전 관문 통과   상장폐지 33%
    거래대금 상위 81    K=15  연 -0.13%   관문 1·3·4 실패
    거래대금 상위 40    K=15  연 -5.13%   관문 1·3·4 실패
    거래대금 상위 20    K=15  연 +0.84%   관문 4 실패

**상위 20 의 실패 방식이 특이하다** — train->holdout 상관이 `+0.653` 으로 **오히려
훨씬 강해지는데도** 관문 4 가 실패한다. 20개 중 15개를 고르면 그건 선택이 아니라
거의 전부라, 무작위 추출과 결과가 같아지기 때문이다
(무작위 `+0.00070` vs 실제 `+0.00077`).

## 이 장의 결론 — 유동성과 수익이 정면으로 상충한다

    저유동성 포함  ->  연 +2.2%,  전 관문 통과,  상장폐지 33% 노출
    대형 알트만    ->  연 +0.8%,  선택이 무의미해져 관문 4 실패

**유동성이 좋다는 것은 이미 남들이 다 걷어갔다는 뜻이다.** 타협의 문제가 아니라
구조다. 큰 자금은 저유동성에서 못 하고, 개인은 저유동성 위험(상장폐지 33%, 넓은
스프레드)을 감당하기 어렵다. 그 사이의 틈이 개인이 접근 가능한 프리미엄의 크기다.

## 세 축의 공통 서명

    1분 평균회귀(볼밴)   +0.0127%      비용선의 32%
    거래소 간 차익        +5.84bp       비용선의 29%
    펀딩/베이시스 캐리     연 +0.8~2.2%  비용선 근처

**서로 완전히 다른 축인데 숫자가 같은 자리에 모인다.** 우연이 아니라
**시장이 비용선까지 정확히 차익을 걷어간다**는 신호로 읽는 것이 맞다.

## 다음 세션이 알아야 할 것

* **탐색 순서를 바꿀 것.** "가격이 어디로 갈까"를 먼저 뒤지지 말 것. 구조적
  프리미엄(캐리·베이시스·유동성 제공)이 홀드아웃 지속성에서 압도적으로 낫다
  (상관 +0.211~+0.653 vs 방향 예측 -0.043).
* **호가 축을 볼 관점도 바뀐다.** 지금까지 "방향을 맞히는 신호"로 보려 했는데,
  1분 평균회귀(비용선의 32%)는 **마켓메이커가 수익화하는 바로 그 현상**이다.
  우리는 그것을 **스프레드를 내면서** 잡으려 했다. 스프레드를 **버는** 쪽에 서면
  부호가 바뀐다. `obook_lab.py` 의 사전등록 8개는 전자를 향해 있으므로,
  마켓메이킹 관점 측정("어느 가격에 얼마나 걸어야 체결되고 얼마나 역선택당하는가")
  은 **따로 설계해야 한다.**
* 새 도구 셋: `funding_carry_lab.py` / `xex_fetch.py` / `xex_lab.py`.
  전부 사전등록을 **실행 전에 커밋**하고 돌렸다.

# 26. 인프라 — 기록기가 또 죽었다. 감시 구조를 뒤집었다 (2026-09-02)

## 27시간을 잃었다

22장에서 만든 무한루프 워치독이 **두 번째로 실패**했다.

    1차(09-01)  기록기와 워치독이 함께 사망        ->  1.9시간 유실
    2차(09-02)  워치독 프로세스는 살아 있는데
                루프가 멈춰(절전 추정) 재시작 시도 자체가 없음 -> **27시간 유실**

2차가 특히 나쁘다. 워치독 로그에 09-01 09:19 이후 항목이 **하나도 없다.**
프로세스는 존재하는데 아무 일도 하지 않았다.

## 원인이 같다 — 감시자 자신이 오래 살아야 하는 구조

두 실패 모두 **감시자가 장기 실행 프로세스**라는 점에서 왔다. 장기 프로세스는
절전·세션 종료·핸들 이상에 취약하고, 그게 정확히 감시하려던 대상의 실패 모드다.

**그래서 뒤집었다.** [obook_ensure.ps1](obook_ensure.ps1) 은
*"기록기 살아있나? 없으면 띄운다"* 만 하고 **즉시 끝난다.**
오래 사는 주체는 **Windows 작업 스케줄러**(`ObookRecorderEnsure`, 5분 주기)이고,
스케줄러는 절전·재부팅·로그오프를 견딘다.

## 등록에서 걸린 것 둘 (재발 방지)

* `RepetitionDuration` 에 `[TimeSpan]::MaxValue` 를 주면 **XML 범위 초과로 거부**
  된다(`P99999999DT23H59M59S`). 유한값(예: 3650일)을 주거나 생략할 것.
* `Register-ScheduledTask` 는 **권한 부족(Access is denied)** 으로 실패했다.
  **`schtasks /Create /TN ... /SC MINUTE /MO 5 /F`(사용자 수준)로 성공**했다.

## 부가 안전장치

`obook_ensure.ps1` 은 **뿌리 프로세스가 2개를 넘으면 최신 것만 남긴다.**
오늘 워치독 중복으로 기록기가 두 개 뜰 뻔했고, 그러면 CSV 에 중복 행이 들어간다
(실제로 확인했을 때는 중복 0행이었다). 기록기는 자식 파이썬을 하나 띄우므로
**정상 상태의 프로세스 수는 2개**다 — 이 값을 헬스체크 기준으로 쓸 것.

## 검증

    작업 등록      ObookRecorderEnsure, 5분 주기, 상태 준비
    수동 실행      LastTaskResult = 0 (정상)
    기록기         프로세스 2개(부모+자식) = 1 인스턴스 유지
    ensure 로그    비어 있음 = 정상이라 개입하지 않음(의도된 동작)
    CSV            정상 증가, 중복 0행

구 [start_obook_watchdog.ps1](start_obook_watchdog.ps1) 은 **더 이상 쓰지 않는다.**
이력 참고용으로만 남긴다.
